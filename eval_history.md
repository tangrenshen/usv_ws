# 评估器历史与基线记录

## 关键结论

### 32.6%之谜裁决（2026-07-21）

**核心发现**：历史代码（main.cpp.bak）在旧评估器下显示32.6%召回率，是坏尺子（0.05s容差配3Hz真值）的幻觉。

| 代码版本 | 旧评估器 | 新评估器(3次中值) | 备注 |
|----------|----------|------------------|------|
| 历史代码(main.cpp.bak) | 32.6% | **12.4%** | 旧尺子虚高20pp |
| 当前代码 | 5% | **4.0%**(估算) | 真实召回率 |

**结论**：
- **32.6%是旧评估器的幻觉**——历史代码真实召回率只有12.4%
- **当前代码确实退步了**——从12.4%降到约4%，存在真实退化
- **误检率也变差**——历史80.4% → 当前93%+

### 评估器三把坏尺子

| 尺子 | 问题 | 修复方式 | 修复时间 |
|------|------|----------|----------|
| 时间基准偏移 | GT时间戳与检测时间戳不在同一基准 | 自动计算偏移量对齐 | 2026-07-xx |
| QoS计数快照 | 统计口径不一致导致丢包率虚高 | 统一统计口径 | 2026-07-xx |
| 时间容差失配 | 3Hz真值用0.05s容差配10Hz检测 | 线性时间插值匹配 | 2026-07-21 |

## 真实基线（新评估器下）

### 当前代码（2026-07-21）

| 指标 | 数值 |
|------|------|
| 召回率 | ~4% |
| 误检率 | ~93% |
| 位置误差均值 | ~1.2m |

### 漏检归因（新评估器）

| 问题类型 | 占比 | 说明 |
|----------|------|------|
| 聚类阶段问题 | 79.8% | 无候选簇在3m范围内 |
| 分类阶段问题 | 20.2% | 有候选簇但未判定为boat |

### 分桶召回率

| 距离范围 | 真值总数 | 命中 | 召回率 |
|----------|----------|------|--------|
| [0-20m) | 2154 | 333 | 15.5% |
| [20-40m) | 3276 | 16 | 0.5% |
| [40-60m) | 1890 | 7 | 0.4% |

### 误检特征

| 特征 | 数值 |
|------|------|
| 误检总数 | 4977 |
| 平均距离 | 782.2m |
| 近距误检(<60m) | 74个(1.5%) |

## 战略建议

1. **先解决聚类阶段问题(79.8%)**——这是最大的低垂果实，需要优化点云密度/覆盖（⚠️此结论基于坏基线3.2%版本，需在12.4%基线上重新验证，见下）
2. **降门槛解封**——误检24.6%（但近距仅1.5%），降门槛是正和交易
3. ~~当前代码存在真实退步——需要diff历史代码找出被改坏的部分~~ **已裁决，见下**
4. **时间累积通道**——在聚类问题解决前暂时不上

## 退步裁决与基线锁定（2026-07-23）

**结论：main.cpp 存在真实退步，已裁决并修复。**

| 版本 | main.cpp | 召回率 | 误检率 | 复现次数 |
|------|----------|--------|--------|----------|
| 好版本(main.cpp.bak, 07-04) | 历史 | **12.4%** | 80.6% | 3次(07-21) |
| 好版本 + 关cluster诊断日志 | 历史 | **12.4%** | 80.3% | 3次(07-23) |
| 440dca3(坏版本，含two-level ICP fallback + sync_span跳帧等) | 当前(退步) | 3.2%~3.8% | ~94.5% | 6次(07-21+07-23) |

**关键发现**：
- **12.4%是真实、稳定的基线**——5次独立3x测试(不同日期、不同诊断日志开关)全部落在12.3-12.4%，方差<0.2pp，不是噪声
- **440dca3这批"修复"实际是净负作用**——从12.4%跌到3.2-3.8%，其中 sync_span 时间戳跳帧确认有害（约1.6pp）
- **诊断日志(debug_cluster_diagnostics)不影响性能**——关掉后检测消息数857→858(+10%)，但召回率/误检率纹丝不动，排除"日志I/O压制真实上限"的假设
- **main.cpp 曾因submodule脱离git管理+cp覆盖，存在丢失历史版本的风险**——现已将好版本正式commit进submodule（`ff80298`），打tag `baseline-12.4-confirmed`，main.cpp.bak不再是唯一副本
- 保留了 ObstacleDetector.cpp/LidarCalibrator.cpp/ImageStitcher.cpp 等文件在此期间的改进（NaN过滤、独立锁等），只回滚了main.cpp

**待办**：上面"漏检归因"表（聚类79.8%/分类20.2%）、分桶召回率表都是在**坏基线(3.2%)**上算出来的，需要在**12.4%confirmed基线**上用修好的插值评估器重新跑一遍病因归因，才能指导下一步优化方向。

## 第四把坏尺子：det/gt/odom三种时钟域混用（2026-07-23）

**核心发现**：`boat_evaluator.py` 里 `det`（检测结果）的时间戳和 `gt`/`odom` 完全可能不在同一个时钟域，导致 `bisect` 查找越界后"钳制到数组末端"，实质上**每一次检测比对都被拿去和bag最后一帧（第86秒）那一瞬间冻结的真值位置做匹配**，而不是检测发生时刻真正对应的真值。这个bug从07-21第一次测试起就一直存在，**上面所有"12.4%""3.2-3.8%"的数字都是在这个bug下测出来的**。

**根因**：这套系统里实际存在三种时钟域，且不同版本的main.cpp用的时钟不一样：
1. `gt`（真值船位置）：绝对纪元时间（仿真器/世界状态发布，如 `1782954197.86...`），且几乎从bag开始（t≈0）就有记录，起始延迟可忽略
2. `odom`（船体自身位姿）：bag相对时间，从0开始（如 `0.104~76.752`）
3. `det`（检测结果）：**取决于main.cpp具体实现**——
   - 历史版本（main.cpp.bak）调用 `detect(fused)` 不传时间戳，`publishObstacles` 用 `now()`；若节点未声明 `use_sim_time`，`now()`是**系统墙钟**，与仿真时间毫无关系（相差整整2天）
   - 退步版本（04e45df起）用 `msg->header.stamp`（真实lidar传感器时间戳），恰好和 `odom` 在同一个bag相对时钟域，反而没有这个问题

**验证过程**（详见对话记录，未逐条搬运）：
1. 发现历史main.cpp的 `detect()` 调用没传时间戳 → 诊断日志全是 `t=0`
2. 补上 `detect(fused, now().seconds())` → 发现 `now()` 是墙钟（`1784778034+`），和 `odom`(0~76.7) 对不上
3. 加 `--ros-args -p use_sim_time:=true` 启动perception_node → `now()` 正确读 `/clock`，但发现修正后det和gt同处一个"绝对纪元"域，仍需和odom对齐的偏移量
4. `boat_evaluator.py` 原本只对gt做偏移校正，改成"gt和det都套同一个偏移"——**这个改法本身还有bug**：用 `det_records[0]` 自己算偏移量，把节点启动/标定延迟（约8秒）也算了进去，导致偏移量偏大，历史基线的召回率被错误压到3.1%
5. 最终修法：偏移量永远只用 `gt_records[0] - odom_records[0]` 估计（gt起始延迟可忽略，可靠），det只用一个**中位数样本**判断"是否处于绝对纪元域、要不要套用这个偏移"，不用det自己的[0]算偏移量

**修复内容**：
- `run_*.sh`：给 `ros2 run usv_perception perception_node` 加 `--ros-args -p use_sim_time:=true`
- `src/usv_perception/src/main.cpp`（历史基线版本）：`detector_.detect(fused)` → `detector_.detect(fused, now().seconds())`，让诊断打印能拿到真实时间戳（纯诊断用途，不影响分类逻辑）
- `boat_evaluator.py`：偏移校正逻辑改为"用gt估计偏移量，det/gt各自按中位数样本判断是否需要套用该偏移"，不再对两者都套用同一个从`[0]`算出的量（那样对退步版本是错的，对历史版本也因起始延迟而有误差）

## 修复后的最终基线（2026-07-23，完整时钟修复）

| 版本 | main.cpp | 召回率(3次) | 中值 | 误检率(3次) | 中值 |
|------|----------|-------------|------|-------------|------|
| 历史(main.cpp.bak) | 历史 | 18.7% / 18.8% / 18.8% | **18.8%** | 70.5% / 70.2% / 70.2% | **70.2%** |
| 04e45df(含two-level ICP fallback+sync_span跳帧) | 退步 | 3.4% / 4.1% / 2.9% | **3.4%** | 94.8% / 93.8% / 95.4% | **94.8%** |

**这组数字取代上面所有旧数字（12.4%/3.2-3.8%等），是目前最可信的结果。**

**关键结论**：
- **真实差距比之前记录的更大**：修复前测出的差距是 12.4%→3.5%（8.9pp），修复后是 **18.8%→3.4%（15.4pp）**。时钟bug主要压低了历史版本的表现（用墙钟发布，被冻结在末帧），退步版本的检测时间戳本来就正确（用传感器header.stamp），所以它的数字修复前后变化很小（3.5%→3.4%，符合误差范围）
- **"当前代码存在真实退步"这个方向性结论没有被推翻，反而更严重了**——历史代码的真实上限（18.8%）比之前认为的（12.4%）高得多，退步版本吃掉了将近全部的检测能力
- 目前锁定的基线main.cpp（submodule commit `ff80298` + 后续的时间戳诊断修复）**没有变过**，18.8%/70.2%就是它现在的真实数字
- ~~待办：`missed_cluster_analyzer.py` 等归因分析脚本里独立实现了一套时间对齐逻辑，需要检查/修复~~ **已完成，见下文"分析脚本各自独立的第四把时钟bug"和"聚类vs分类归因"两节**

## 直接证据验证（2026-07-23，不只是推理）

"3次复现"只证明流程稳定，不证明时钟对齐准确——如果offset估计存在稳定的系统性偏差，3次跑测会稳定地重复同一个错误结果（32.6%那次就是先例：结果很稳定，但是错的）。因此在钉死基线前，直接看了未经Python校正的原始 `det` stamp：

```
det (raw, 未校正): 1782954206.2238865, 1782954206.3238728, 1782954206.4238694, ...
odom (raw, 未校正): 0.104 ~ 76.752
gt   (raw, 未校正): 1782954197.856339 ~ 1782954283.5236266
```

**结论**：
- `det` 原始值不再是墙钟（修复前是 `1784778034+`，today's epoch），而是 `1782954206+`——落在 `gt` 自己的原始范围（`1782954197.86~1782954283.52`）内，证明 `use_sim_time` 确实让 `now()` 读到了 `/clock`
- 一个容易踩的细节：`/clock` 本身发布的是**仿真器的绝对纪元**（和gt同源），**不是从0开始的bag相对时间**——odom是唯一用"相对时间"这个约定的topic，这是仿真器架构本身的特点，不是bug，但排查时容易误判
- 用 `det[0] - _epoch_offset`（`_epoch_offset` = `gt[0]-odom[0]` = `1782954197.7523`）反推节点启动延迟：`1782954206.22 - 1782954197.7523 ≈ 8.47秒`——这是感知节点预热传感器+标定收敛的合理延迟量级，不是荒谬数字，作为offset估计合理性的间接佐证

**已知残留不确定性**：`_epoch_offset` 只做了到"量级正确、物理上合理"这一层的验证（大整数对齐+启动延迟量级检查），**没有做亚秒级精度的交叉验证**（比如同一时刻另一个独立信号源的时间戳做逐帧比对）。当前的召回率数字（18.8%/70.2%）作为宏观基线是可信的，但如果后续要做**帧级别的精细归因**（比如"某一帧漏检的具体原因"），offset的亚秒级误差可能仍会造成个别帧的错配，需要留意。

## 分析脚本的独立时钟对齐bug（2026-07-23）

`distance_bucket_analyzer.py` 和 `missed_cluster_analyzer.py` 各自独立实现了一套时间对齐逻辑，没有复用 `boat_evaluator.py` 修好的那套，且比修复前的 `boat_evaluator.py` 还要更原始一层——**两个脚本都只对 `gt` 做了偏移校正，`det` 完全没有校正**，直接用未校正的原始det时间戳去 `find_nearest`。

**表现**：这次去跑 `missed_cluster_analyzer.py`，返回"漏检总数: 0"——一开始以为是 `cluster_diagnostic` 正则不认科学计数法（`t=1.78e+09`）导致的，修完正则后问题依旧存在。后来定位到根因：这两个脚本用的 `find_nearest`（最近邻+`MAX_TIME_GAP=0.5`硬性丢弃）遇到未校正、相差18亿秒的det时间戳，会把每一条det记录都判定为"odom_gap超限"直接跳过，最终匹配不上任何东西。

**修复**：把两个脚本的偏移校正逻辑，改成和 `boat_evaluator.py` 里 `_align_to_odom` 完全一致的写法（gt估计偏移量，det按中位数样本判断是否需要套用）。

**修复后又发现一层更深的问题**：`distance_bucket_analyzer.py` 改完偏移校正后能跑出结果了，但和 `boat_evaluator.py` 在同一份数据上交叉验证时，总的真值统计对不上（8580 vs 6890，缺口约19.7%）。追下去发现：这份bag里 **`odom` 话题比 `gt`/`det` 提前约9秒结束记录**（odom止于76.75s，gt/det到85.8~86s）。`find_nearest`+硬性`MAX_TIME_GAP`检查，会把落在这最后9秒里的所有det记录直接丢弃；而 `boat_evaluator.py` 用的 `interpolate_odom` 对odom越界不做丢弃检查，只是钳制到最后一个已知位姿继续算（船体本身在这86秒里几乎没怎么移动，钳制引入的误差可以忽略）。

**最终修复**：把 `distance_bucket_analyzer.py` 的 `find_nearest` 整体换成 `boat_evaluator.py` 的 `interpolate_odom`/`interpolate_gt`（逐字复用同一份实现），换完之后总数（真值8580/命中1616/召回率18.8%/误检3769）和 `boat_evaluator.py` **逐位对上**，交叉验证通过。`missed_cluster_analyzer.py` 尚未做这一层的同等修复（它还依赖 `cluster_diagnostic` 时间戳的精度问题，见下）。

## 分距离桶召回率（18.8%基线，交叉验证通过）

| 距离范围 | 真值总数 | 命中 | 召回率 |
|----------|----------|------|--------|
| [0,20m) | 2763 | 1226 | **44.4%** |
| [20,40m) | 3608 | 390 | **10.8%** |
| [40,60m) | 2209 | 0 | **0.0%** |
| [60m+) | 0 | 0 | — |
| 合计 | 8580 | 1616 | 18.8%（与boat_evaluator.py逐位一致） |

**结论**：0-20m是唯一有实际探测能力的区间（44.4%），20-40m骤降到约1/4（10.8%），40m以外**一次都没命中过**（0.0%，比坏钟时代旧记录的0.4%更绝对）。"20m是有效感知距离的硬边界"这个结构性判断在干净数据下依然成立——避障逻辑不应设计成依赖20m以外的检测结果。

## std::cout全局格式状态污染——第三个独立测量bug（2026-07-23）

排查 `missed_cluster_analyzer.py` 为什么帧匹配率是0/858时发现：`cluster_diagnostic` 打印出的 `t=` 时间戳本身不可靠。日志里同一批打印，一部分是 `t=1.78295e+09`（科学计数法，只有6位有效数字），另一部分是 `t=1782954283.7`（定点格式，恰好1位小数，同一个值重复了8755次）。

**根因**：`src/usv_perception/src/ImageStitcher.cpp:319` 有一行
```cpp
std::cout << "  [buildMaps] " << ext.name
          << "  覆盖率: " << std::fixed << std::setprecision(1)
```
`std::fixed`/`setprecision`是`std::cout`这个全局流对象的持久状态，设置之后从未复位。这行代码在程序某个时刻执行一次后，**之后所有模块（包括`ObstacleDetector.cpp`里完全无关的`cluster_diagnostic`打印）的`std::cout`输出都被永久截断到只剩1位小数精度**，直到程序退出。0.1秒的精度损失正好卡在`missed_cluster_analyzer.py`用的`FRAME_MATCH_TOL=0.05`容差边缘，导致帧级别的时间戳匹配完全失败。

**影响范围**：只影响依赖`cluster_diagnostic`时间戳做逐帧关联的分析（目前是`missed_cluster_analyzer.py`），不影响`boat_evaluator.py`和`distance_bucket_analyzer.py`（它们只用`boat_log.jsonl`里的gt/odom/det，不解析这份日志）。**这个精度污染很可能不是这次才有的**——任何过去依赖这份日志做精确时间匹配的分析都可能受过影响，包括更早"聚类79.8%/分类20.2%"那次归因（但那是在不同代码版本/不同执行路径上跑的，不能直接类推，只作为存疑标记）。

**修复（已完成，2026-07-23）**：在 `ObstacleDetector::detect()` 开头加一行 `std::cout << std::fixed << std::setprecision(6);`，每次调用都显式复位精度，不依赖谁最后设了什么全局状态。改的是`#include <iomanip>`+1行，纯格式修复，不碰任何分类逻辑，A/B验证召回率不变（18.8%→18.8%）。修复后 `cluster_diagnostic` 时间戳恢复到微秒级精度（778个不同值，之前只有2-3个因精度丢失而坍缩的桶）。

**这个修复没有解决的问题——`std::cout`并发写入本身不安全（已知风险，未加锁修复）**：`main.cpp` 用 `MultiThreadedExecutor`（4线程），`image_timer_group_` 和 `lidar_timer_group_` 是两个不同的 `MutuallyExclusive` 回调组——组内互斥，**组间不互斥**，也就是说 `processImageBranch`（触发 `ImageStitcher` 的 `std::cout` 打印）和 `processLidarBranch`（触发 `cluster_diagnostic` 打印）在结构上可以真正同时跑在不同线程上，共享同一个 `std::cout` 却没有锁保护。这次实测中确实抓到了一次字符级损坏（`t=61782954238.748861`——真实值`1782954238.748861`前被插入了一个多余的"6"，被 `missed_cluster_analyzer.py` 的离群值过滤器丢弃，见下）。**这次只丢了86000+行里的1行，影响可忽略，但根因（无锁并发写共享流对象）没有修，不代表"已解决"——只是这次跑测幸运。** 如果要根治，需要给所有诊断`std::cout`/`std::cerr`调用加互斥锁，或者改用线程安全的日志机制；目前判断投入产出比不够，先记录风险，不做代码改动。

## 分析脚本各自独立的第四把时钟bug（2026-07-23）

`missed_cluster_analyzer.py` 在解析 `cluster_diagnostic` 日志时，独立实现了一套"要不要减epoch_offset"的判断——最初这段代码**硬编码"总是不减"**（历史遗留，因为脚本写的时候det还是`t=0`，没人预期cluster的t=需要和det一样做时钟对齐）。结果是：`cluster_frames`字典的key全是原始绝对纪元（~1782954206+），主循环里拿去查找的`det['stamp']`已经是平移过的（~8~86），bisect查找的索引完全对不上，导致"附近有候选簇"永远算出0%——**看起来像是"聚类阶段彻底失败"的假象，其实是第四个独立的时钟未对齐bug**。

**修复**：把这段也改成和`_align_to_odom`一致的"先收集原始值，用中位数样本判断是否需要平移"的自适应逻辑，不再硬编码"总是不减"或"总是减"，这样以后换成退步版数据（cluster时间戳本来就在odom域）也不会错判。顺带加了基于`odom`范围的离群值过滤，用来丢弃并发写入导致的损坏行（见上）。

## 聚类vs分类归因（18.8%基线，四个时钟bug+精度bug全部修复后，交叉验证通过）

| 距离段 | 漏检数 | 附近有候选簇(→分类问题) | 附近无候选簇(→聚类问题) |
|--------|--------|------------------------|------------------------|
| 0-20m | 1150 | **75.0%** | 25.0% |
| 20-40m | 2681 | 21.2% | **78.8%** |
| 40-60m | 2039 | 21.9% | **78.1%** |

**0-20m的病因是分类问题为主**：附近候选簇里`discarded_shape_implausible`（形状判据不通过）占45.8%，`fp_max`集中在0.5-2.0m。**20-40m/40-60m的病因是聚类问题为主**：压根没形成候选簇。

**量化佐证——命中船只对应候选簇的pts，对比漏检船只附近候选簇的pts**：

| 距离段 | 命中船只对应候选簇pts（n，中位数，均值） | 漏检船只附近候选簇pts特征 |
|--------|------------------------------------------|---------------------------|
| 0-20m | n=1253，中位数**243**，均值232.8 | — |
| 20-40m | n=392，中位数**89**，均值84.2 | 77.6%落在`<20`点 |
| 40-60m | 无命中样本（0次） | 98.2%落在`<20`点 |

命中的船只，候选簇点数是几十到几百；同一距离段里漏检船只附近哪怕有候选簇，点数普遍不到20——差了一个数量级。**"远距点云密度不足以支撑聚类"不是直觉推测，这组数字是量化依据。**

**结论——两个区间需要两个不同的工程动作，不能用同一个参数改**：
- **0-20m**：调分类阈值（形状/点数判据），聚类阶段是ok的
- **20-40m / 40-60m**：本质是点密度/传感器覆盖问题，调分类阈值救不了，需要考虑聚类参数（降低最小点数、放宽聚类半径）或者点云预处理链路（降采样/滤波是否在这个距离段过度丢点，需要单独确认）
- **40m以外**：0次命中、78.1%的候选缺失、pts几乎全<20，三个信号一致指向"硬件覆盖盲区"，不是算法能单独救的——避障逻辑不应设计成依赖40m以外的检测结果

## 方法论提醒（写给未来自己）

这次一共修了**五把独立的坏尺子**（进程污染→gt/odom时间基准→评估器越界返回→多脚本时间匹配口径不一致→cluster_diagnostic的std::cout全局精度状态污染，其中"多脚本口径不一致"这条本身又分成两层：`find_nearest`硬容差丢尾段数据、cluster_frames自己不做epoch校正），每一把都曾经让"跑出来的数字"看起来完全合理、可以自圆其说。**任何新写的分析脚本，凡涉及跨话题/跨文件的时间戳匹配，必须先和`boat_evaluator.py`的口径做一次总数交叉验证（真值总数/命中数/误检总数逐位对比），不能假设"跑出数字就是对的、够稳定就是对的"**——32.6%和这次的"附近有候选簇0%"都是稳定复现的错误结果，稳定不等于正确。

**第六把坏尺子(2026-07-23，`cost_evaluation.py`/`near_cluster_analyzer.py`)**：这两个新写的离线代价评估脚本，各自独立犯了同一个错——`cluster_diagnostic`打印的`center`坐标本身已经是船体系(base_link，`ObstacleDetector.hpp`文档和`cloud_boat`形参名都写明了)，脚本却又对它调用了一次`world_to_boat(c['x'], c['y'], odom['x'], odom['y'], yaw)`，把一个已经是船体系的小量级坐标当成世界坐标去减`odom`的大量级世界坐标，凭空制造出几百米的偏移假象(实测45m的真实距离被这个bug算成763m，和"700-900m"的错误结论量级吻合)。这两个脚本还各自重复了`missed_cluster_analyzer.py`早期版本同款的`time_offset`公式错误(用`gt_records[0]['stamp']`而不是`gt_records[0]['stamp'] - odom_records[0]['stamp']`)。**教训：即使前面的坏尺子已经在别的脚本里修过，新写一个脚本时同样的坑还是会掉进去——这类脚本的时钟对齐和坐标系处理，最好直接复用`boat_evaluator.py`里已验证的函数，而不是每次重新实现一遍。**

## 第一个数据驱动的成功改动：0-20m距离自适应`boat_fp_max_sane`（2026-07-23）

在18.8%基线上做0-20m归因时发现约146个候选簇卡在boat的`fp_max`上限(2.0)和block的`dz`上限(0.7)之间的缝隙里——太宽不算船，太高不算浮块。按簇中心与最近真值船的偏移分成两组：65.8%(96个)偏移>1m(更像是和相邻物体粘连，指向聚类参数问题，这次没动，见下"待办2")，34.2%(50个)中心基本重合(更像是单船fp_max被单纯估计偏大)。

**动手前的代价评估**：全局范围内把`boat_fp_max_sane`从2.0放宽到2.5，符合条件的候选簇里只有4.6%(127/2734)是真船，95.4%是噪声——比之前否决掉的"降低pts门槛"方案(1:3)还要差，全局放宽这条路直接否决。但**按距离拆开看**，同样的形状特征在0-20m内是100%真船(88个全中)，在20-40m/40-60m几乎全是噪声——问题从来不是"这个特征没用"，是"不分距离地全局套用"。

**修法**：不改全局阈值，改成距离相关的阈值——`ObstacleDetector.hpp`新增`near_range_threshold=20.0f`和`boat_fp_max_sane_near=2.5f`两个配置项，`ObstacleDetector.cpp`里判断船只候选(主判据和boat_fallback两处)时，20m以内用`boat_fp_max_sane_near`，20m以外维持原来的`boat_fp_max_sane=2.0`不变。改动前离线代价评估：63个候选簇，63个全部匹配真值船，0个噪声。

**实测结果(3次)**：召回率18.8%→**20.4%**(+1.6pp)，误检率70.0%→**68.4%**(-1.6pp，同步下降)。误检率下降是预测之外的额外收益——原本被丢弃的簇现在被正确分类为船，同一帧里原本可能"顶替"这个位置的错误候选不再被误记为误检，相当于用一次正确分类替换了一次错误分类，分子(误检数)减小、分母(检测总数)基本不变。

**这次验证的方法论(比数字本身更重要)**：
1. 归因分析定位具体病灶(不是笼统的"分类不准"，是精确到"卡在两条判据中间的缝隙")
2. 按特征细分成子群(小簇/大簇)，因为病因可能不同
3. **任何"放宽判据"的改动，动手前必须做全局代价评估**——统计"符合放宽后条件的所有候选，真/假比例是多少"，不能只看"能救回多少"这一半。1:3(降pts门槛)和1:20(全局放宽fp_max)两次被数据否决，1:0(0-20m内放宽fp_max)才被采纳
4. 发现"全局代价差"不等于"这个特征没用"——可能只是没有按合适的维度(比如距离)拆分统计
5. 精准实现(只在证据支持的范围内生效，不波及其他范围)
6. 双指标(召回+误检)一起看验证结果，不能只看一个

## 第七把坏尺子：`assigned=`正则锚定行首，丢失所有成功分类的label（2026-07-23）

排查"大簇组粘连"问题时，先发现原来的146/96/50这套数字是在**修复前**的旧数据上算的，修复后"大簇组"这个群体本身已经变了，重新按同一套方法在新数据上跑，结果显示大量漏检船附近的候选簇label是`unknown`——一度以为这是"聚类阶段没形成候选"的证据。

**根因**：`ObstacleDetector.cpp`里成功分类(block/buoy/pillar/boat/*_fallback)的打印格式是`[cluster_diagnostic] t=... -> assigned=X`（assigned=在行尾），而只有`discarded_low_confidence`/`discarded_shape_implausible`两条丢弃路径是`[cluster_diagnostic] assigned=X`这种assigned=在行首的简短格式。`missed_cluster_analyzer.py`/`cost_evaluation.py`/`near_cluster_analyzer.py`三个脚本的`assigned_pattern`全部用`re.match`锚定行首，只认得出丢弃路径的短格式，**所有成功分类的簇label全部丢失，被误记成`unknown`**。修法：正则去掉行首锚定、`.match()`改`.search()`，在整行范围内找`assigned=(\w+)`。

**验证受影响范围**：`missed_cluster_analyzer.py`里"有候选簇/无候选簇"这个总量判断只依赖几何距离，不依赖label，重跑对比（regex修复前后同一份数据）结果完全一致(78.3%/21.7%不变)——确认没受影响。但任何**依赖具体label细分**的历史结论（比如更早的"discarded_shape_implausible占45.8%"这类）需要用修好的正则重新核实。修复后重新解析，`unknown`变成了具体的`buoy`（62.1%）——这才引出了下面的buoy线调查。

## buoy判据截胡——一次被否决的调查，和一条重要方法论收获（2026-07-23）

**现象**：0-20m范围内，"不在大簇组"的1344个漏检船实例里，78.3%(1052个)在2m内确实有候选簇，其中62.1%(653个)被判成了`buoy`。这些候选簇的`fp_max`死死贴着`buoy_fp_max=1.0`的天花板（p75=0.99），`pts`中位数174（和真实命中船只的233同量级，远超真浮球该有的点数），初步判断很像"buoy判据排在boat前面、把船截胡了"——和block截胡（146个）是同一类结构问题的镜像。

**代价评估揭示这条路走不通**：全局范围内统计所有满足`fp_max<=1.0 && dz<=1.5`(buoy判据)的簇，无论用`pts`还是用密度(`pts/(fp_max*fp_min)`)扫阈值，**任何阈值下"匹配真浮球"的数量都数倍于"匹配真船"**（如pts>=100时：580真船 vs 1007真浮球；密度>=300时：591真船 vs 1811真浮球）。没有清白的分界线——这条线判死。

**核心方法论收获——条件概率陷阱（比这次否决本身更重要）**：

一开始看"653个候选簇"的密度分布时，发现清晰的双峰（一堆密度200-800像船，一堆密度<50像真浮球），非常有说服力。但这是**方法论错误**——那653个样本，是先筛"漏检船附近2m内"再看密度分布的，测的是：

$$P(\text{是船} \mid \text{形状像buoy}, \text{附近有一艘漏检的真船}) \quad \text{——这个概率很高}$$

而分类器运行时用不上"附近有没有漏检船"这个条件（这是只有对着真值才知道的信息，运行时看不到），它面对的是：

$$P(\text{是船} \mid \text{形状像buoy}, \text{密度高}) \quad \text{——这个概率很低（这次实测1:3左右，怎么调都翻不了盘）}$$

差的这一层，是因为赛道上真浮球的数量远超真船（base rate悬殊），"密度高的buoy形状簇"这个全局池子里，绝大多数命中的还是真浮球，不是船。**任何用真值条件预筛过的样本池，得出的判据都可能只是在描述"事后看这是什么"，而不是"运行时能不能筛出来"。**

**由此确立两条纪律**：
1. **代价评估的样本池必须是分类器运行时真正能看到的全集，不能用任何依赖真值的条件预筛**（比如"只看漏检船附近"）——这类预筛会把答案偷偷藏进样本选择里，让错误的判据看起来极有说服力。前面被否决的"小簇组"(1:3)和这次的buoy(1:3~1:20)如果早点意识到这条纪律，本可以更早判死，不用做完整套A/B分组分析才发现问题。
2. **判断一条改动路线有没有希望，可以先看"新判据在全局范围内命中规模"作为快速筛查**：0-20m放宽fp_max那次能通过，是因为新判据(`fp_max∈(2.0,2.5], dz∈[0.3,1.8], pts>=40, dist<=20m`)全局只命中63个，规模小到"特异"；buoy线光是`fp_max<=1.0&&dz<=1.5&&密度>=50`一个门槛就命中18962个，规模大到必然被base rate支配。**先看命中规模的量级，能在花时间做完整代价评估之前就排除掉明显没戏的方向。**

**判据顺序是结构性问题——已积累两个案例**：block截胡(146个)、buoy截胡(653个)，都是同一个架构原因：分类逻辑是if-else顺序判断、先匹配先赢，boat排在block和buoy之后。这次两个案例都通过"给boat开一个精确的、代价评估过的例外口子"处理（block那次做了，buoy这次代价评估没过、没做），没有改动整体结构。**如果后续再出现第三个"XX判据截胡boat"的案例，应该认真考虑改判据结构（比如所有类别算匹配得分取最高分、或者给boat一个优先检查位），而不是继续逐个打补丁。**

**近距离(0-20m)分类阈值调整路径的整体判断**：四次尝试（降pts门槛、全局放宽fp_max、0-20m内放宽fp_max、buoy判据调整），只有一次通过，且只值1.6pp召回率提升。**这条路径大概率已经接近榨干**，边际收益在下降，下一步的重心应该转向20-40m那个规模更大（2681次漏检、79%聚类问题）的方向。

## 20-40m"聚类问题"框架被推翻——真实瓶颈是点数门槛的静默丢弃（2026-07-23）

用`ObstacleDetector.cpp`里现成的"链路诊断"打印（`trace_boat_count<10`，前10帧，10个硬编码目标位置`boat0_far`~`boat9_far`）回溯了20-40m范围内几个漏检目标的完整处理链路。先验证了其中6个（boat0-4、boat7）精确匹配这次跑测第一帧的真值船位置（另外4个坐标对不上，可能是更早某次跑测遗留的旧坐标，予以排除）。

**证据1——聚类算法本身零缺陷**：这6个验证过的目标，每一个都精确聚出了一个簇，质心偏移全部在0.2-0.6米（boat0偏移0.29m、boat1偏移0.37m、boat2偏移0.21m、boat3偏移0.23m、boat4偏移0.59m、boat7偏移0.22m）。裁剪和水面去除两级预处理，点数和原始点数完全一致，没有丢失任何点。**聚类算法在稀疏点云下依然能精确定位，不是瓶颈。**

**证据2——`min_cluster_pts=4`造成静默丢弃，是`missed_cluster_analyzer.py`统计"附近无候选簇"的一个隐藏陷阱**：`ObstacleDetector.cpp`里`total_pts < cfg_.min_cluster_pts`时直接`return`，**在`cluster_diagnostic pending`打印之前**——也就是说点数<4的簇，聚类阶段确实形成了，但从未进入日志。全局统计所有`cluster_diagnostic`记录的`pts`分布：`min=4`，且恰好`pts=4`这一个值出现了8437次（占比~10%），在4这个值上堆出一座尖峰——这正是`min_cluster_pts=4`边界效应的signature，不是巧合。boat3(2点)就是被这道门槛静默丢弃、从未留下日志痕迹的真实案例。

**结论**：78.8%"附近无候选簇"这个数字的真实含义，不是"聚类阶段没形成候选"，而是"点云稀疏(2-104点，多数个位数到十几) → 大部分簇连`min_cluster_pts=4`都过不了(静默丢弃，从未进日志) → 少数过了但远低于`boat_min_pts=40`"——是**同一个根因（远距点云稀疏）的两级表现**，不是"聚类算法有毛病"。这次认知反转完全靠硬证据钉死（6/6精确命中的偏移量、`pts=4`处的8437次尖峰），不是推测。

**这个诊断陷阱不只影响这一次**：任何依赖`cluster_diagnostic`日志做统计的分析，都会把"点数<4被静默丢弃"的簇误算成"不存在"，是一个持续性的盲区。**待办**：在`min_cluster_pts`丢弃分支也加一行诊断打印（标记为`discarded_below_min_pts`，带pts和位置），几行代码、零风险，能消除这个盲区，并直接回答"全程有多少簇死在这道门槛上"这个目前还不知道答案的问题。

**这一阶段调参工作的完整战绩**：

| 尝试 | 结果 | 真假比 | 备注 |
|------|------|--------|------|
| 降小簇点数门槛(0-20m) | 否决 | 1:3 | 数据实测 |
| 0-20m内放宽`boat_fp_max_sane`到2.5 | **通过** | **1:0** | 数据实测，唯一成功案例 |
| buoy判据调整(pts/密度阈值) | 否决 | 1:3~1:20 | 数据实测，base rate陷阱 |
| 20-40m降点数门槛 | 否决 | 预期更差 | **机制推定，未实测**——见下方限定 |

**唯一通过的那次，特征是"判据组合足够特异"**：全局只命中63个簇，且全是船。**实操准则：新判据在全局范围内命中的簇数如果是几万个，基本没戏（会被base rate支配）；几十上百个，才可能是干净信号。** 这条能在做完整代价评估之前，先用命中规模的量级快速排除掉明显没戏的方向。

**关于"20-40m降点数门槛"这条——重要限定**：这次判定为"否决"是**基于机制推定（远距噪声天然更多，重演小簇组1:3甚至更差的真假比），没有像其他三次那样做实测代价评估**。这个推定目前站得住，但如果将来点云预处理链路、滤波参数、或传感器配置有变化，需要重新验证——不能和前面几个有完整实测数据支撑的否决混为一谈。

## 方法论沉淀（本阶段最终版）

1. **代价评估的样本池必须是分类器运行时真正能看到的全集，不能用任何依赖真值的条件预筛**（详见上"buoy判据截胡"一节的条件概率陷阱推理）
2. **判断改动路线有没有希望，先看新判据的全局命中规模**——几万个基本没戏，几十上百个才可能干净
3. **任何"看似合理的统计结果"，如果背后的日志/数据源本身有静默丢弃或截断机制，必须先确认统计口径和数据源的对应关系，不能想当然**——这次的"无候选簇=聚类失败"就是被日志的静默丢弃机制误导的例子，和更早"det时钟域不对齐导致98.3%被判定为噪声"是同一类错误
4. **机制推定 ≠ 实测验证**，两者在文档里要明确区分标注，前者需要在前提条件变化时重新核实

## 多帧累积——记录为未来方向，本阶段不做

如果20-40m确认是点云稀疏的硬约束，理论上唯一还没试过的路是跨帧累积点云（把船连续几帧的点数攒起来，凑过`min_cluster_pts`/`boat_min_pts`门槛）。这次不做，原因记录清楚，方便以后接手时判断从哪开始：

1. **收益上限受base rate约束**——累积会同时把噪声也攒起来，远距离噪声本来就多，攒完可能仍是类似1:3的真假比，只是分子分母同时变大
2. **需要运动补偿**——船在动，跨帧累积必须先做位姿补偿，否则攒出来的点云是拖影，`fp_max`会被拉大，反而更难分类
3. **前置条件是评估链路要支持时序分析**——现在的`boat_evaluator.py`是逐帧独立匹配，验证多帧累积的效果需要先改评估方法本身

## 四类完整评估首次建立 + block_h_max修复——方法论标杆案例（2026-07-23）

**背景**：boat这条线收益已递减（四次阈值尝试仅一次通过、只值1.6pp），评分是四类（boat/buoy/pillar/block）一起算的，之前从未单独评估过另外三类。扩展`boat_logger.py`订阅`buoys_check`/`pillars_check`/`blocks_check`，写了`four_category_evaluator.py`补上这块空白——过程中先踩了一次熟悉的坑：脚本第一版的`_align_to_odom`独立地用每个序列自己的`[0]`算epoch_offset，对det系列(`det`/`det_buoy`/`det_pillar`/`det_block`)重犯了"用det自己的[0]而不是gt的[0]算offset"这个已经修过好几次的错误(det[0]-odom[0]比正确的gt[0]-odom[0]多算了~8.3秒的节点启动延迟)，导致boat算出3.4%而不是应该吻合的20.0%。修法：全文件只用一个从`gt`(boat真值)派生的`_canonical_epoch_offset`，其余7个序列都复用这同一个值、只各自判断要不要套用。**每写一个新脚本都要重犯这类时钟对齐错误——这已经是第八次同款bug了，足以说明"复用`boat_evaluator.py`的实现而不是每次重新写一遍"不是可选项，是硬要求。**

**首次四类基线（block_h_max=0.7）**：boat 20.0%/69.3%，buoy 63.8%/24.1%，pillar 14.6%/72.5%，**block 0.5%/75.9%**。block数字极端异常——5154次真值(场景里稳定6个block)只命中28次，且误检数(88)只有boat/pillar的零头，是"判据太严、几乎没东西能通过"的特征，不是"判据太宽导致误检泛滥"。

**逐条判据诊断**：对真值block附近2m内的候选簇，逐条统计block五条判据(`fp_max∈[1.7,3.2]`/`dz<=0.7`/`square>=0.55`/`pts>=10`/`|center.z|<=1.0`)的通过率——四条都在85%+，唯独`dz<=0.7`只有**5.4%**。实测该批候选簇的`dz`分布：中位数**1.30米**，几乎是判据上限的两倍。`block_h_max`的历史注释写着"从0.9收紧到0.7，贴近真实厚度0.4m，挡住boat桅杆污染"——**这次收紧是为了保护boat类别做的，但从没验证过对block自身召回率的影响，代价是把block压到了0.5%。**

**代价评估（含两个关键的"排除误判风险"确认）**：扫描`block_h_max`候选值(1.0/1.3/1.5/1.6)，在h_max=1.5时：新增1442个匹配真block的候选、64个匹配真boat的候选、1036个匹配真buoy位置的候选。后两个数字看起来是"零和交换"的证据，但确认了当前label分布后判断完全不同：
- **1036个"匹配真buoy位置"的候选，当前label里零个是`buoy`/`buoy_fallback`**——这些候选的`fp_max∈[1.7,3.2]`，真buoy的`fp_max<=1.0`根本不可能是这个形状，只是大簇的位置巧合落在buoy真值附近（course上buoy密度是block的7倍，2m匹配容差下容易撞上）。**buoy召回率零成本**
- **64个"匹配真boat"的候选，只有27个当前label是`boat`(其余37个本来就是discarded噪声)**——真实代价是27，不是64
- **1442个"匹配真block"的候选，只有28个当前已算作block命中(和现状吻合，无重复计数)，其余1414个是净新增**

**净账（预测 vs 实测，方法论首次经受精确检验）**：

| 类别 | 改前 | 预测 | 实测 | 备注 |
|------|------|------|------|------|
| block | 0.5% | ~28% | **27.8%** | 净新增1414，代价评估几乎逐位命中 |
| boat | 20.0% | ~19.7% | **19.7%** | 代价27次，预测精准命中 |
| buoy | 63.8% | 不变 | 63.9% | confirmed零影响 |
| pillar | 14.6% | 不变 | 15.7%(+1.1pp) | **机制上block(dz<=1.5)和pillar(dz>=1.5)区间基本不重叠，理论上不该有交集；单次跑测未做3次复现，推定为这条线上已知的±1pp跑测抖动，未专门验证** |

总召回率36.3%→38.4%(+2.1pp)，总误检率44.2%→45.1%(+0.9pp，block自身误检率反而从75.9%降到68.3%，总误检率上升是block检测总量暴增导致分母变化)。

**这是这一阶段方法论的标杆案例**：预测27.8%对实测27.8%、预测19.7%对实测19.7%，小数点后一位吻合。此前几次成功/否决的判断只能给出方向和大致幅度，这次证明"逐条判据通过率定位瓶颈→实测分布vs判据值找错配→扫描候选值→label分布排除误判风险→净账预测"这套流程，**已经能在编译之前定量预测改动效果，不只是筛掉坏主意**。完整步骤：
1. 逐条判据通过率 → 定位唯一瓶颈(dz通过率5.4%)
2. 实测特征分布 vs 判据数值 → 发现1.30m对0.7m的量级错配
3. 扫描候选阈值 + 组合方案(`square_min`收紧)对比
4. 用label分布确认三个误判风险：buoy是否被截胡(否)、boat真实代价是多少(27非64)、block有没有重复计数(没有)
5. 净账预测 → 实测验证

**暴露的系统性模式，值得专门排查**：`block_h_max`这个bug的本质是"为保护A类别收紧的判据，从没验证过对A自己或对B类别的代价"。`ObstacleDetector.hpp`里还有几个类似措辞的注释，尚未逐一验证：`pillar_max_pts=300`("从60放宽，60会误伤近距离高密度真立柱")、`block_fp_min=1.7`("从1.5再收紧，缩小与boat尺寸的重叠区间")、`boat_fp_max_sane=2.0`("从6.0大幅收紧……仅损失11%真船召回"——这条注释自己承认了代价，且是为了排除block误判）。**pillar当前15.7%和block改之前的0.5%一样反常**——立柱又高又粗、点云特征鲜明，静态目标不该比运动的boat还难测，大概率也有一条判据卡死了同款的问题，值得用完全相同的方法查一遍。

## pillar_max_pts修复——跨类别污染，双赢案例（2026-07-23）

用和block完全相同的方法（逐条判据通过率诊断）查pillar的15.7%异常。**结果和block不是同一种模式**：pillar自己的四条判据(`fp_max<=2.0`/`dz>=1.5`/`slenderness>=2.5`/`pts<=300`)单独通过率都在74-92%，组合通过率58.9%——不是单一判据被卡死到近乎0，而是有相当一部分（25.1%，2065个）候选被`boat_fallback`抢先分类掉。逐条排查这2065个的失败判据：`fp_max`超限0.1%、`dz`不够0%、细长比不够0.5%、**`pts>300`不满足99.5%**——几乎单独解释了全部误判。这批候选的pts分布：中位数**2489**，max **7094**，`pillar_max_pts`历史注释"从60放宽到300，实测被误判入boat的立柱点数达900-2600"——**现象诊断对了，但修的力度完全不够，2489的中位数远超300这个"放宽后"的新上限**。

**代价评估揭示这次不是取舍，是双赢**：扫描`pillar_max_pts`候选值(300/1000/3000/5000/8000)，"匹配真boat"这一列**全程恒为92，一次都没变过**——说明这92个候选本来就在pts<=300范围内、且已经被判成pillar（不是新放行的），放宽上限完全不触碰这批。真正增长的只有"匹配真pillar"(5379→7520，净增2141)，"都不匹配"(纯噪声)在pts>1000后完全封顶不再增长——1000-8000这个区间里几乎全是真实的近距离立柱。**更关键的是那2140个当前被误判为`boat_fallback`的真立柱，本身就在污染boats_check——修复后不只是pillar召回涨，boat的误检也会同时下降**，因为这些"假boat"从boats_check里消失了。

**取值选择**：3000而非8000——3000已拿到76%的收益(7015 vs 7520)，同时对"这份bag没出现过、未来场景可能出现的超大异常簇（比如码头结构）"保留一道拦截，8000实质上等于取消上限。

**实测结果**：pillar召回15.7%→19.4%(+3.7pp，预测~20.5%，同一量级)，**boat误检率69.3%→51.3%(-18pp)**——是这条线迄今对单一指标影响最大的一次改动。boat召回率19.7%→19.4%(-0.3pp)、block 27.8%→26.1%(-1.7pp)，两者都在已知的±1-2pp跑测抖动范围内。总召回率38.4%→39.8%(+1.4pp)，总误检率45.1%→43.4%(-1.7pp)——**两个指标同时改善，不是取舍**。

**block_h_max 和 pillar_max_pts 两次修复的性质对照**：

| | block_h_max | pillar_max_pts |
|---|---|---|
| 性质 | 取舍（boat让利换block） | **双赢** |
| 机制 | 判据阈值和实物尺寸错配 | **误判导致跨类别污染** |
| 代价 | boat -27次命中 | 零 |
| 意外收益 | — | **boat误检 -18pp** |

**"跨类别污染"是一个值得专门排查的新机制**：一个类别的判据设置不当，不只是自己漏检，还会让本该属于它的目标被误判进另一个类别，制造那个类别的误检。这次是2140个立柱冒充船。boat目前误检率仍是51.3%，四类里最高——用同样的思路（把当前label是`boat`/`boat_fallback`但匹配不上真boat的候选，按它们实际匹配到的真值类别分组）查一遍，看还有没有下一个`pillar_max_pts`级别的污染源；如果查出来大部分是"都不匹配"（真噪声，比如水面反射/尾流），那说明调阈值这条路对boat误检已经到头，需要换思路（比如时序一致性检查）。

## boat误检归因 + block_fp_min微调（2026-07-24）

**本轮目标**：接着上一阶段待办第10项，把`boat`/`boat_fallback`里匹配不上真boat的候选按真实来源拆分；只有全局代价评估为正的改动才进入C++。

### 基线复现

在最新`boat_log.jsonl`上用`four_category_evaluator.py`逐位复现当前基线：

| 类别 | 召回率 | 误检率 |
|------|--------|--------|
| boat | 19.4% | 51.3% |
| buoy | 64.0% | 24.1% |
| pillar | 19.4% | 67.3% |
| block | 26.1% | 70.2% |
| 四类汇总 | **39.8%** | **43.4%** |

### 误检位置归因

boat误检共1751个。按2m容差归到最近的其他类别真值：

| 最近真值类别 | 数量 | 占boat误检 |
|-------------|------|------------|
| buoy | 415 | 23.7% |
| pillar | 345 | 19.7% |
| block | 166 | 9.5% |
| 都不匹配 | 825 | 47.1% |

**重要限定：2m空间归因不能直接等同于真实类别污染。** 半径从0.5m放宽到2m时，命中任意其他类别真值的误检从191增到926；且有400个同时落在buoy和pillar 2m内、250个同时落在block和buoy 2m内。赛道目标密集，必须再结合簇形状和逐条判据判断，不能只凭“附近有某类真值”下结论。

分距离看：

| boat误检距离 | 总数 | 最近buoy | 最近pillar | 最近block | 都不匹配 |
|-------------|------|----------|------------|-----------|----------|
| 0-20m | 540 | 135 | 333 | 0 | 72 |
| 20-40m | 935 | 280 | 11 | 150 | 494 |
| 40-60m | 273 | 0 | 1 | 16 | 256 |

近距误检主要呈pillar污染特征，20-40m开始以无法对应任何真值的噪声为主，40m外几乎全是纯噪声。说明单靠类别阈值最多解决近距交叉污染，无法解决远距噪声主导的问题。

### 三条候选路线的全局代价扫描

新增`boat_fp_attribution.py`，完全复用四类评估器的时钟对齐、位姿插值、世界系到船体系变换和2m匹配口径；再把boat检测与`cluster_diagnostic`按时间戳+质心关联，模拟候选簇从boat重分类到目标类别后的完整评估指标。

在历史39.8%基线日志和本轮新产生的未修改控制日志上分别扫描，结论如下：

1. **继续放宽`pillar_max_pts`：否决。**
   - 表面收益：残留pillar型boat_fallback的pts确实集中在3042-6968。
   - 但在新控制日志上，`3000→7500`会让boat召回约20.0%→18.8%（-1.2pp），虽然boat误检率可降到47.1%、pillar召回可升到22.2%，但已不是双赢。
   - 这推翻了“pts>3000全是真pillar、对boat零成本”的旧推定；保留3000。

2. **放宽`buoy_fp_max`：继续否决。**
   - `1.0→1.1`等候选会大量截胡真实boat；在新控制日志上boat召回约20.0%→18.0%。
   - 再次验证base rate陷阱：形状接近buoy的候选池太大，没有干净分界线。

3. **`block_fp_min: 1.70→1.65`：通过离线代价评估。**

| 数据 | boat改前 | boat模拟后 | block改前 | block模拟后 |
|------|----------|------------|-----------|-------------|
| 39.8%历史基线日志 | 19.4% / 51.3% | **19.4% / 49.9%** | 26.1% | **27.8%** |
| 本轮新控制日志 | 20.0% / 51.3% | **19.8% / 49.7%** | 27.7% | **29.8%** |

两份独立日志方向一致：boat误检率下降1.4-1.6pp，block召回提升1.7-2.1pp，boat召回代价0-0.2pp。`1.65`只打开很窄的尺寸窗口；继续降到1.60/1.55/1.50时真实boat和噪声代价快速增加，因此不继续放宽。

### 本轮实测暴露的第五把坏尺子：重复stamp被重复计分

为验证组合改动，使用临时编译目录跑了候选/控制两轮bag，不覆盖正式源码和原始日志。测试暴露出新的评估稳定性问题：

- 候选轮记录928条检测消息，控制轮878条；
- 去重后分别只有820/777个独立检测stamp，重复stamp内的poses完全一致；
- 现有`four_category_evaluator.py`逐条消息累计，把同一传感器结果的重复发布当作新帧重复计分；
- 两轮感知定时器相位相差约25-50ms，没有精确相同的det stamp；
- bag播放器还出现`Message queue starved`，两轮各类别一起波动，连未改动的buoy也变化，不能把原始A/B差值归因到阈值。

因此本轮不采用这组原始A/B绝对值，只采用同一份日志上的评估器同口径重分类，以及两份独立日志的交叉验证。后续评估器必须增加：
1. 按传感器stamp去重（重复stamp且poses一致只计一次）；
2. A/B按同一输入帧或共同stamp配对；
3. 不再依赖各轮`gt[0]-odom[0]`重新估offset——本轮两次自动估计分别比直接证据验证值多0.667s和1.000s，首帧漏记会稳定制造系统偏移。

### 本轮决定

- C++只保留`block_fp_min=1.65`。
- `pillar_max_pts`恢复并保持3000。
- buoy判据不改。
- 新增误检归因脚本，后续阈值调查继续坚持“运行时全集 + 完整代价评估”，不使用真值预筛样本池。

## v2评估器 + Boat时序追踪（2026-07-24）

### 先修评估口径

新增`four_category_evaluator_v2.py`，固定三项语义：

1. 相同传感器stamp且poses相同的重复发布只计一帧；
2. 检测与真值使用一对一最大匹配，额外重复检测计为误检；
3. epoch offset必须使用显式已验证值或logger的时钟证据，不再静默使用易漂移的首帧差。

同时修改`main.cpp`：

- 障碍物检测和发布统一使用front lidar的传感器stamp，不再使用处理完成时的`now()`；
- 同一front lidar stamp只处理一次，实际回放的重复检测消息从101条降为0；
- 为时序算法保存5秒odom历史，按点云stamp取最近位姿，避免处理延迟把静态目标伪装成运动目标。

**重要裁决**：旧版19.4%/51.3%的Boat数字依赖“处理时刻`now()`”而非“点云采集时刻”，在处理队列存在延迟时会把检测与错误真值帧配对，因此不能继续作为时序算法基线。使用正确传感器时间后，587个独立输入帧的v2原始基线为：

| 类别 | 召回率 | 误检率 | 位置误差 |
|------|--------|--------|----------|
| boat | **2.7%** | **93.6%** | 1.16m |
| buoy | 76.6% | 15.3% | 0.18m |
| pillar | 59.1% | 0.6% | 0.65m |
| block | 74.0% | 21.2% | 0.71m |
| 四类汇总 | 61.8% | 15.9% | — |

这组结果说明旧时间错配不只会整体抬高或压低指标，而会对不同类别造成方向不同的偏差；后续所有新实验必须使用v2口径，旧口径数字仅保留为历史记录。

### BoatTracker路线

第一版离线扫描曾在旧时间轴上得到18.4%→19.0%、52.9%→48.1%的结果，但真实C++接入未复现。排查发现追踪器读取“回调时最新odom”而不是“点云stamp对应odom”，导致处理延迟制造假速度。该版本否决，未同步。

修正时间同步后，重新在正确时间轴原始日志上扫描：

| 配置 | Boat召回率 | Boat误检率 | F1 |
|------|------------|------------|----|
| 不追踪 | 2.70% | 93.62% | 3.79% |
| 2帧确认、2.0m关联门、最多补偿5帧、无最低速度门 | **4.08%** | **92.04%** | **5.40%** |
| 同上 + 0.10m/s最低速度门 | 3.26% | **91.81%** | 4.66% |

选择无最低速度门版本：它依靠“两帧确认”抑制单帧噪声、依靠短时恒速补偿填补漏检，在误检率下降的同时取得更大的召回收益；速度门保留为ROS参数，默认关闭。

最终C++完整bag独立回放复现：

| 指标 | 正确时间轴原始基线 | BoatTracker实测 | 变化 |
|------|--------------------|-----------------|------|
| Boat召回率 | 2.7% | **3.9%** | **+1.2pp（相对+44%）** |
| Boat误检率 | 93.6% | **91.9%** | **-1.7pp** |
| Boat位置误差 | 1.16m | **1.13m** | -0.03m |
| 重复检测消息 | 0 | 0 | 无回退 |

**结论**：时序确认/短时补偿在离线扫描和真实C++回放中均同时改善召回与误检，达到同步正式工程的门槛。但Boat绝对性能仍很差，说明追踪只能修补时序断裂，不能替代前端聚类/分类；下一步应在v2正确时间轴上重新做Boat漏检和误检来源归因。

## header.stamp时间轴翻案：v2"更正确的时间轴"结论被推翻（2026-07-24）

### 起因

上一节记录的BoatTracker集成同时改了main.cpp四处：引入tracker逻辑、时间戳源从`now()`改成`msg->header.stamp`、加了一个"相同front lidar stamp只处理一次"的去重guard、加了odom订阅。当时的结论是"旧19.4%基线受处理时刻`now()`错配影响，应统一用v2正确时间轴"，据此把BoatTracker的2.7%→3.9%记为最终结果。**这个结论没有做单变量拆解就下了，本节把它拆开重新核实，结果是相反的。**

### 第一步：交叉诊断复现"header.stamp四类暴涨、boat暴跌"的模式

在`onLidar`加`[时钟对比]`诊断打印`now()`与`header.stamp`的差值，发现该差值**不是匀速漂移，而是运行中段（bag内容锚定的header≈50-65秒附近，两次独立跑测复现于同一位置）出现一个膨胀到~7秒又自行收缩的"鼓包"**——开局和结尾的diff几乎相等，只有中段异常。同一次跑测里给去重guard加计数器，发现丢帧率也不是均匀的19.3%，而是集中爆发在同一个header时间窗口（33%~65%），两个异常时间上重合。

### 第二步：假说一（front_lidar滞后导致丢帧、进而拖累时间戳）——被数值量级检验推翻

推理方向：`reference_stamp = lidar_stamps_.front()`只取front_lidar一路的stamp代表整个融合帧，如果它滞后于其余6路的实际内容，会让评估器在错误时刻查boat真值。

**量级检验**：boat在86秒内移动约20米，均速0.23m/s，匹配容差2.0m，需要**至少8.7秒**的时间戳偏差才会造成失配。而实测的diff全程漂移不到1秒（鼓包最严重时约7秒，且只持续约12秒）。**1秒偏差对应约0.23米的位置误差，远小于2m容差，不足以解释boat从19%到2-4%（跌幅87%）的崩溃。** 这个假说方向对、机制说得通，但一算数量级就不成立——**是本轮最有价值的一次否决，如果没做这步量级检验，很可能会去改`reference_stamp`的取法（比如改成取7路中位数），白投入一轮**。

**教训（应作为纪律）**：任何"时间戳偏差导致匹配失败"类假说，在开始改代码前，必须先用"目标最大速度 × 需要的偏差量 vs 匹配容差"做一次量级检验，通不过就不要投入实现。

### 第三步：直接排除去重guard——A/B测试推翻"去重是boat崩溃真凶"

`git show HEAD:src/main.cpp`确认这个去重guard是这次BoatTracker集成新加的、从未提交过的代码，此前所有commit里都不存在。移除后（保持header.stamp、tracker关闭）重新跑：det消息数从628→687（确认去重此前确实在丢帧），但**boat召回率没有回升，反而从3.2%略降到2.3%**。直接证伪"去重guard是boat崩溃主因"。去重guard本身被永久移除（无论如何都不该只认一路雷达的stamp）。

### 第四步：shift扫描定位真实偏移量——四类同时收敛，一锤定音

用离线脚本对header.stamp版`boat_log.jsonl`的det时间戳做整体平移扫描（去重已移除、tracker已关闭，纯粹隔离时间戳源这一个变量）：

| shift(s) | boat | buoy | pillar | block |
|---|---|---|---|---|
| 0（header.stamp原始） | 3.1% | 74.8% | 59.0% | 73.4% |
| +8 | 17.5% | 64.4% | 19.6% | 28.0% |
| +9 | 18.6% | 63.7% | 19.1% | 27.5% |
| **now()基线（独立路径）** | **19.3%** | **64.0%** | **19.5%** | **28.2%** |

**四个类别、四个不同数量级，全部在shift≈+8~9秒处同时收敛到`now()`路径独立测出的基线数字。** 单一类别收敛可能是巧合，四个类别同时收敛只有一种解释：**这就是真实的系统性偏移量**，而且证明了header.stamp路径下buoy/pillar/block在shift=0时的"暴涨"（59-75%）本身就是同一个错位造成的测量假象——不是评估变准，也不是检测变好。

**这个~8秒的数字并不是新发现**——`four_category_evaluator.py`自己的注释里早写着"该节点有真实启动延迟(~8秒)……绝不能用det自己的[0]算offset"，但当时的理解停留在"防止污染offset估算"，没意识到这个延迟已经污染了header.stamp路径下**每一条**det记录的时间戳本身，不只是第[0]条。**这是一次"知道这个事实、但没意识到其完整影响面"的典型案例。**

### 结论（推翻上一节的裁决）

- **上一节"旧19.4%基线受`now()`错配影响，应改用header.stamp"的结论是错的，方向反了。** `now()`路径配合`gt[0]-odom[0]`的epoch_offset校正是可靠的；header.stamp路径本身携带一个未被任何机制校正的~8-9秒系统性滞后。
- **两条独立路径（now()、header.stamp+shift修正）交叉验证后的真实基线是：boat≈19.3%、buoy≈64.0%、pillar≈19.5%、block≈28.2%，总召回≈39.9%、总误检≈42.6%**——和这条线更早期反复验证过的39.8-39.9%/42.6-43.4%基线一致。62.2%（header.stamp原始口径的总召回）不能采信。
- **上一节记录的BoatTracker "2.7%→3.9%"是在错误(header.stamp、~8-9秒偏移)时间轴上测出来的，不能作为最终结论**，需要在改回`now()`后重新测。
- main.cpp的时间戳源已改回`now()`；去重guard已永久移除。改动处加了注释，防止以后有人觉得"应该用传感器时间戳"又改回去：
  ```cpp
  // 时间戳用now()而非msg->header.stamp：header.stamp路径下det时间戳会带上
  // 节点/传感器管线未知来源的系统性滞后（实测~8-9秒，shift扫描定位），且这个
  // 滞后无法被epoch_offset机制校正（offset只用gt[0]-odom[0]估计，不含这一项）。
  // 2026-07-24：四类召回率在shift=0时全部失真（boat虚低、buoy/pillar/block虚高），
  // shift+8~9秒后四类同时收敛到now()路径的独立基线，证实为系统性偏移而非检测差异。
  ```

### 方法论沉淀

1. **"多个独立指标是否在同一参数处同时收敛到已知基线"是验证系统性偏移的强判据**——比单一指标的改善/恶化更可信，因为多指标同时对齐几乎排除了巧合。以后遇到"某个改动让多个指标同时变化"的情况，优先尝试这个方法。
2. **任何"时间戳偏差导致匹配失败"的假说，必须先做数量级检验（目标最大速度 × 所需偏差 vs 匹配容差）再投入实现**，不要等代码写完、测试跑完才发现数量级不对。
3. 两个被推翻的假说本身要记录，不只记成功——"污染窗口"假说被A/B测试（钳制vs排除逐位相同）推翻，"front_lidar滞后"假说被数值量级检验推翻，这两次否决都避免了往错误方向继续投入。
4. **一次改动不要捆绑多个变量。** 这次BoatTracker集成同时改了tracker逻辑、时间戳源、去重guard、odom订阅四处，导致后续花了好几轮才拆解清楚哪个变量导致了哪个效应。以后任何涉及超过一个可能影响输出变量的改动，应拆成多个commit分别测试。

### main.cpp已改回now()，去重guard与临时诊断打印均已移除

- `processLidarBranch`的`reference_stamp`改回`now()`（带解释性注释，说明为什么不用`header.stamp`）
- 只判front_lidar一路stamp的去重guard永久删除
- 临时的`[时钟对比]`(onLidar)和`[去重诊断]`(processLidarBranch)诊断打印均已移除
- 未使用的`lidar_stamps_`成员及其读写点一并清理（去重guard删除后这个成员变成只写不读的死代码）

### BoatTracker在干净时间轴上重测：净效果在噪声范围内

用改回`now()`后的main.cpp，tracker开启/关闭各跑3次取中位数（每次det消息数857-859，口径一致）：

| 配置 | Boat召回率(3次) | 中位数 | Boat误检率(3次) | 中位数 |
|------|------------------|--------|------------------|--------|
| tracker关闭 | 19.0% / 19.2% / 19.2% | **19.2%** | 39.4% / 39.3% / 39.2% | **39.3%** |
| tracker开启 | 19.0% / 19.2% / 19.3% | **19.2%** | 39.5% / 39.2% / 41.7% | **39.5%** |

**中位数召回率完全相同(19.2% vs 19.2%)，误检率差异(39.3% vs 39.5%)落在单次跑测本身的抖动范围(±0.1-0.3pp)内。BoatTracker在这个场景下目前没有可测量的净效果**——此前"2.7%→3.9%"的增益完全是header.stamp错误时间轴（~8-9秒系统偏移）造成的假象，不是tracker的真实能力。tracker的参数（2帧确认、2.0m关联门、5帧补偿）或运动模型可能和这个场景（低速漂移目标为主）不匹配，需要单独评估是否调整或搁置。

## BoatTracker时钟域修复与有效A/B（2026-07-24）

### “Tracker无效果”结论再次作废：当时Tracker实际从未运行

上一节把发布stamp改回`now()`后，Tracker仍直接用同一个`sensor_stamp_sec`去查odom历史。最新日志直接证明两者不在同一时钟域：

- det/`now()`：绝对纪元时间，中位数约`1782954249`
- odom header：bag相对时间，中位数约`38.428`
- 最近邻同步容差：`0.25s`

因此`odom_valid`永远为false，开启Tracker时每帧实际都执行`boat_tracker_.reset()`，从未调用`update()`。此前“tracker开/关各跑3次、指标相同”的A/B，本质是“tracker关 vs tracker关”，不能用于判断算法效果。

同时纠正上一节的速度数量级：用“首尾净位移/总时间”得到的0.23m/s不是逐帧运动速度。对最新GT逐帧计算（2520个样本）：

| 指标 | 船速 |
|------|------|
| 中位数 | **1.307m/s** |
| P90 | 1.388m/s |
| P95 | 1.418m/s |
| 最大值 | 1.716m/s |

因此2m匹配容差在典型速度下只对应约1.5s，不是8.7s；此前据此推翻front_lidar/融合时间滞后假设的数量级依据错误，该中间结论撤回。

### 发布时钟与Tracker测量时钟分离

`main.cpp`现在明确维护两个互不混用的时间：

1. `publish_stamp=now()`：绝对纪元域，只用于发布消息和现有评估链路；
2. `tracker_stamp_sec`：七路lidar header stamp的算术均值，属于bag相对时间域，只用于查找odom和推进Tracker状态。

odom历史窗口从5s扩到30s，以覆盖实测约8-9s的“测量时间→发布时间”管线延迟。新增诊断：

- `tracker_update_count`
- `odom_sync_hit/miss`
- 最近odom gap的8档分布与均值
- Tracker总reset、非单调reset、大间隔reset
- 七路lidar stamp跨度
- 七路stamp完全相同的重复融合输入跳过计数

第一轮有效运行的最终计数：

| 诊断 | 结果 |
|------|------|
| Tracker update | **764次** |
| odom sync hit / miss | **764 / 0** |
| odom最近时差均值 | **25.90ms** |
| 七路lidar跨度（2s抽样） | 0-200ms，中位数0ms |
| 发布相对时间 - Tracker测量时间 | 8.318-9.222s，中位数8.918s |

这说明8-9s主要是整组测量数据相对处理/发布时间的管线延迟；本轮抽样没有看到“只有front_lidar单路落后7s、其他六路接近当前时刻”的证据。

进一步稳定化：

- 不恢复旧的“只看front_lidar stamp”去重guard；
- 只在七路stamp逐项完全相同时跳过，确保没有任何传感器缓存发生更新；
- Tracker代表时刻从七路stamp中位数改成算术均值，任意一路单调更新都会推动时间前进。

稳定化后实测：761次update、0 sync miss、**0次reset**，检测消息775 raw/775 unique，消除了此前单轮20-64次reset和101条重复stamp。

### 真正运行后的A/B与单变量速度门扫描

同一代码、同一bag、v2一对一/去重口径，Tracker关闭3轮：

| 配置 | Boat召回率 | Boat误检率 |
|------|------------|------------|
| off-1 | 18.1% | 41.0% |
| off-2 | 18.2% | 41.3% |
| off-3 | 17.9% | 41.9% |
| **中位数** | **18.1%** | **41.3%** |

Tracker稳定化后和速度门单变量筛选：

| 最低速度门 | Boat召回率 | Boat误检率 | 相对关闭中位数 | 裁决 |
|------------|------------|------------|----------------|------|
| 0.00m/s | 21.2% | 43.5% | 召回+3.1pp，误检+2.2pp | 取舍 |
| 0.10m/s | 19.8% | 43.0% | 召回+1.7pp，误检+1.7pp | 取舍 |
| 0.40m/s | 17.8% | 40.6% | 召回-0.3pp，误检-0.7pp | 取舍 |
| 0.50m/s | 17.5% | 40.7% | 召回-0.6pp，误检-0.6pp | 取舍 |

**结论**：Tracker真正运行后具有可测效果，但当前实现沿速度门扫描没有找到“召回不降且误检不升”的Pareto改进点。低速度门用更多短时补帧换召回，同时带来更多误检；高速度门能压误检，但也过滤真实目标。故保留实现和诊断作为实验能力，`boat_tracker_enabled`默认改为`false`，不进入正式默认输出。

### 提交纪律

- 感知子仓库此前没有`.gitignore`，导致在子目录误跑colcon后`build/`、`install/`、`log/`出现在未跟踪列表；现补充三项忽略规则。
- 本轮仍不提交。后续应把“时钟域/诊断基础设施”和“实验性Tracker”拆成独立commit，且只按精确文件列表暂存，禁止`git add .`。

## 待办

1. **`std::cout`并发写入加锁**——已知风险，未修复，投入产出比暂不够，先记录
2. ~~0-20m分类阈值调整~~ **已完成**——召回18.8%→20.4%，误检70.0%→68.4%
3. ~~buoy判据调整~~ **已否决**——base rate陷阱，没有清白的分界线
4. **大簇组的粘连部分(96个，65.8%偏移>1m)**——只处理了"中心重合"的34.2%，粘连部分需要动`cluster_tol`/`boat_split_fp_threshold`，风险面是全局性的，动手前必须先做代价评估（样本池不能用真值预筛）。偏移分布max=1.99m顶到`CANDIDATE_SEARCH_RADIUS=2.0m`天花板，真实粘连规模可能被低估
5. **判据顺序结构性重构**——暂不动，等第三个截胡案例出现再评估
6. ~~20-40m聚类/点密度问题~~ **已定性**——是点数门槛静默丢弃，机制推定为死胡同（未实测），下一步是多帧累积（大改动，暂不做）或接受为能力边界
7. **`discarded_below_min_pts`诊断打印**——几行代码零风险的改动，消除日志静默丢弃盲区，待做
8. ~~另外三类目标从未单独评估过~~ **已完成**——当前基线boat 19.4%/buoy 64.0%/pillar 19.4%/block 26.1%，总召回39.8%/总误检43.4%
9. ~~pillar判据逐条通过率诊断~~ **已完成，见上"pillar_max_pts修复"一节**——15.7%→19.4%，且带来boat误检-18pp的额外收益
10. ~~**boat误检归因**~~ **已完成，见上文2026-07-24记录**——2m最近真值归因为buoy 23.7%/pillar 19.7%/block 9.5%/都不匹配47.1%；结合形状和代价评估后，只有`block_fp_min 1.70→1.65`通过，继续放宽pillar/buoy均因boat召回代价被否决
11. **`ObstacleDetector.hpp`里其他"为保护X类别而收紧/放宽"的参数**——`block_fp_min`已完成排查并微调到1.65；下一项是`boat_fp_max_sane=2.0`（"从6.0大幅收紧……仅损失11%真船召回"），需要用同样的运行时全集代价评估核实
12. **pillar/block两次改动后出现的±1-2pp波动，单次跑测未复现验证，推定为正常抖动**——如果后续诊断查出真实问题，需要重新核实
13. **给师姐的结论**：这套感知目前的有效探测距离约20米，20-40m召回率骤降到约1/4，40米以外基本等于没有感知能力（0次命中）——避障/控制逻辑不应设计成依赖20米以外的检测结果
14. ~~**评估器按stamp去重**~~ **已完成**——`four_category_evaluator_v2.py`按stamp去重且一对一匹配；C++端"同一front lidar stamp只处理一次"的去重guard后被证实会误伤丢帧且不解决boat问题，已永久移除（见下"header.stamp时间轴翻案"一节）
15. ~~**canonical epoch offset改为可复用的已验证值或稳健估计**~~ **已完成第一阶段**——v2要求显式已验证offset或logger时钟证据，不再默认首帧差；下一步给logger记录`clock_stamp`，消除手工传参
16. ~~BoatTracker "2.7%→3.9%"结论已作废，需在改回`now()`后重测~~ **已完成**——干净时间轴上tracker开/关各跑3次取中位数，召回率19.2% vs 19.2%完全相同，误检率39.3% vs 39.5%落在噪声范围内，**净效果不可测**，见上"BoatTracker在干净时间轴上重测"一节
17. ~~在v2正确时间轴上重做Boat病因归因~~ **前提已推翻**——"v2的header.stamp时间轴更正确"这个前提本身是错的（见下一节），旧`now()`基线(19.4%/51.3%附近)才是可靠的；Boat病因归因应在改回`now()`后的干净基线上重做

## v2插值边界翻案 + 候选级时序重分类（2026-07-24）

### 第八把坏尺子：v2注释说“不外推”，代码却把越界时间永久钳到首/末帧

在准备做候选轨迹模拟时复查`four_category_evaluator_v2.py`，发现
`make_odom_interpolator()`和`make_gt_interpolator()`的首尾边界条件都写反：

```python
# 错误：任何晚于末帧的t都满足，等价于永久钳制到最后一帧
return records[-1] if t >= stamps[-1] - MAX_TIME_GAP else None

# 正确：只允许末帧之后MAX_TIME_GAP以内
return records[-1] if t <= stamps[-1] + MAX_TIME_GAP else None
```

开头边界同理，应检查`t >= first_stamp - MAX_TIME_GAP`，而不是
`t <= first_stamp + MAX_TIME_GAP`。本bag的det有777个独立stamp，但odom只覆盖到
76.752s；修复前最后约9秒的88帧一直用冻结的末帧odom/GT计分且不报错。修复后有效
帧统一为689帧，修正版评估器与独立候选归因脚本逐项交叉验证（hit/miss/fp完全一致）。

三轮Tracker-off重算：

| 轮次 | Boat召回率 | Boat误检率 | hit / fp | 有效帧 |
|---|---:|---:|---:|---:|
| off-1 | 15.54% | 44.96% | 1071 / 875 | 689 |
| off-2 | 15.59% | 45.40% | 1074 / 893 | 689 |
| off-3 | 15.34% | 46.04% | 1057 / 902 | 689 |
| **中位数** | **15.54%** | **45.40%** | — | **689** |

因此此前18.1%/41.3%的Boat基线作废。Tracker结论方向不变：普通ON三轮中位数
17.00%/48.63%，稳定版0m/s门单轮18.17%/47.32%，仍然都是用误检换召回，没有
Pareto点。修复影响绝对数字，但没有推翻“旧BoatTracker不作为默认输出”的裁决。

### 修正口径上的候选簇归因

三轮OFF日志合并统计：

- 全部Boat漏检中，**68.86%没有2m内原始候选簇**；仅31.14%能靠分类层补救。
- 0-20m漏检中，**71.5%存在候选簇**，主要被判成`buoy`（42.87%）或
  `discarded_shape_implausible`（25.79%），适合候选级重分类。
- 20-40m与40-60m漏检中分别有78.38%和77.89%没有候选簇；这部分必须改聚类、
  多帧点云积累或接受为能力边界，分类/Tracker无法凭空恢复。
- Boat误检中75.02%不在任何其他类别GT的2m内；原始标签中56.22%来自
  `boat_fallback`。说明误检主因已经从“跨类别截胡”转为远距/水面噪声与fallback
  的单帧误判。

### 候选级因果时序规则

离线规则只使用当前及过去信息，不读取GT：

1. 把全部原始候选（包括buoy和两个discarded分支）变换到世界系；
2. 0.5m门限、最大0.35s间隔做一对一贪心关联；
3. 主`boat`标签直接保留；
4. `boat_fallback`必须在同轨迹过去1秒内已有boat/fallback证据才发布；
5. 0-20m内当前被判成`buoy`、`discarded_shape_implausible`或
   `discarded_low_confidence`的候选，如果同轨迹过去1秒内有boat证据，则重分类为boat；
6. 被提升的主`buoy`从buoy输出移走，避免双标签。

三轮旧OFF日志的离线因果复现：

| 轮次 | 基线 Boat召回/误检 | 候选时序 Boat召回/误检 |
|---|---:|---:|
| off-1 | 15.54% / 44.96% | **21.18% / 36.76%** |
| off-2 | 15.59% / 45.40% | **21.46% / 36.93%** |
| off-3 | 15.34% / 46.04% | **20.97% / 37.63%** |
| **中位数** | **15.54% / 45.40%** | **21.18% / 36.93%** |

0.5/1.0/1.5m三个关联门方向一致。独立新日志上，离线结果为21.28%/37.11%；
同时Buoy召回68.43%→68.42%（-0.01pp），Buoy误检25.09%→24.17%（-0.92pp），
说明Boat收益没有以牺牲Buoy为代价。

### C++真实回放

新增默认关闭的`CandidateTemporalClassifier`，`ObstacleSet`内部保留完整候选流。
参数：

- `candidate_temporal_enabled=false`
- `candidate_temporal_gate_m=0.5`
- `candidate_temporal_max_gap_sec=0.35`
- `candidate_temporal_evidence_sec=1.0`
- `candidate_temporal_range_m=20.0`

真实C++回放（旧BoatTracker关闭，候选时序开启）：

| 类别/汇总 | 基线 | 候选时序 | 变化 |
|---|---:|---:|---:|
| Boat召回 | 15.5% | **21.1%** | **+5.6pp** |
| Boat误检 | 45.4% | **37.3%** | **-8.1pp** |
| Buoy召回 | 68.4% | **68.4%** | 基本不变 |
| Buoy误检 | 25.1% | **24.2%** | **-0.9pp** |
| 四类总召回 | 37.6% | **38.1%** | **+0.5pp** |
| 四类总误检 | 47.9% | **47.2%** | **-0.7pp** |

775个独立检测帧、688个有效评估帧；候选诊断最终累计提升495次、暂缓无历史依据的
fallback 80次。C++结果与离线预测一致，属于真实Pareto改进。当前仍保持默认关闭，
待再做至少两轮真实ON复现后再决定是否改为默认开启。

### 新增诊断与一次无效回放

`cluster` JSON记录新增`measurement_t`（七路lidar header stamp均值），同时保留`t`
（发布/评估时间）；新日志77402/77402个候选均有测量时间。`boat_logger.py`支持
`BOAT_LOG_PATH`和`CLUSTER_LOG_PATH`环境变量，便于隔离测试而不覆盖正式日志。

第一次隔离回放漏传`use_sim_time:=true`，导致det使用墙钟`1784889...`、GT仍在bag
时钟`1782954...`，得到4.7%/84.8%的无效结果。该轮不进入任何结论。修正脚本后，
det、GT重新处于同一bag绝对纪元域，基线复现为15.49%/45.56%。再次证明回放脚本
必须显式传`use_sim_time:=true`，不能只看到`ros2 bag play --clock`就假设节点会自动
使用仿真时钟。

### 当前裁决

- v2首尾永久钳制已修复；所有旧v2绝对数字需按需重算，不能继续引用。
- 旧BoatTracker继续保留为默认关闭实验能力。
- 候选级时序重分类通过三轮离线复现、双类别代价检查和一次真实C++回放，暂保留且
  默认关闭；再完成两轮真实ON复现后决定是否默认开启。
- 20m外主要瓶颈仍是“没有候选簇”，下一条独立路线是多帧点云积累/距离自适应聚类，
  不应继续靠放宽分类阈值解决。

---

## 比赛接口、无 odom 合规性与七路外参扰动首轮审计（2026-07-24）

### 正式输出接口：静态检查 + 完整 bag 动态核验

按任务书只检查感知/成像模块。当前六个正式输出话题名称、消息类型与坐标系声明均正确：

| 输出 | 消息类型 | 实测频率 | frame_id | 动态结果 |
|---|---|---:|---|---|
| `/world/obstacles/bev_check` | `sensor_msgs/Image` | 4.94 Hz | `wamv/base_link` | 401 帧，时间戳非零且单调 |
| `/world/obstacles/lidar_check` | `sensor_msgs/PointCloud2` | 9.99 Hz | `wamv/base_link` | 776 帧，`x/y/z` 字段完整 |
| `/world/obstacles/buoys_check` | `geometry_msgs/PoseArray` | 9.99 Hz | `wamv/base_link` | 776 帧 |
| `/world/obstacles/boats_check` | `geometry_msgs/PoseArray` | 9.99 Hz | `wamv/base_link` | 776 帧 |
| `/world/obstacles/pillars_check` | `geometry_msgs/PoseArray` | 9.99 Hz | `wamv/base_link` | 776 帧 |
| `/world/obstacles/blocks_check` | `geometry_msgs/PoseArray` | 9.99 Hz | `wamv/base_link` | 776 帧 |

四类消息的首个非空姿态四元数模长为 1。BEV 为 `1000×1000 bgr8`，船中心位于图像中心；
`range_m=10` 的实现范围是 x/y 各 `[-10m,+10m]`，即船周围各方向 10m、总覆盖
`20m×20m`，与任务书“无人船周围 10m 范围内”一致。

### 本船位姿稳定性与纯 base_link 时序关联

从完整 bag 的 1476 帧 odom（有效跨度 76.648s）计算：

- 端点平移 1.047m，x/y 范围分别为 0.413m/0.963m；
- 平移逐帧 P95 为 1.95mm，最大 1.95mm；
- roll 全程范围 `3.6e-5°`，pitch 范围 `0.372°`；
- yaw 全程累计变化 `48.47°`，因此“全程姿态不变”不成立；
- 但 yaw 逐帧 P95 仅 `0.088°`、最大 `0.121°`，角速度 P95 `0.846°/s`。

候选时序不是固定参考帧匹配，而是每帧更新轨迹位置，因此决定性量是相邻帧变化。对同一份
689 有效帧、84353 个候选簇做世界系与纯 `base_link` 关联 A/B：

| 关联坐标系 | Boat 召回率 | Boat 误检率 | promoted | suppressed fallback |
|---|---:|---:|---:|---:|
| world（依赖 odom） | 21.40% | 37.31% | 427 | 38 |
| base_link（不依赖 odom） | 21.40% | 37.34% | 427 | 37 |

结论：纯 `base_link` 误检仅 +0.03pp，收益完整保留。正式测试不发布 odom，因此
`CandidateTemporalClassifier` 下一步可以移除 odom 依赖；不需要为这个数据集额外引入 IMU。

### 旧外参鲁棒性脚本无效

旧 `robustness_test.py` 只打印“应用扰动”，并未实际改写外参；旧 `run_robustness.sh` 传入的
`nominal_perturb_*` 参数在当前节点中没有声明。此前“扰动后收敛到 0.04–0.11m”的结论
不能继续作为当前代码的鲁棒性证据。

已增加默认关闭、固定随机种子的测试入口，对七路雷达分别独立施加
`x/y/z ±0.5m`、`roll/pitch/yaw ±10°`，并逐路打印真实扰动和最终残差。

### 七路同时扰动 seed 0：首轮端到端失败

首次有效 seed 0 回放中，七路最大初始误差达到约 0.54m/9.7°。当时 ICP 没有产生任何有效
更新，最终残差几乎等于初始扰动：

- 最大平移残差 0.5378m；
- 最大旋转残差 9.706°。

端到端结果：

| 指标 | 无扰动基线 | 七路同时扰动 seed 0 | 变化 |
|---|---:|---:|---:|
| Boat 召回/误检 | 15.5% / 45.4% | 13.7% / 71.3% | 明显恶化 |
| Buoy 召回/误检 | 68.4% / 25.1% | 51.1% / 47.7% | 明显恶化 |
| Pillar 召回/误检 | 11.3% / 80.7% | 5.8% / 90.1% | 明显恶化 |
| Block 召回/误检 | 15.6% / 83.1% | 5.3% / 94.3% | 明显恶化 |
| 四类总召回/误检 | 37.6% / 47.9% | 26.8% / 65.2% | `-10.8pp / +17.3pp` |

这轮证明外参扰动确实是当前最大端到端风险。

### 标定一直失败的根因之一：Inf 未过滤

原输入回调只过滤 NaN，没有过滤 LiDAR 无回波常见的 `±Inf`。同一时刻七路原始点云统计：

| 传感器 | 总点数 | 有限点 | NaN | Inf |
|---|---:|---:|---:|---:|
| front | 17280 | 2015 | 185 | 15265 |
| right | 17280 | 5825 | 204 | 11455 |
| right2 | 17280 | 4121 | 204 | 13159 |
| back | 17280 | 2372 | 170 | 14908 |
| left2 | 17280 | 3516 | 204 | 13764 |
| left | 17280 | 6022 | 204 | 11258 |
| forward | 34560 | 8899 | 0 | 25661 |

大量 Inf 进入 ICP 后污染协方差矩阵，使 `H.allFinite()` 失败；同时这些无效点也被发布到
`lidar_check`。输入边界改为 `std::isfinite(x/y/z)` 后：

- `lidar_check` 抽样帧从 137069 点降为 31705 个有限点；
- 非有限点数量变为 0；
- BEV 仍约 4.88Hz，激光与四类输出仍约 10Hz；
- 无扰动完整回放为四类总召回 37.6%、总误检 47.8%，与旧基线等价；
- Boat 15.8%/44.8%，Buoy 68.4%/25.1%，Pillar 11.3%/80.6%，
  Block 15.5%/83.1%。

因此有限点过滤是“融合点云合法性显著改善、检测无代价”的有效修复。

### Inf 修复后的 ICP 现状：能运行，但仍不能可信收敛

过滤 Inf 后，ICP 从“立即失败”变成能找到大量对应点并输出修正。但无扰动短测中，
`forward_lidar` 被持续向 z 方向推动（单轮约 0.85→0.49→0.38→0.28→0.23m），5 轮仍
不收敛；seed 0 下多路修正也出现大幅、方向不稳定的跳动。单次标定调用约 10–18s，
完整 bag 只完成约 6 次尝试，计算成本也过高。

另外，当前算法固定 `front_lidar` 为名义外参，无法校正参考传感器自身的随机误差；
即使其余传感器内部对齐，整体相对船中心仍存在不可消除的公共偏差。

已加入安全闸：达到最大尝试次数仍未收敛时，拒绝热替换未验证估计，保留名义外参。
这避免把正确基线校坏，但不等于解决了比赛标定任务。

### 当前裁决与下一步

1. 六个正式输出接口通过动态核验；BEV 10m 语义按“船中心向各方向 10m”保留。
2. `std::isfinite` 输入过滤应保留，它同时修复标定输入和 `lidar_check` 无效点，且检测无代价。
3. 未收敛结果拒绝上线的安全闸应保留。
4. 当前轮转式“每路对其余六路并集”的单帧 ICP 不可作为比赛标定方案；不得因为日志显示
   “标定尝试”就认为标定在工作。
5. CandidateTemporal 的纯 `base_link` 合规路线已被离线 A/B 证明可行，但外参鲁棒性仍优先。
6. 标定下一步先拆分：Airy 相邻传感器配对、forward 单独约束、跨帧静态目标累积，并显式
   引入残差下降/对应点比例/无扰动不漂移三道接受条件；通过前不得自动热替换。
7. BEV 与融合点云仍缺官方同口径误差评分器；本轮只完成消息合法性和检测端到端代价检查。

### 七路 LiDAR 两两重叠矩阵：可标定性不是均匀的

为避免继续盲调 ICP，先在完整 bag 的 10/25/40/55/70s 五个时刻量化几何重叠。统计口径：

- 每路点云先过滤非有限点，再用 0.1m 体素质心降采样；
- 使用名义外参变换到 `wamv/base_link`；
- 对每一对点云计算双向最近邻；
- “保守重叠率”取两个方向覆盖率的较小值，“保守对应点数”取两个方向命中数的较小值；
- 下表每格为五个时刻的中位数，格式为 `0.3m 保守重叠率 / 保守对应点数`。

| | front | right | right2 | back | left2 | left | forward |
|---|---:|---:|---:|---:|---:|---:|---:|
| **front** | 100% | 1.0% / 20 | 9.7% / 82 | 0 / 0 | 11.0% / 81 | 0.8% / 9 | 3.5% / 64 |
| **right** | 1.0% / 20 | 100% | 23.9% / 300 | 21.9% / 354 | 0 / 0 | 0 / 0 | 0.3% / 5 |
| **right2** | 9.7% / 82 | 23.9% / 300 | 100% | 3.7% / 56 | 0 / 0 | 0 / 0 | 0.6% / 12 |
| **back** | 0 / 0 | 21.9% / 354 | 3.7% / 56 | 100% | 0.4% / 4 | 3.7% / 59 | 0 / 0 |
| **left2** | 11.0% / 81 | 0 / 0 | 0 / 0 | 0.4% / 4 | 100% | 32.4% / 297 | 1.1% / 20 |
| **left** | 0.8% / 9 | 0 / 0 | 0 / 0 | 3.7% / 59 | 32.4% / 297 | 100% | 0.4% / 8 |
| **forward** | 3.5% / 64 | 0.3% / 5 | 0.6% / 12 | 0 / 0 | 1.1% / 20 | 0.4% / 8 | 100% |

关键配对在五个时刻的严格 0.3m 重叠范围：

| 配对 | 重叠率 min/median/max | 对应点数 min/median/max | 判断 |
|---|---:|---:|---|
| left2-left | 31.1/32.4/40.5% | 277/297/469 | 强且稳定 |
| right-right2 | 17.9/23.9/26.0% | 254/300/385 | 强且稳定 |
| right-back | 7.3/21.9/43.2% | 149/354/724 | 强，但受场景内容影响 |
| front-left2 | 6.9/11.0/15.2% | 58/81/94 | 中等 |
| front-right2 | 8.5/9.7/10.8% | 73/82/88 | 中等且稳定 |
| front-forward | 1.9/3.5/6.5% | 43/64/70 | 弱，不足以支撑通用 6DoF ICP |

0.5m/0.8m 阈值下 `front-forward` 的中位保守重叠虽升到 8.7%/107 和
16.7%/140，但放宽最近邻门限会同时引入更多错误对应，不能把它当成 0.3m 严格几何重叠的
等价证据。其共同点集中在很窄的空间带，无法稳定约束 6DoF；此前无扰动下 forward 被持续
推高 z，和这个几何退化完全一致。

Airy 可先采用一棵有数据依据的相邻配对树：

`front-right2-right-back` 与 `front-left2-left`

它能覆盖六路 Airy，避免把任一路与“其余五路并集”强行配准。`back-left`、
`right2-back` 等弱边只可作为冗余核验，不能作为主约束。`forward_lidar` 不再进入通用
点云 ICP：优先保留名义外参，后续单独研究水面平面约束和跨传感器共同目标约束。

### 无扰动三轮“标定不害人”验证

在 Inf 过滤和当前未收敛拒绝策略下，用同一完整 bag 独立运行三次：

| 轮次 | det / 有效帧 | Boat 召回/误检 | Buoy 召回/误检 | Pillar 召回/误检 | Block 召回/误检 | 四类总召回/误检 | 标定尝试/接受 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | 776 / 689 | 15.7% / 45.4% | 68.4% / 25.1% | 11.3% / 80.6% | 15.6% / 83.1% | **37.6% / 47.9%** | 6 / 0 |
| 2 | 770 / 682 | 15.3% / 45.9% | 68.6% / 25.3% | 11.3% / 80.5% | 15.4% / 83.2% | **37.6% / 47.9%** | 6 / 0 |
| 3 | 777 / 689 | 15.7% / 45.3% | 68.4% / 25.1% | 11.3% / 80.6% | 15.5% / 83.1% | **37.6% / 47.9%** | 6 / 0 |

三次总指标逐位一致，18 次标定尝试均未被接受，也没有发生外参热替换。当前版本在这段
比赛长度数据上已证明“未收敛时不害人”，但同时也证明它仍未完成任何有效自标定。

### 标定路线裁决：有限但可靠

后续不再追求“七路单帧 ICP 全自动收敛”的表面完整性，改为有限但可靠的方案：

1. 六路 Airy 只沿重叠矩阵支持的相邻边做配准，并使用名义外参先验防止整图漂移；
2. forward 不参与通用 ICP，默认名义外参；水面平面只能约束部分自由度，共同立柱/船体等
   目标级对应存在时再补充可观测方向；
3. 接受条件扩为四道：残差真实下降、双向对应比例达标、无扰动不得漂移、修正量必须处于
   任务书的 ±0.5m/±10° 物理边界内；
4. 第四道采用“整项拒绝”而不是 clamp。若估计要求修正 0.8m，说明解本身不可信，不能剪成
   0.5m 后继续应用；
5. 每条边需跨多个时刻重复成立，再进入全局图优化；任何失败都回退到名义外参。

这一路线承认 forward 和弱重叠边的可观测性边界，优先保证扰动下不从基线
37.6%/47.9% 崩到 26.8%/65.2%，同时保留一个理论依据明确、可在比赛说明中诚实陈述的
无陆地标定方法。

### seed 0 部分校正上界：只完美校正六路 Airy 不够

在实现配对图之前，增加了默认关闭的测试入口
`test_restore_airy_after_perturb`，用于估算“六路 Airy 标定完美成功、forward 完全未校正”
的乐观上界。实验严格复用七路同时扰动 seed 0 的随机序列：

- 七路均先施加和原实验逐值相同的扰动；
- front/right/right2/back/left2/left 六路随后直接恢复到参考真值；
- forward 保留 seed 0 扰动：
  `dxyz=(-0.2777,-0.1135,+0.4026)m`，
  `drpy=(-1.001,+2.261,+8.047)°`；
- 将标定定时器移到回放窗口外，实测标定尝试为 0，避免 ICP 干扰固定外参对照；
- 776 个检测 stamp、688 个有效评估帧，与基线口径差异小于 1%。

端到端结果：

| 状态 | Boat 召回/误检 | Buoy 召回/误检 | Pillar 召回/误检 | Block 召回/误检 | 四类总召回/误检 |
|---|---:|---:|---:|---:|---:|
| 无扰动名义外参 | 15.7% / 45.4% | 68.4% / 25.1% | 11.3% / 80.6% | 15.6% / 83.1% | **37.6% / 47.9%** |
| 七路同时扰动 seed 0 | 13.7% / 71.3% | 51.1% / 47.7% | 5.8% / 90.1% | 5.3% / 94.3% | **26.8% / 65.2%** |
| 六路 Airy 完美、forward 仍扰动 | 11.7% / 69.9% | 63.3% / 39.4% | 6.8% / 89.2% | 0.1% / 99.9% | **32.1% / 59.9%** |

相对七路扰动，完美恢复六路 Airy 带来总召回 `+5.3pp`、总误检 `-5.3pp`；但距离干净
基线仍差 `-5.5pp / +12.0pp`。按总损失粗略计算，只恢复了约 49% 的召回损失和约 31%
的误检恶化。由于融合、聚类和分类是非线性的，各类别甚至出现非单调变化（Block
召回降至 0.1%），不能把这些比例解释为各雷达独立贡献；但它已经可靠否定了
“只校正 Airy 就能恢复到约 35%”的乐观假设。

**裁决：**

1. Airy 配对图仍值得做，但单独实施的端到端收益天花板不足；
2. forward 的残余误差会持续污染融合点云，不能仅保留扰动外参并接受损失；
3. forward 水面平面约束必须和 Airy 配对图处于同一优先级。下一项最低成本实验应先模拟
   “六路 Airy 完美 + forward 的 z/roll/pitch 完美恢复，x/y/yaw 仍扰动”，测出水面平面
   方案的理论上界，再决定是否需要投入共同目标约束；
4. 当前实验开关默认关闭，不改变正式运行行为；本结果仅用于路线选择。

### forward 部分自由度 A/B：水面可观测量不是主要恢复杠杆

继续固定 seed 0、六路 Airy 完美恢复、在线标定不介入，测试：

- 配置 A（plane）：forward 的 `z/roll/pitch` 恢复真值，只保留
  `dx=-0.2777m, dy=-0.1135m, dyaw=+8.047°`；
- 配置 B（pitch）：forward 只恢复 pitch，仍保留
  `dx=-0.2777m, dy=-0.1135m, dz=+0.4026m, droll=-1.001°, dyaw=+8.047°`。

| 状态 | Boat 召回/误检 | Buoy 召回/误检 | Pillar 召回/误检 | Block 召回/误检 | 四类总召回/误检 |
|---|---:|---:|---:|---:|---:|
| forward 全扰动 | 11.7% / 69.9% | 63.3% / 39.4% | 6.8% / 89.2% | 0.1% / 99.9% | **32.1% / 59.9%** |
| A：恢复 z/roll/pitch | 11.9% / 69.7% | 63.2% / 39.5% | 6.7% / 89.8% | 0.2% / 99.7% | **32.1% / 60.2%** |
| B：只恢复 pitch | 11.8% / 73.0% | 62.1% / 38.7% | 6.6% / 90.0% | 0.3% / 98.4% | **31.6% / 58.7%** |

配置 A 没有让 Block 回升，总召回也没有超过 forward 全扰动状态；A 相对 B 仅多 0.5pp
召回，误检反而高 1.5pp。由此撤回“Block 归零主要由 forward 的 z/pitch/roll 错层导致”
这一假说。即使把水面平面理论上能约束的三个自由度直接恢复到真值，端到端结果仍不恢复。

更可能的主因是剩余 yaw：seed 0 的 `+8.047°` 在 100m 处会造成约 14m 横向错位，远大于
2m 匹配门限。由于 forward 与 Airy 几乎没有共同覆盖，错误的远距点形成独立簇，而不是
简单地把同一簇在 z 方向撑高。x/y 也有贡献，但其量级远小于远距 yaw 放大。

因此水面约束可以作为部分物理校验，但不应作为当前第一实现优先级；它无法处理实测最关键的
x/y/yaw，尤其是 yaw。

### 误检簇来源：forward 主导信号高度可分

为避免用推测决定下游策略，给融合点云增加了只读来源标签。来源编号与点坐标平行传播，
体素化时累计原始点数，分类不读取该标签，因此不改变检测结果。对每个输出簇记录
`forward_pts/total_pts`，再按 v2 评估器的一对一匹配区分 TP/FP。

配置 A 的约 90% 输出检测可以和簇诊断精确回连：

| 类别 | TP 中 forward_ratio≥0.5 | FP 中 forward_ratio≥0.5 |
|---|---:|---:|
| Boat | 5.6%（41/735） | **98.2%（1678/1708）** |
| Buoy | 5.8%（956/16403） | **41.8%（4468/10695）** |
| Pillar | 6.9%（102/1485） | **81.7%（10725/13123）** |
| Block | 100%（7/7） | 99.3%（2721/2740） |
| **四类合计** | **5.9%（1106/18630）** | **69.3%（19592/28266）** |

配置 B 复现同一结构：总体 TP 为 6.4%，FP 为 67.6%；Boat FP 98.5%、Pillar FP 82.0%、
Block FP 97.6% 为 forward 主导。比率大多直接落在 0 或 1，和重叠矩阵揭示的“forward
与 Airy 几乎各自独立观测”一致。

在不改变聚类、只在分类后拒绝 `forward_ratio>=0.5` 输出的离线估算中：

| 配置 | 过滤前总召回/误检 | 过滤后预测总召回/误检 | 变化 |
|---|---:|---:|---:|
| A：恢复 z/roll/pitch | 32.1% / 60.2% | **30.4% / 37.6%** | `-1.7pp / -22.6pp` |
| B：只恢复 pitch | 31.6% / 58.7% | **29.8% / 37.4%** | `-1.8pp / -21.3pp` |

未能回连的约 10% 检测在估算中全部保留，因此误检收益是保守的。该规则不是无代价 Pareto
改进，但真假交换比非常高；尤其在“forward 外参不可信”的降级状态下，比继续只优化水面
自由度更有价值。

Block 是例外：当前仅有的少量 Block TP 也几乎全部依赖 forward，直接过滤会把其召回从
约 0.2% 降到 0。由于它本来已经近乎失效，这不会改变当前总账，但说明正式策略不能把
“forward-only 永久拒绝”写死。

**新的优先级：**

1. 先在干净名义外参上复测 TP/FP 的 forward 来源分布，量化防御规则在正常状态的代价；
2. 若干净状态下 forward-only 包含大量有效远距目标，则把防御设计成由 forward 标定可信度
   控制的降级模式，而不是永久过滤；
3. Airy 配对图继续实现，用于恢复近距六路的一致性；
4. forward 水面平面约束降为辅助校验；下一项几何上更关键的上界实验是“只恢复 yaw”，
   确认远距角度误差是否为主导；
5. 共同目标约束只有在 yaw 上界验证后仍有足够收益时再投入。

### 干净外参来源对照：forward-only 不能默认拒绝

使用名义外参、关闭在线标定和所有时序增强，仅开启来源诊断重新完整回放。基线稳定复现：

- 777 个检测 stamp、689 个有效帧；
- 四类总召回 37.6%、总误检 47.9%；
- Block 召回 15.4%、误检 83.3%。

约 88%–90% 的输出检测能回连到来源簇。干净外参下：

| 类别 | TP 中 forward_ratio≥0.5 | FP 中 forward_ratio≥0.5 |
|---|---:|---:|
| Boat | 30.6%（292/954） | 96.3%（762/791） |
| Buoy | 22.4%（3914/17499） | 30.0%（1774/5910） |
| Pillar | 56.0%（1405/2510） | 85.7%（8788/10259） |
| Block | **100%（576/576）** | 99.6%（2776/2788） |
| **四类合计** | **28.7%（6187/21539）** | **71.4%（14100/19748）** |

这和扰动状态的总体 TP 5.9% 明显不同：外参正确时，forward 不仅制造误检，也承担大量真实
远距检测。若在干净状态下直接拒绝 `forward_ratio>=0.5`：

- 总召回预计从 37.6% 降到 **28.0%**（-9.6pp）；
- 总误检从 47.9% 降到 **31.4%**（-16.5pp）；
- Block 召回从 15.4% 降到约 1.5%。

因此“forward 主导簇拒绝”不能作为默认行为。它只能是**明确知道 forward 外参不可信时的
降级模式**，或改造成更细的时序/几何置信策略；否则会把正常工作的远距通道一起关闭。

### Block 真值距离分布：全部属于 forward 主导的远距任务

对 232 个 odom 可覆盖的 Block GT 帧进行船体系距离统计，共 1362 个目标观测，每帧中位数
为 6 个 Block：

| 距离桶 | 观测数 | 占比 |
|---|---:|---:|
| 0–20m | 0 | **0.0%** |
| 20–40m | 981 | **72.0%** |
| 40–60m | 381 | **28.0%** |
| 60m 以上 | 0 | 0.0% |

总体距离 `min=24.43m, p25=27.54m, median=28.78m, p75=41.35m, max=47.06m`。
六个 GT 序号的典型中位距离约为 24.69m、27.54m、28.05m、38.13m、41.35m、46.83m。

这解释了此前的表面矛盾：本数据集里根本没有近距 Block，六路 Airy 对 Block 缺乏贡献符合
量程/俯视几何；Block TP 100% forward 主导不是诊断错误，也不是近距 Airy 被水面过滤切掉。

**路线进一步收敛：**

1. 永久/默认 forward-only 拒绝被否决；
2. Block 不能套用通用 forward 降级过滤，它的有效检测能力天然依赖 forward；
3. 来源比例仍可作为“forward 不可信”状态的诊断量，但是否进入降级状态必须由独立标定
   可信度、跨帧稳定性或几何一致性触发，不能由来源比例自身循环判定；
4. 下一项高信息量实验改为“六路 Airy 完美、forward 只恢复 yaw”，直接验证远距横向角度
   误差是否主导；
5. 随后在七路全扰动状态下复测来源拒绝，量化尚未完成 Airy 配对标定时的实际收益折扣。

### forward 仅恢复 yaw：主导自由度已锁定

固定六路 Airy 为真值、forward 保留 seed 0 的
`dx=-0.2777m, dy=-0.1135m, dz=+0.4026m, droll=-1.001°, dpitch=+2.261°`，
只把 `dyaw=+8.047°` 恢复到真值；在线标定不介入。

| 状态 | Block 召回/误检 | 四类总召回/误检 |
|---|---:|---:|
| forward 全扰动 | 0.1% / 99.9% | 32.1% / 59.9% |
| 恢复 z/roll/pitch | 0.2% / 99.7% | 32.1% / 60.2% |
| **只恢复 yaw** | **9.8% / 80.7%** | **37.1% / 46.4%** |
| 干净名义外参 | 15.4% / 83.3% | 37.6% / 47.9% |

仅恢复 yaw 带来总召回 `+5.0pp`、总误检 `-13.5pp`，恢复了 forward 全扰动相对干净基线
约 91% 的召回缺口；总误检甚至比本次干净回放低 1.5pp。Boat/Buoy/Pillar 的命中数也分别
恢复到 1059/19801/2757，接近干净状态的 1090/19814/2806。

因此 seed 0 下 forward 的关键误差自由度可以锁定为 yaw。此前水面自由度全恢复却无收益，
不是运行噪声，而是因为 z/roll/pitch 并非主要矛盾。下一步不应继续投入通用 6DoF ICP，
而应研究共同目标上的 yaw 单自由度约束。

### 七路全扰动下的来源降级收益

重新运行七路 seed 0 全扰动、在线标定关闭、来源诊断开启：

- 基础结果为 26.8% 总召回、65.3% 总误检；
- 可回连 TP 中 8.2% 为 forward 主导；
- 可回连 FP 中 63.1% 为 forward 主导；
- 分类后拒绝 `forward_ratio>=0.5` 的保守估算为 **24.8% / 46.8%**。

即真实全扰动状态下仍可用 `-2.0pp` 召回换取 `-18.5pp` 误检。收益比 Airy 完美状态下的
`-22.6pp` 有折扣，但没有缩小到只有 5–8pp。降级模式仍有实用价值，不过它只能压制错误
输出，无法恢复已经消失的远距 TP，因此不能代替 yaw 校正和 Airy 配对标定。

### 15–25m 重叠带一致性：可区分 forward yaw 失准

在 5–70s 均匀取 14 个时刻，将六路 Airy 和 forward 独立做有限点过滤、水面以上过滤、
0.2m 体素化与 0.9m 聚类，再对两组簇中心做一对一匹配。该审计只用于判断相对一致性，
不参与检测。

| 外参状态 | Airy/forward 簇数 | 5m 内候选共同簇 | 1m 内严格吻合 | 严格占比 | 每帧严格匹配中位数 | ≥2个严格匹配的帧 |
|---|---:|---:|---:|---:|---:|---:|
| 干净 | 113 / 97 | 56 | **56** | **100%** | 4 | **14/14** |
| Airy 真值、forward 全扰动 | 113 / 105 | 56 | **6** | **10.7%** | 0 | **0/14** |
| Airy 真值、forward 只恢复 yaw | 113 / 118 | 56 | **56** | **100%** | 4 | **14/14** |
| 七路全扰动 | 73 / 105 | 36 | **6** | **16.7%** | 0 | **1/14** |

这个信号与 yaw 因果同步：forward 全扰动时严格一致性从 56/56 跌到 6/56，只恢复 yaw
后立即回到 56/56；即使 Airy 也处于 seed 0 扰动状态，严格匹配仍只有 6 对。因此 20m
重叠带虽然不足以支持 6DoF ICP，却足以成为 forward 相对失准的候选触发信号。

同时，干净和 yaw 恢复状态下每个采样帧中位数有 4 对共同簇，14/14 帧均至少有 2 对；
数据量上支持研究 yaw 单自由度配准。不过当前共同簇尚未按类别确认，不能直接宣称“每帧有
2–3 根共同立柱”。下一步应先统计共同簇中立柱锚点的数量与稳定性，再用 RANSAC/稳健角差
中位数估计 yaw，并在多个随机扰动种子上验证。

**当前最终方向：**

1. Airy 使用重叠矩阵支持的配对图，解决六路近距一致性；
2. forward 不做 6DoF ICP，转为 20m 重叠带共同目标的 yaw 单自由度估计；
3. 重叠带严格匹配率可作为 forward 可信度/降级触发候选，但阈值需多 seed 验证；
4. 水面平面只保留为 z/roll/pitch 辅助校验；
5. 来源拒绝只在 forward 明确失准时作为保底，不作为默认输出规则。

### 5m 配对内容核验：89.3% 是同一批目标，仍需稳健估计

“干净、forward 扰动、yaw 恢复时 5m 配对数都是 56”只证明数量稳定，不证明内容相同。
为核验目标身份：

- Airy 外参在干净与 forward-only 扰动状态下相同，用船体系簇中心建立稳定 Airy ID；
- forward 簇中心分别逆变换回原始传感器坐标，在 0.75m 内建立跨状态原始观测 ID；
- 5m 配对改用匈牙利算法，在最大有效配对数下最小化总距离，消除遍历顺序影响；
- 最后比较 `(Airy ID, forward ID)` 配对集合，而不是只比较数量。

| 对比状态 | 干净参考对 | 候选对 | 可映射候选对 | 内容相同 | 一致率 | 全部配对保持的帧 |
|---|---:|---:|---:|---:|---:|---:|
| forward 全扰动（Airy 真值） | 56 | 56 | 56 | **50** | **89.3%** | 8/14 |
| forward 只恢复 yaw（Airy 真值） | 56 | 56 | 56 | **56** | **100%** | 14/14 |

forward 扰动时共有 6 对换配，分别出现在 5/10/15/25/60/70s；每个异常帧都只错 1 对，
没有形成连续多帧的系统性错配。说明 5m 对应关系的地基大体可靠，但“56 对全部可信”
不成立：

- 不能直接对所有配对做普通最小二乘；
- 约 10.7% 外点需要 RANSAC、稳健角差中位数或时序稳定性剔除；
- yaw 恢复后配对内容 56/56 回归，进一步证明换配由 forward 角度失准引起，而不是 ID
  方法随机漂移。

七路全扰动时 Airy 簇本身也移动，使用 0.1m 干净 Airy ID 门限无法跨状态映射，因此该对比
不用于判断物理目标是否消失。实际流程必须先让 Airy 配对图达到可信状态，再用 Airy 作为
forward yaw 的参考。

**前置检查结论：**共同簇配对可作为 yaw 解算地基，但只能在稳健估计和闭环重算严格吻合度
的条件下使用；“普通最小二乘直接吃全部 5m 配对”被否决。

### ForwardConsistencyMonitor：在线可信度状态机与降级消费链路

将此前离线验证的 15–25m 重叠带信号落成在线模块，但没有重复运行两遍完整
`ObstacleDetector::detect()`。`ForwardConsistencyMonitor` 直接读取已经转换到
`base_link` 的融合点云和逐点传感器来源，只执行：

1. 使用与主检测器相同的量程/船体裁剪和 5% 水面分位数；
2. Airy 六路与 forward 单路分别在 13–27m 软边界内做 0.2m 体素化、
   0.9m 欧氏聚类，最终只保留中心位于 15–25m 的簇；
3. 分别计算 5m 候选最大匹配数和 1m 严格最大匹配数；
4. 用 `strict/candidate` 比例产生 `GOOD/BAD/UNCERTAIN/INSUFFICIENT` 观测。

状态机把“观测”和“锁存状态”分开：

- 启动状态为 `UNKNOWN`，行为上不降级，但日志不冒充已经确认可信；
- 候选对少于 2 时为 `INSUFFICIENT`，冻结状态和连续计数；
- 比例 `<0.5` 为 `BAD`，连续 5 个有效坏帧进入 `DEGRADED`；
- 比例 `>=0.8` 为 `GOOD`，连续 15 个有效好帧进入/恢复 `TRUSTED`；
- 0.5–0.8 为 `UNCERTAIN`，同样冻结，避免阈值附近反复横跳。

同时把来源比例从“诊断打印的临时变量”提升为 `DetectedObstacle` 常驻元数据：
`forward_points`、`forward_ratio` 和 `source_provenance_valid`。该元数据不参与原始聚类和
分类；只有状态锁存为 `DEGRADED` 且降级过滤开关开启时，才拒绝
`forward_ratio>=0.5` 的四类输出和候选流，防止候选时序模块把已拒绝目标重新提升。

为避免未经验证的模块改变当前提交基线，`forward_consistency_enabled` 默认仍为 `false`；
打开 Monitor 后，`forward_degraded_filter_enabled` 默认开启。第一版按计划采用统一一刀切，
因此会牺牲全部 forward 主导 Block；分类别时序保留策略留到下一阶段。

#### 四组完整回放

固定关闭 BoatTracker、候选时序增强和在线标定，使用同一 bag、同一 v2 评估口径完整回放。
第二组首次运行因会话中断只得到 641 个有效帧，超过 5% 帧数差异阈值，已废弃并重跑；
下表采用覆盖 684–689 个有效帧的可比结果。

| 场景 | 状态转换 | 末段观测累计（G/B/U/I） | 总召回/误检 | 参考值 | 判断 |
|---|---|---:|---:|---:|---|
| 无扰动 | 1.586s：`UNKNOWN→TRUSTED` | 748 / 0 / 8 / 0 | **37.5% / 48.0%** | 37.6% / 47.9% | 无误降级，基线不变 |
| Airy 真值、forward 全扰动 | 0.600s：`UNKNOWN→DEGRADED` | 0 / 758 / 2 / 0 | **30.9% / 32.3%** | 离线保守估算 30.4% / 37.6% | 快速降级，在线精确过滤优于旧估算 |
| 七路全扰动 | 0.600s：`UNKNOWN→DEGRADED` | 7 / 536 / 102 / 117 | **25.1% / 42.1%** | 离线保守估算 24.8% / 46.8% | Airy 失准时仍保持降级，无错误恢复 |
| Airy 真值、forward 只恢复 yaw | 1.504s：`UNKNOWN→TRUSTED` | 751 / 0 / 19 / 0 | **37.2% / 46.3%** | 37.1% / 46.4% | 不误触发过滤，恢复结果不变 |

七路全扰动后半段确实出现样本不足和少量偶发 GOOD，但双阈值、连续帧和冻结机制共同阻止了
错误恢复；没有任何一次从 `DEGRADED` 跳回 `TRUSTED`。这验证了“样本不足保持原状态”
不是理论装饰，而是在 Airy 也失准时实际生效。

**适用边界必须明确：**该 Monitor 判断的是 forward 相对于六路 Airy 的一致性，不是两套
外参的绝对正确性。只有 Airy 已由可信名义外参或配对图标定约束时，`TRUSTED` 才能解释为
forward 可信；若 Airy 和 forward 同时失准，两边可能偶然错到一起而产生 GOOD，也可能一起
漂出窄带而变成 INSUFFICIENT。15 帧恢复滞回只能降低这种偶然事件造成错误恢复的概率，
不能从理论上证明两边绝对正确。因此 Airy 配对图标定是未来启用 Monitor 自动恢复和 yaw
热校正的前置条件；在 Airy 未确认可信时，已锁存的 `DEGRADED` 可以保守保持，但不能仅凭
若干 GOOD 宣称外参恢复。

旧离线过滤为什么比在线结果保守：`forward_fp_analyzer.py` 只能把发布结果按位置回连到
簇诊断，覆盖率约 90%；未回连的约 10% 检测必须全部保留。现在来源比例直接附着在输出簇上，
在线过滤覆盖 100% 有效来源元数据，因此 forward-only FP 能被更完整删除。七路全扰动
`42.1%` 误检低于旧估算 `46.8%`，属于链路精确化收益，不是评估口径异常。

运行开销也满足实时性：四组日志中的 Monitor 平均约 `0.44–0.49ms/帧`，
常见峰值 `0.8–1.2ms`；干净组出现一次 4.97ms 离群峰值，仍远低于 100ms LiDAR 周期。
代码已在 WSL 中通过 `colcon build --packages-select usv_perception --symlink-install`，
新增模块没有产生新的编译警告（仅保留 ImageStitcher 原有 signedness warning）。

**当前裁决：**

1. 轻量独立聚类 Monitor 的信号和在线状态机成立，可以保留；
2. `UNKNOWN/TRUSTED/DEGRADED` 链路与消费者已经真正打通，不再是只打印日志；
3. 统一 `forward_ratio>=0.5` 拒绝只适合作为失准保底，虽然显著降误检，但会令当前
   forward 主导的 Block 召回归零，不能作为最终精细策略；
4. 下一阶段优先实现 forward yaw 单自由度稳健估计与“校正后严格吻合度上升”的闭环接受；
5. 在 yaw 热替换之前，先为 Block/远距目标设计降级状态下的时序稳定保留规则，避免保底模式
   永久放弃远距任务；
6. 新模块当前保持默认关闭，等多 seed 外参扰动验证完成后再考虑切换生产默认值。

#### 干净外参强制过滤：永久开启被实测否决

为消除旧来源日志约 90% 回连覆盖率带来的估算误差，在无扰动外参上保持真实在线消费者不变，
仅用测试参数把 Monitor 的 BAD/GOOD 阈值临时设为 1.1/1.2，使严格吻合率 1.0 也在 5 个
有效帧后进入 `DEGRADED`。这不是生产配置，只用于测量“外参正确时仍永久拒绝
`forward_ratio>=0.5`”的真实代价。

| 状态 | 四类总召回/误检 | Boat | Buoy | Pillar | Block |
|---|---:|---:|---:|---:|---:|
| 干净、正常 TRUSTED | 37.5% / 48.0% | 15.6% / 45.1% | 68.4% / 25.2% | 11.3% / 80.7% | 15.4% / 83.3% |
| 干净、强制 DEGRADED 过滤 | **27.0% / 26.7%** | 11.1% / 4.4% | 53.3% / 23.2% | 5.2% / 56.5% | **0.6% / 38.5%** |
| 变化 | **-10.5pp / -21.3pp** | -4.5pp / -40.7pp | -15.1pp / -2.0pp | -6.1pp / -24.2pp | **-14.8pp / -44.8pp** |

在线实测比旧离线估算的 28.0%/31.4% 删除得更彻底，但并没有让召回代价变小：召回实际
下降 10.5pp，略大于旧估算的 9.6pp。无扰动 forward 主导簇确实含有大量 FP，永久过滤
可以把误检压到 26.7%，但同时删除了不可替代的远距 TP，特别是几乎清空 Block。

**新裁决：**

1. `forward_ratio>=0.5` 永久/default 过滤正式被在线实测否决，只能由独立失准状态触发；
2. 评分如何加权都不应只看总误检率下降，因为四类召回损失高度不均，Block 承受了近乎全部
   检测能力被关闭的代价；
3. 下一实现优先级调整为：**Block 降级多帧稳定保留 → Airy 配对图标定 →
   forward yaw 单自由度估计**；
4. Block 第一版应只在 `DEGRADED` 且 `forward_ratio>=0.5` 时进入候选轨迹，连续 N 帧在
   同一位置稳定出现后才恢复输出；偶发噪声仍由时序门抑制；
5. 不能直接复用依赖 odom 的旧 CandidateTemporal 正式路径。比赛没有 odom，Block 关联必须
   使用 `base_link`，并以前面已验证的本船平移/yaw 稳定性作为适用前提。

#### Block 候选邻域诊断：多帧保留不能修复系统性 yaw 错位

在实现 Block 降级时序保留前，先对“干净外参、仅 forward 全扰动、仅恢复
forward yaw”三组完整日志做候选层诊断。分析对象不是最终 Block 输出，而是
`forward_ratio>=0.5` 的全部聚类后候选，包括被形状或低置信度规则淘汰的簇；
因此可以区分“没有形成簇”和“形成了簇但位置错误”。

| 状态 | Block 真值观测 | 8m 内有 forward 主导簇 | 5m 内 | 2m 内 | 匹配偏移中位数 | 角偏差中位数 | yaw 扫描峰值 | 峰值下 2m 匹配 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 干净 | 4560 | 94.8% | 88.9% | **83.4%** | 0.69m | +0.12° | +2.1° | 3982/4560 |
| forward 全扰动 | 4146 | **91.1%** | **74.7%** | **4.3%** | **3.21m** | **+6.09°** | **+8.6°** | **3725/4146** |
| 仅恢复 forward yaw | 4566 | 94.9% | 89.0% | **83.7%** | 0.96m | -0.10° | +1.6° | 3976/4566 |

forward 全扰动时，Block 附近并非没有候选：91.1% 的真值在 8m 内仍能找到
forward 主导簇，且这些匹配簇的 `forward_ratio` 中位数为 1.00；其中 2695 个
仍被分类为 Block，另有 709 个落入 `discarded_shape_implausible`。真正崩溃的是
位置：2m 评估容差内覆盖率从干净的 83.4% 降到 4.3%，偏移中位数为 3.21m，
切向偏移中位数 2.82m，而径向偏移中位数只有 -0.91m，符合旋转误差主导的形态。

对每帧 Block 真值绕船体原点做 `[-12°, +12°]` 扫描，再与 forward 主导候选做
2m 一对一最大匹配：失准组在 **+8.6°** 达峰，匹配数由零旋转时的
179/4146 跃升到 **3725/4146**。该峰值与 seed 0 的 forward yaw 扰动
`+8.047°` 同量级；仅恢复 yaw 后，原始 2m 覆盖率立即回到 83.7%。干净与
yaw 恢复组出现约 +1.6°～+2.1° 的扫描次峰，是在大量邻近非 Block 候选中以
“最大匹配数优先”产生的残余偏置，不能解释失准组独有的 +8.6° 峰及 20 倍
以上匹配跃升。

**裁决更新：**

1. “forward 失准时 Block 簇根本没有形成”被否决；候选大量存在，但系统性旋转错位。
2. 在 yaw 未修复时做多帧稳定保留，会保留“连续多帧稳定地出现在错误位置”的簇，
   主要增加误检，无法把它们送回 2m 真值容差；Block 时序策略暂缓。
3. 实现优先级改为：**Airy 配对图标定 → forward yaw 单自由度稳健估计与闭环验收
   → 再评估 Block 是否还需要类别专用时序策略**。
4. 本次用 Block 真值扫描只用于离线病因验证，正式测试没有真值，不能把该扫描作为
   在线 yaw 解算方法；在线仍应使用 15–25m Airy/forward 共同簇，并以校正后严格
   吻合度上升作为自验证条件。

### 用 Airy 簇代替真值做 yaw 扫描：方法可迁移，但依赖 Airy 本身可信

上一节的 yaw 扫描用 `gt_block` 当参照，比赛没有真值不能直接用。新增
`yaw_scan_airy_reference.py`，把参照换成同一份`cluster`日志里自身的
`forward_ratio<=0.1`（Airy主导）簇，扫描对象换成`forward_ratio>=0.5`
（forward主导）簇，两者都已经在`base_link`系，不需要 odom/世界系转换。

**场景一（干净外参）**：`clean_forward_support`日志，777个双边都有候选的帧，
scan无清晰峰值（best=-2.3°，仅352/28940≈1.2%匹配，和0°的306/28940几乎
没区别）。

**场景二（Airy真值、forward只恢复yaw）**：`forward_monitor_full_yaw_fixed`
日志，同样无清晰峰值（best=-0.1°，1139/29033≈3.9%，就是0°本身）。

**场景三（Airy真值、forward全扰动）**：两份独立日志
（`forward_monitor_full_forward_bad`及其repeat）：

| 日志 | best | matches | at 0° |
|---|---:|---:|---:|
| forward_bad | -7.4° | 9360/26580（35.2%） | 2976/26580（11.2%） |
| forward_bad_repeat | -7.5° | 10154/28851（35.2%） | 3173/28851（11.0%） |

两次独立复现只差0.1°，且都逼近真实扰动`+8.047°`需要的反向修正量
`-8.047°`（差0.5-0.6°）。**结论：用 Airy 簇代替真值的 yaw 扫描方法本身
成立——对齐时不给出假峰值，失准时给出清晰、可复现、精度足够的信号。**
"干净和yaw恢复时都没有峰值"不是方法失效，反而是期望行为：两者对齐时，
同一物体的 Airy/forward 观测会在聚类阶段直接融合进同一个簇（forward_ratio
落在0.1-0.5的中间地带），不会分裂成一对可供扫描匹配的独立簇；只有存在
错位时，两路观测才会各自形成独立簇，扫描才有信号可寻。

**场景四（七路全扰动，Airy自己也不准）**：`seed0_all_perturbed_forward_support`
日志，776帧。先用±12°扫描，峰值顶在边界（best=-12.0°），说明真实峰值在
扫描范围之外；扩大到±30°后峰值落在范围内部（best=**-14.3°**，
matches=7840/29170≈26.9%，远高于0°的1315/29170≈4.5%，不是噪声）。

**但 -14.3° 和 forward 自身真实扰动 `+8.047°`（需要 -8.047° 修正）不吻合
——多出的约6°是六路 Airy 自身扰动叠加进参照系统之后的复合误差，不是
forward 的真实误差。** 这证实了扫描测的是"forward 相对于（可能也不准的）
Airy 参照系"的相对偏差，不是 forward 外参的绝对真值；Airy 自己失准时，
即使信号依然清晰可辨（不是噪声），解出来的角度也不能当成 forward 的真实
yaw 误差来热修正。

**回答本节最初提出的两个问题：**

1. "用 Airy 簇代替真值的方法能不能迁移" —— **能**，场景一/二/三的结果确认
   方法在 Airy 可信时有效、精度足够、且对齐时不误报。
2. "forward yaw 能否独立于 Airy 标定先做" —— **不能**。场景四证明 Airy
   自身失准时，扫描给出的角度是被 Airy 误差污染的复合量，直接拿去修正
   forward 会引入新的系统误差，而不是恢复真值。**优先级不变，维持前一节
   "Airy 配对图标定 → forward yaw 单自由度估计"的顺序，forward yaw 不能
   提前单独实施。**

### 补充验证：六路 Airy 的扰动本身不是单一系统性旋转，peak 依然被污染6°+

针对"如果 Airy 的误差不是系统性旋转、峰值可能依然准确"这个假说，直接核对
seed 0 六路 Airy 的真实 yaw 扰动值（`TestExtrinsicPerturbation`日志）：

| 传感器 | yaw扰动 |
|---|---:|
| front | -2.312° |
| right | -0.400° |
| right2 | +9.143° |
| back | +3.578° |
| left2 | -0.528° |
| left | -7.007° |

六路符号混杂、量级不一，均值仅约+0.41°——已经是"非单一系统性旋转"的
误差profile，正是该假说要验证的情形。**但场景四实测peak仍是-14.3°，
和forward真实-8.047°差6.25°（30m处约3.3m横向误差，超出2m容差）**，
不是"轻微偏差"，是实打实超出容差的错误结果。

原因是 forward 与 Airy 的真实重叠集中在 front/left2/right2 三路窄带
（重叠矩阵：3.5%/1.1%/0.6%），scan 的 peak 由这几路各自的（同时含平移+
旋转的完整6DoF）扰动共同决定，不是六路的简单平均，front+left2各自的yaw
扰动（-2.312°、-0.528°）本身也解释不了6.25°的偏差量，说明这是多个自由
度耦合后的复合效应，比单纯"参照系整体转了几度"更复杂。

**更值得注意的是**：这个失败模式不是"信号变弱/变模糊"——峰值依然清晰
（26.9%匹配率，远高于噪声基线），和场景三(Airy可信时)看起来同样"自信"。
也就是说，**这种情况下没有廉价的在线自检手段能分辨"这个峰值可信"还是
"这个峰值被污染"，除非已经独立知道 Airy 是准的**——这比"结果单纯依赖
Airy"更麻烦，是该假说被否决后浮现的一个新风险点，值得在设计 Airy 配对图
的接受条件时一并考虑（比如闭环后再用同一 scan 复算严格吻合度，而不是只
看有没有清晰peak）。

**结论**：假说被证伪，优先级维持不变。

### 设计原则：闭环复算优先于求解过程自评价

"峰值清晰但结果错误"（-14.3° vs 真实-8.047°，26.9%匹配率和Airy可信组同样
"自信"）暴露了一个比"依赖Airy"更危险的风险：**没有任何在线可观测量能
区分"峰值可信"和"峰值被污染"**。这比标定失败更糟——失败还能退回名义
外参（现有安全闸的做法），"成功但成功到错的地方"会主动把系统推向更坏
状态且不报警。

**由此确立的设计约束（后续Airy配对图与forward yaw校正均适用）：不相信
求解过程本身的自评价（残差小、峰值清晰、对应点比例高），只相信独立于
求解过程的结果验收指标。**

forward yaw热替换的接受条件（全部满足才生效）：

1. 扫描峰值存在且明显（必要不充分）；
2. **校正后，15-25m重叠带1m严格吻合率必须显著回升**（失准时10.7%，
   正确修正后应接近干净基线水平，建议硬阈值>80%）——这是独立于扫描
   本身的验收指标，是本条设计原则的核心；
3. 校正量必须在物理合理范围内（±10°，任务书扰动上限）；
4. 多帧一致性：连续N帧独立解算的yaw标准差需小于阈值（如1°），避免
   采信不稳定解。

Airy配对图每条边的接受条件同样不能只看残差下降和对应点比例，需加一条
独立指标：**校正后两路点云在重叠区的实际重合度必须提升**。链式结构里
`back`若能通过right链和left链两条路径到达，两条路径解出的外参应一致，
这是免费的交叉验证，应利用（需先确认`back-left`重叠是否足够，此前
矩阵里该对未被列为强连接）。

### 优先级重新定价：Airy配对图的真实权重是10.3pp，不是5.3pp

forward外参的完整可行性链条：6DoF ICP不可行（重叠3.5%）→ 水面约束无效
（z/roll/pitch非主要矛盾）→ yaw扫描+真值可行但比测无真值 → yaw扫描+Airy
参照依赖Airy准确且失败时不可自检。**forward yaw这条路的可行性完全押在
Airy配对图上**，Airy配对图做不成，forward只能退回名义外参+Monitor降级
保底。

因此Airy配对图不只值它自己的+5.3pp（26.8%→32.1%），它还是forward yaw
那+5.0pp（32.1%→37.1%）的前置条件——**实际权重是两者相加的10.3pp，是
当前最高价值的单项工作**，优先级判断在此基础上进一步加强。

### Airy配对图落地前置调查：front-right2稠密ICP不可行，地标法可行但揭示架构问题

在写正式配对图求解器前，先在front-right2这条边（重叠矩阵评级"中等"9.7%/82点）
做了四轮验证，全部离线、复用bag原始点云，不涉及C++改动。

**稠密点云ICP两版原型均未收敛**：给front施加seed 0扰动、right2固定名义外参
（root），用点到点ICP（粗到细gate schedule + yaw预扫两种方案）尝试恢复
已知的seed0扰动逆变换，两版结果都和真值对不上（真值需要dxyz≈(0.11,-0.34,0.16)
drpy≈(-2.96,2.64,-9.14)°，ICP两版分别解出(1.49,-0.41,0.14)/(2.54,-1.04,2.58)°
和(1.34,0.69,0.11)/(2.66,-1.10,-8.64)°），0.3m重合率全程停留在7.8%-10.8%，
远未回到应有水平。**"中等重叠"在这个场景下也撑不住通用稠密ICP**，和forward
（3.5%重叠、"弱"）的失败是同一机制：水面场景弱结构方向病态（面内平移/绕法向
旋转对平面近似无约束），稠密ICP会把误差错误分配到各自由度。

**改用离散地标（候选簇）匹配，全程census验证：**

1. 全bag（768同步帧）统计front-right2共视地标：2350对匹配，中位数偏移
   0.077m，看起来数据量很大；
2. **但去重后只有约4个不同物理目标**，其中1个（占2350对里的1219对，52%）
   经单帧检查证实是eps阈值把多个相邻buoy揉成了一个假目标（该目标在444/571
   帧里同时有2-5个front簇成员），必须剔除；
3. 剩余3个干净目标（单帧内均为单一成员，跨帧的位置分散是本船移动/转向导致
   的正常轨迹，不是合并瑕疵）用于约束强度检查：奇异值(yaw,tx,ty)=
   (44.6,1.73,0.55)，条件数81.2——**yaw方向约束强，平移方向弱**（3个点
   全部集中在23-27m距离带、约42°角扇区内），3点解2D三参数已经很勉强，
   谈不上稳健；
4. **对census里最干净的那个目标（578次单帧单一观测的立柱）做多路可见性
   检查：front/right/right2三路雷达同时看到它（100%/100%/100%），
   back/left2/left三路看不到（0%/0%/0%）**——符合几何直觉（前三路
   共享右前方视野扇区）。首版检查曾错误显示"全部为0"，定位为流式
   同步bug（消息到达顺序导致其余5路的缓存值经常过期），改为离线全量
   缓存+最近邻匹配后修复，front/right/right2三路复现为100%一致，结果
   可信。

**架构结论**：front-right2单边约束太弱（条件数81、只有3个可信点），不足以
独立解出稳健外参。但同一根立柱被front/right/right2三路同时观测到，说明
真实的连接性不是"重叠矩阵定义的两两配对链"，而是**以场景内公共地标为
锚点的多路联合观测**——front/right/right2很可能构成一个能被同一批地标
同时约束的观测簇（对称地，left侧的front/left2/left大概率也是一个簇，
仍需验证）。**下一步不应实现原计划的链式两两配对图，应先验证左侧簇是否
成立，再设计"公共地标联合解算"（多路对同一批地标做联合最小二乘，而不是
front→right2→right顺序传递）替代链式方案。**

### 完整雷达-地标二部图：真实拓扑是闭合环，不是两条链，back不是孤儿

对全部六路Airy各自独立做帧间最近邻track（≥15帧才保留，过滤瞬时噪声），
再对所有跨雷达track pair算共同帧匹配率（0.3m门限、共同帧≥8、匹配率≥0.6
才判定为同一物理目标），union-find合并，得到47个被≥2路看到的目标、125条
跨雷达链接。首版"流式同步"重复了之前的bug教训，这次直接用离线全量缓存+
最近邻时间戳查询，同一方法论。

按雷达对汇总边权重（共享目标数）：

| 雷达对 | 共享目标数 |
|---|---:|
| front–right2 | 12 |
| right2–right | 13 |
| right–back | 6 |
| back–left | 8 |
| left–left2 | 10 |
| left2–front | 5 |
| （弱连接，不用）front-right/front-left/right2-left2/back-left2 | 3/1/2/1 |

**这六条边闭合成一个环：front→right2→right→back→left→left2→front，绕船
一圈，相邻雷达都有5-13个共同地标。** 且census抓到一个被back/left2/left/
right四路同时观测到的目标（-10.17,-0.02），本身就横跨环的大半圈。

**back_lidar不是孤儿**——原计划里back排在链尾、被认为最难连，实测它和
right有6个共同目标、和left有8个，比front-left2(5)/front-right2弱连接的
子集还要扎实。

**架构意义**：闭合环相对链式结构的关键优势是提供了免费的全局一致性检验
——从front出发，顺时针（right2→right→back）和逆时针（left2→left→back）
两条独立路径都能到达back，两条路径解出的相对位姿必须一致，这是链式结构
（root单向传播、末端无法交叉验证）提供不了的。

**但闭环本身还没做数值验证**：沿环一周的累积旋转/平移残差是否接近零，
还未检验，这是下一步——如果残差大，说明某段边的地标关联仍有问题（比如
再次踩到eps合并陷阱），需要先修那一段，不能直接信任整个环去做bundle
adjustment式的联合解算。

### 闭环一致性检验：四项指标分开验证，环状拓扑成立（含一次自我纠错）

**第一版检验结果严重异常**（旋转闭环+23.68°、平移闭环4.26m、各边RMS
1.6-5.2m），定位到脚本bug：`track_mean()`对每条track全程所有帧的位置
取平均，而立柱在body系里的位置本来就随本船移动/转向漂移，两条track各自
独立取全程均值再配对，丢失了瞬时时间对齐，均值点根本不对应同一个真实
瞬间。改为对每对匹配track的**所有共同帧分别取瞬时对应点**（而不是先各自
求全程均值），重新解算：

| 环上边 | 瞬时对应点数 | RMS残差 | max残差 |
|---|---:|---:|---:|
| front→right2 | 1686 | 0.354m | 2.644m |
| right2→right | 4010 | 0.237m | 2.133m |
| right→back | 504 | 0.440m | 2.683m |
| back→left | 1032 | 0.291m | 1.983m |
| left→left2 | 2407 | 0.198m | 1.980m |
| left2→front | 1693 | 0.382m | 1.955m |

原本按目标数看最弱的right-back（6目标）、left2-front（5目标），展开成
瞬时对应点后各有504、1693个约束点，RMS并非全场最差——验证了"瞬时对应点
数量取决于共视目标被观测的帧数，不是目标个数"，边权重弱不等于约束点少。

**旋转闭环残差：+0.872°**（应~0°）——六条边绕一圈累积误差不到1度，直接
支持"yaw方向的解是真实一致的，不是抵消假象"这个最关心的问题。

**平移闭环残差：0.163m**（应~0m）——平移本来约束就弱（此前front-right2
单边条件数81），16厘米在容忍范围内。

**多路共视地标收敛性**（独立于闭环残差、不受抵消掩盖的直接验证）：3路
目标median spread多为0.07-0.19m；那根被back/left2/left/right四路同时
看到、横跨大半圈的地标，median spread **0.101m**（max 1.27m）。

**瑕疵记录**：每条边的max残差普遍在2-2.7m，RMS却只有0.2-0.4m，说明瞬时
对应点集合里混入少量outlier（大概率是偶发跨帧关联错误或track切换瞬间
噪声）。正式实现须用稳健估计（IRLS或RANSAC）自动降权这批点，不能用普通
最小二乘。

**结论**：环状拓扑通过四项独立指标验证（不是靠单一合成数字），架构成立。
下一步可以着手设计公共地标联合解算（bundle adjustment式，雷达外参和
地标世界坐标一起作为未知数），六条边各自的瞬时对应点集合就是现成的
初始约束数据。

### 联合BA原型：干净数据验证通过，扰动数据下发现"精细匹配管线不能自举"

**第一版BA建模有个致命bug**：把"地标在front系里的位置"当成全程共享的
单一未知量，重犯了`track_mean()`那个错误——地标静止在世界里，但front
自己的body frame随船移动，同一根地标在front frame里的位置全程本来就在
漂移。初版结果直接露馅（初始RMS 3.06m，和分边独立计算的0.15-0.19m对不上，
解出的平移动辄1-3米，远超±0.5m物理边界）。

**改正**：不引入地标位置这个未知量，用"同一瞬间两路观测转换回front系后
应该重合"直接消去它——只剩15个雷达外参未知量（5路×yaw/tx/ty），残差为
`T_A^-1(pos_A) - T_B^-1(pos_B)`。这样能把全部125条链接（不只环上6条）
一起用，因为它们共享同一组全局雷达参数。用scipy `least_squares(loss=
'huber')`做鲁棒估计。

**干净数据验证**：初始RMS 0.2325m（和早前分边计算的0.152m/0.194m量级一致，
互相印证），解算后0.2056m，收敛4次迭代。全部5路修正量都在±10°/±0.5m
物理边界内（最大平移0.283m、最大旋转0.4°）。**留出的4个从未参与解算的
地标组（含4路共视的那根），用解出的外参重新检验多路收敛性，median
spread从原本0.07-0.19m进一步改善到0.026-0.068m**——干净场景下解算器行为
正确。

**扰动数据验证（seed 0，5路施加真实扰动，front保持不扰动作为root）——
暴露了架构的真实边界**：

1. 跨雷达匹配（0.3m门限）图几乎完全断裂：链接数从125条骤降到**5条**，
   全部集中在back-left这一对之间；`front-right2`、`front-left2`、
   `right2-right`全部消失（right2的9.143°yaw扰动，在20-27m外造成
   3-4m位移，远超0.3m门限）；
2. `right2_lidar`、`left2_lidar`因完全无约束，解算结果精确停在初值
   0.000°（诚实反映"无数据"，不是错误）；
3. **但唯一幸存的back-left边，解出来的也是错的**：`back_lidar`解出
   dyaw=+11.05°（真值+3.58°，误差+7.47°）；`left_lidar`解出dyaw=+11.82°
   （真值-7.01°，误差达**+18.82°**）——不是"没约束"，是"有约束、但约束
   本身在这个扰动量级下已经混入了错误关联"，和这条线反复出现的"看起来
   有依据、实际是错的"是同一个模式，只是这次表现为"边存在、但边在撒谎"。

**结论**：当前"瞬时对应点匹配+BA"管线是**精修工具，不是自举工具**——它
要求初始外参已经足够接近真值（匹配门限0.3m能覆盖真实误差），扩大门限
虽然能保住更多边，但会引入更多错误关联，不能简单调大了事。真正的比赛级
扰动（±10°/±0.5m）超出了它的工作范围。

**架构含义**：需要两阶段——先用本节前面已验证过的**宽范围扫描法**（用
Airy簇代替真值的yaw scan，此前在front-right2上验证过对齐时不误报、失准
时给出精度足够的信号）做粗对齐，把误差压到0.3m门限的可行域内，再用这套
瞬时匹配+BA做精修。**粗对齐阶段必须先做，不能指望精修管线自己爬出大误差
的坑**——这是本次探索最终定型的裁决，决定了正式实现必须包含粗+精两级，
不能只做其中一级。

### 粗对齐核心假设验证：环闭合能否分辨"清晰但错误"——结果是能

粗对齐阶段计划复用本节前面验证过的"Airy簇代替真值"yaw scan方法。但那个
方法本身在七路全扰动（Airy也失准）场景下已经被证伪过一次：forward单边
scan峰值-14.3°、真值-8.047°，差6.25°超容差，且峰值清晰、无法自检。粗
对齐面对的正是这个场景，直接套用有重蹈覆辙的风险。

**验证实验**：不假设环闭合能救场，直接测。在**完整七路seed 0扰动**（含
front，这次不再当可信root）数据上，对环上6条边各自独立做±15°宽范围yaw
scan（2.0m门限、遍历全程768帧pool matches），得到6个独立峰值：

| 边 | scan峰值 | 真值(相对yaw) | 误差 |
|---|---:|---:|---:|
| front→right2 | +12.0° | +11.46° | +0.54° |
| right2→right | -6.0° | -9.54° | +3.54° |
| right→back | -9.5° | +3.98° | **-13.48°** |
| back→left | -4.5° | -10.58° | +6.08° |
| left→left2 | -4.0° | +6.48° | **-10.48°** |
| left2→front | -3.5° | -1.78° | -1.72° |

**闭环残差：-15.50°**（应~0°，真值本身的闭环严格为0，这是刚体相对位姿
的数学恒等式，与误差大小无关）。

**结论：闭环残差正确报警，不是巧合掩盖。** 六条边里front→right2、
left2→front这两条（重叠较好的边）scan得相当准（误差<2°），而right→back、
left→left2（重叠较弱的边）严重跑偏（误差>10°），这些不均匀的大误差没有
相互抵消成一个看似正常的小闭环残差，闭环残差直接、清晰地暴露了"这组结果
不可信"。**这就是forward单边scan当时缺失的自检手段——环状拓扑把它补上了，
这是本次实验证实的，不是推测。**

**但要明确闭环检验的能力边界**：它是**拒绝**机制，不是**修复**机制——
残差大只能告诉系统"这次不可信，应回退名义外参"，不能反过来指出正确解在
哪。这和已有`ForwardConsistencyMonitor`的`DEGRADED`降级逻辑是同一个安全
模式（检测不可信就保守回退，不做错误恢复），设计上应该复用同一套状态机，
不用另起一套。

**两级架构最终定型**：
1. 粗对齐：六边独立yaw scan + 闭环残差自检（阈值待定，本次-15.5°的失败
   案例和之前干净/yaw恢复场景的noise-level残差可以作为参考区间的两端）；
   闭环残差过大则回退名义外参，不进入下一级；
2. 精修：闭环通过后，用瞬时对应点匹配（0.3m门限）+ IRLS/Huber联合BA
   （已验证：干净数据RMS 0.15-0.23m，held-out验证集收敛至0.03-0.07m）
   做高精度收敛。

这条从"链式两两配对图"→"环状拓扑"→"联合BA"→"粗精两级架构"的调查，
每一步转折都由数据驱动（含两次自我发现并修正的建模bug：`track_mean()`
的跨帧漂移误用、以及重犯该错误的landmark-position-as-unknown BA建模），
到此形成一个自洽、经过多轮证伪检验的最终设计。实现仍需：粗对齐scan的
C++移植、闭环阈值的多seed标定、BA求解器的C++移植与在线触发条件设计。

### 粗对齐有效区间验证：完整流水线扫描发现"架构有根本问题"是误判，真凶是两条边的实现偏置

在投入C++实现前，先验证粗对齐scan的实际工作区间，避免"精修管线拿不到
有效输入、净产出等于什么都没标定"。

**第一轮（有缺陷，已废弃）**：为省时间复用了2D近似（对名义外参下的聚类
中心做事后刚体搬移，而不是重新变换原始点云），scale=1.0（对应seed0全量
扰动）给出loop_residual=-5.50°，但同一场景用完整流水线之前测出的是
-15.50°——两者差10度，近似引入的误差已经大到污染想测量的效应本身，
结果作废。

**第二轮（完整流水线，无近似）**：4档扰动幅度（2°/5°/8°/10°，六路统一
方向、按幅度缩放）+ 3个合成seed（±5°）：

| 配置 | loop_residual |
|---|---:|
| 2° | -14.00° |
| 5° | -17.50° |
| 8° | -18.50° |
| 10° | -17.50° |
| seed201(±5°) | -18.00° |
| seed202(±5°) | -16.00° |
| seed203(±5°) | -12.50° |

残差全部压在-12.5°到-18.5°的窄带里，和扰动幅度基本无关（2°和10°几乎
同量级），当时据此下了"scan方法系统性测不准、架构有根本问题"的结论，
建议放弃粗对齐转向BEV。

**这个结论被推翻**：质疑点是"BA闭环残差只有0.872°，同样六路数据，scan
闭环却有-14°到-18°，量级差一个数量级，更像scan实现有bug，不是数据/
架构问题"。直接验证——在**完全干净（无扰动）**数据上跑同样的六边scan+
闭环：

```
front→right2: peak=-1.0° matches=2513 at_0°=2500   （峰值和0°几乎不可分，正常）
right2→right: peak=+1.5° matches=4826 at_0°=4752   （同上，正常）
right→back:   peak=-5.5° matches=1113 at_0°=973    （即使干净数据，峰值也明显偏离0°）
back→left:    peak=-6.5° matches=1543 at_0°=1292   （同上，明显偏离）
left→left2:   peak=+2.0° matches=3590 at_0°=3562   （轻微）
left2→front:  peak=-1.0° matches=2245 at_0°=2242   （正常）

干净数据loop_residual = -10.50°（不是~0°）
```

**干净数据闭环残差-10.50°，和扰动数据的-14°~-18.5°同量级**，证实存在
一个和扰动幅度基本无关的系统性偏置，不是"scan方法本身测不准"。偏置
集中在`right→back`和`back→left`这两条边——即便完全干净，峰值也明显
偏离0°（-5.5°、-6.5°），且这两条边的匹配点数（1113、1543）远少于
`front-right2`(2513)、`right2-right`(4826)，是环上最弱的两条边（重叠
矩阵里评级也最低）。稀疏样本下大概率存在混叠（比如buoy排布的局部规律性
让错误旋转角度也能凑出一个不小的匹配数峰值）。

**结论更正**："架构有根本问题"的裁决作废。真实情况是：BA精修管线本身
没问题（闭环0.872°），粗对齐scan在多数边上表现正常（4条边干净时峰值
和0°几乎不可分），问题集中在两条稀疏边的关联策略上（简单的max-
cardinality匹配对这两条弱边不够稳健）。下一步应该是**定位并修复这两条
弱边的偏置**（比如改用地标关联+离群点剔除代替简单scan，或对弱边直接用
之前验证过的多路共视地标约束），而不是放弃整个两级架构。

**待办**：弱边偏置定位与修复（`right→back`、`back→left`两条边），修复
后重新做一次幅度扫描确认残差是否恢复到随扰动幅度增长的正常模式。

### Airy六路自标定：最终收尾——七轮验证后触及可辨识性边界，体面暂停而非撞墙

在"弱边偏置"待办之后，又做了三轮更深的排查，最终定位到一个**没有清晰下一步实验的开放问题**，据此决定暂停这条线，转向BEV精度（任务书基础指标第1条，此前从未测过）。收尾前完整记录七轮验证链条，避免以后被误读为"标定失败/白做"，并给未来重启留出入口。

**七轮独立验证链条（每轮都推翻或修正了上一轮的结论，全部基于完整流水线+独立验收，不采信内部残差）：**

1. **scan方法（六边独立yaw scan+闭环）**：幅度扫描（2°/5°/8°/10°）显示闭环残差恒定在-12.5°~-18.5°，一度误判"架构有根本问题"；干净数据复测发现残差-10.5°（不是0°），证实是**集中在right→back、back→left两条稀疏边的固定偏置**，不是方法整体失效，该误判被撤回。
2. **宽门限BA（2.0m门限替代0.3m）**：连接性从5条链接恢复到126条（六路全连通），但精度不足——多数雷达残差3-5°，back方向甚至错误，证明"宽门限单独"不够。
3. **coarse-to-fine迭代（门限2.0→1.0→0.5→0.3m）**：内部RMS单调下降（0.888→0.235m，看起来收敛很好），但**独立验收（4路共视地标）全程卡在1.1-1.4m，从未接近干净基准0.1m**——抓住了"内部残差降、整体是错的"典型陷阱，back还被越修越差（-5.37°→-5.76°），left被拖累恶化（+5.00°→+6.54°）。
4. **五路联合（排除back）**：right支子图（front/right2/right）独立验收从1.4m跃升到**0.045m，接近干净基准**——标定线第一次拿到真正干净的收敛；但left不降反升（+8.22°），**推翻"back是唯一污染源"假说**——污染是通过共享的front参数在联合最小二乘里传染的，不是靠地标关联传染。
5. **左支孤立（仅front/left2/left）**：left2、left的误差和"含back/right"时几乎完全相同（-4.22°、+8.23° vs 之前的-4.54°、+6.54°量级一致），**排除了跨支污染**——left的问题是分支内在的，不是传染来的。但独立验收（front/left2/left地标）收敛到**0.036m**（和right支一样好），与8°+的yaw误差并存——发现**可辨识性(identifiability)矛盾**：三路观测同一地标能重合到3.6cm，不代表每一路自己的外参解对了，存在"误差在雷达间如何分摊"的零空间，重投影残差和独立地标验收都测不出这个零空间内的偏移。
6. **方位跨度假说**：推测"零空间由地标方位集中度决定，标准的路(right)方位跨度应该更大"。**统计验证直接推翻**：right2跨度148.5°、left2跨度157.8°（六路最大）都不小于right的133.4°，但right2/left2恰恰是没收敛的那批，和假说方向相反。
7. **back独占覆盖率统计**（同时修复了一个GT话题绝对纪元/bag相对时间的时钟域bug，这条线上第N次踩到同一类坑）：back独占覆盖（其他5路都看不到）仅**4.2%（665/15938）**的真值目标，即使back完全放弃标定、保留名义外参，损失有界。

**结论**：
- **确定性正面成果**：环状拓扑成立、BA闭环残差0.87°、right支独立验收0.045m（证明这份数据上至少部分雷达可标定）、"独立验收优先于内部残差"方法论被反复验证有效（3次抓住虚假收敛）、back的名义外参损失有界（4.2%）。
- **确定性负面成果**：七种方法/假说穷尽后，核心障碍定位为**可辨识性问题**（不是数据脏、不是关联错、不是方法弱），且方位跨度这个最后的具体机制假说被数据证伪，**没有留下清晰的下一步可测假说**——从"半小时能证伪一个假说"的工程排查，变成了"没有假说的开放研究问题"，性价比不再支持继续投入。
- **当前安全默认值**（名义外参 + `ForwardConsistencyMonitor`风格降级检测）不是"没做标定"，是经过七轮验证后的理性选择。

**未来重启入口（若比测数据条件改变，值得回来接续）**：
1. **离散混叠假说（未证伪，未设计实验）**：可辨识性矛盾也许不是连续零空间，而是buoy/pillar排布的某种周期性结构，导致少数几个离散角度组合"看起来一样"（类似之前scan方法虚假峰值的机制）。需要设计专门检验多峰/周期性的实验，本轮未触及。
2. **多帧本船运动约束（未利用）**：本船在动，理论上不同时刻的观测方位差异能提供额外的可辨识性约束，但需要准确的本船位姿——比赛无odom，需要先有可信的位姿来源才能利用。
3. **前提改变**：若比测目标密度/空间分布好于本bag，可辨识性问题可能自然缓解，届时上述七轮的方法（尤其right支的分支隔离+独立验收框架）可以直接复用。

## BEV精度初探：诊断出视差根因，但确认不需要优先修（任务书原文佐证）

标定线收尾后转向任务书基础指标第1条（BEV精度，此前从未测过）。完整调查链条：

### 坐标链路与首次定性对比

抓取同一时刻的`/world/obstacles/bev_check`帧和`overhead_camera`原始帧并排对比：船体朝向、目标船相对位置、大原木位置基本一致，拼接框架整体work。船体正中央的黑色矩形经代码确认（`self_occlusion_masks_`）是故意的自身遮挡设计，不是bug。

### 定量链路：GT世界系→odom（epoch校正）→base_link→BEV像素

从`ImageStitcher.cpp`读取精确的BEV像素几何（`v=(bev_range_m-bx)/res, u=(bev_range_m-by)/res, res=0.02m/px, bev_range_m=10.0`），建立GT到BEV像素的精确投影。boat目标匹配精度**0.17m**（精确测量，非目测），验证了坐标链路本身正确。

### buoy 15/15完全缺失，非检测方法误差

初版用固定HSV阈值粗筛，13/15未匹配；怀疑是黑色buoy未检测，加入黑色色相检测后仍是13/15缺失。放大两个"匹配"位置直接肉眼确认，发现两次都命中的是附近大原木的边缘曲面，不是buoy——**真实情况是15/15全部缺失，两次"匹配"都是假匹配（被附近大目标的暖色像素骗过）**，比先前判断更彻底、更干净（不需要为"两个例外"找机制）。

### 排除相机拍摄环节：原始鱼眼图里buoy清晰可见

抽查front/right/left2三路原始鱼眼图，buoy（橙色和黑色球体）对比度极好、部分近处目标像素直径达20-40px，排除"相机没拍到/太暗太小"。问题定位在拼接/投影环节。

### 视差机制：精确量级计算+boat判据，排除外参偏差叠加

IPM假设所有目标位于`ground_z_m_=0.0`地平面，buoy实际浮于水面上方约0.2m（GT z一致确认），产生视差误差：

```
offset ≈ d_camera × Δz / (H - Δz)，H=3.0m（相机高度，从buildCameraExtrinsicsFromLidar的lidar_z+0.1确认）
```

对15个buoy精确计算（用各自最近的真实相机位置，非船中心近似）：预测偏移0.27~1.06m，普遍超过buoy自身尺寸（约0.3~0.5m直径），足以解释15/15全部脱靶，且不需要"近处该少丢"这种预期（因为d_camera与GT range不是简单线性关系，取决于具体最近相机的位置）。

**boat判据排除外参偏差叠加**：若存在额外的相机外参系统性偏差，boat这类大目标也应表现出可观测的残留偏移；实测boat精确偏移仅**0.17m**，与"boat吃水线附近部分预测视差≈0"的判据吻合，没有证据支持外参偏差是共同致因。**视差本身即可完整解释现象，机制单一、干净。**

### 反直觉但关键的结论：改`ground_z`治标不治本，在写代码前就被排除

直觉解法"把`ground_z_m_`改成buoy高度0.2m"在数值上不成立，原因：
1. IPM只有一个全局地平面高度，改了buoy准、但水面纹理本身/boat吃水线/原木接水部分会产生等量反向视差偏移，是零和转移不是修复；
2. 视差偏移本身是**距离的函数**（0.27~1.06m随目标到最近相机的距离变化），单一常数`ground_z`修正不了一个随距离变化的误差——这一点从计算数据本身就能看出，不需要写代码验证再失败。

### 决定性节点：任务书原文明确BEV精度与障碍物检测精度是独立指标

任务书3.1.2节基础指标：
1. 鸟瞰图的精度：**与俯视摄像头的成像数据进行对比**，获得鸟瞰图误差
2. 周围障碍物检测精度：与话题发布数据对比，计算误差
3. 周围障碍物检测遗漏率：与话题发布数据对比，计算误差
4. 激光数据融合精度：与精准点云对比，计算误差

**buoy等目标的检测精度/遗漏率由指标2、3单独评分**（对应`buoys_check`等话题，即本session从头到尾在做的检测评估线，39.9%召回率那部分工作）。**指标1"鸟瞰图的精度"明确是与俯视摄像头的成像数据对比，是图像级别的比较，不是"BEV图里能否识别出单个小目标"。**

**结论：buoy因视差在BEV图像中丢失，大概率不影响BEV精度这项指标的得分**——buoy定位精度已由完全独立的检测指标覆盖，BEV只需要在整体成像层面（水面纹理、大目标位置与形状、几何对齐）与overhead camera相似。一个占整幅1000×1000图像极小像素比例的小目标缺失，对图像级相似度的影响有限。

**当前裁决**：
1. **不需要优先修复buoy的BEV渲染**——无论是改`ground_z`（已证明数值上不成立）还是检测结果叠加方案（额外工程量，且当前证据不支持这是评分刚需），都不是当前优先级；
2. 已确认的正面信号（boat精确匹配0.17m、水面有真实纹理非纯色填充、自遮挡是有意设计）初步支持BEV整体成像质量是合理的，但**尚未对"BEV精度"这个指标本身做真正的图像级相似度量化**（如SSIM或结构对齐误差），这是如果要继续投入BEV这条线时，唯一直接对应任务书指标1的下一步；
3. 视差机制诊断本身的价值：**这是一次完整、严谨的负面结论**（诊断到位、量化到位、排除了误诊方向、且被任务书原文验证为"不需要修"），避免了投入检测叠加这类大工程去解决一个可能根本不影响得分的问题。

## 融合点云精度初探：无法建立基线，但诊断本身有交叉验证价值

标定线、BEV线收尾后，尝试量化任务书基础指标4"激光数据的融合精度：与精准点云进行对比"。

### 前提检查：这份bag没有精准点云真值

`ros2 bag info` 核对全部62个话题，只有7路各自的原始`/points`和标准障碍物GT话题（boats/buoys/pillars/blocks/dock），**没有任何"精准点云"或类似`/world/points_gt`的话题**。任务书要求的比对基准大概率由比测现场的裁判基础设施提供，不在离线录制数据中——这是数据条件的硬限制，不是可以绕过的方法问题。

### 替代方案：跨雷达自洽性 + 相对GT目标位置的绝对参照

在没有精准点云的前提下，设计了两个替代度量：

**指标A（跨雷达自洽性）**：复用标定线的多路共视地标框架，统计融合点云中同一物理目标在不同雷达观测下的重合度。**性质必须明确**：自洽性是融合精度的**必要不充分条件**——如果全部雷达外参朝同一方向系统性偏移，自洽性可以完美（spread≈0）而融合点云整体相对真实世界是错的（标定线"3.6cm地标重合vs8°yaw误差"矛盾的同一机制）。

**指标B（融合点云vs GT目标位置）**：用`buoys/pillars/blocks`话题的真值世界坐标（转到base_link系）作为绝对参照，比较融合点云中对应目标簇心与GT位置的误差。这比指标A更接近评分意图，且是**融合点云本身**的误差，不同于检测线报告的"位置误差"（那是经过聚类+分类之后、检测器输出层面的误差，中间隔了额外环节）。

### 分距离段结果：0-20m样本量致命不足，无法建立基线

按0-20m/20-40m/40m+分段统计（只用静止目标——pillar/buoy/block，避开boat运动引入的时间对齐误差）：

| 距离段 | 检测率(指标B相关联的匹配率) | 静止目标数量 | 出现机会数 |
|---|---:|---:|---:|
| 0-20m | 69.3% | **仅2根立柱** | 1554 |
| 20-40m | 18.1% | 4根柱 | 3108 |
| 40m+ | 12.1% | 14根柱 | 10878 |

**0-20m只有2根立柱支撑**——不管这段距离内的误差算得多精细，2个样本不能支撑任何"融合精度基线"的结论，样本量本身就否决了这条线在当前数据下的可行性。

### 一次成功的交叉验证：69.3% vs 19.4%不是矛盾

初看69.3%（0-20m局部检测率）与检测线报告的总体19.4%似乎矛盾，但两者分母完全不同——69.3%的分母是"2根柱在0-20m内出现的1554次机会"，19.4%的分母是"全部36根柱在所有距离上的出现次数"。按上表加权：

```
(1077 + 564 + 1314) / (1554 + 3108 + 10878) = 2955 / 15540 ≈ 19.0%
```

**与检测线独立测出的19.4%高度吻合**——20-40m/40m+这两段远距离样本占了全部机会的90%，把总体拉到19%附近。这不是矛盾，是**两条完全独立的评估路径（检测评估器 vs 融合精度诊断脚本）在同一份数据上互相印证**，增加了两边结论的可信度，值得记录。

另需注意的方法论陷阱：近距离簇密度高时，2m匹配半径容易饱和错配（约31%匹配到`discarded_*`类簇而非真实目标簇），做近距离统计时必须对此做甄别，不能直接信任粗匹配结果。

### 结论

- **无法建立真正的融合精度基线**——根本限制是数据条件：没有精准点云真值，且唯一可用的静态真值参照（立柱）在近距离样本量不足（0-20m仅2根）；
- **诊断本身仍有价值**：识别出0-20m密集匹配的错配陷阱、且通过独立的加权重算验证了检测线19.4%基线的可信度（交叉验证成功）；
- **真正的融合精度评分只能在比测现场用精准点云完成**，这不是离线数据能补齐的，不应该继续在这条数据上投入。

## 阶段性小结（三条探索线收尾）

本session从BoatTracker/时间戳调查开始，先后完整走完标定、BEV、融合精度三条独立探索线，结论汇总：

| 线 | 结论 | 性质 |
|---|---|---|
| Airy六路自标定 | 七轮验证后触及可辨识性边界，当前数据/扰动量级下不可靠；安全默认值=名义外参+`ForwardConsistencyMonitor`降级 | 探到真实边界，非放弃 |
| BEV精度 | 视差机制诊断清楚（含反直觉的"改ground_z治标不治本"结论）；但任务书明确BEV评的是图像相似度，buoy检测精度由独立指标覆盖，不需要优先修 | 诊断证明"不必修" |
| 融合点云精度 | 无精准点云真值，静态真值近距样本量不足，无法建立基线；诊断本身与检测线19.4%基线交叉验证成功 | 数据条件限制 |

三条线共同的方法论主线：**不信过程/内部指标（内部残差、匹配计数、自洽性），只信独立验收**——这个原则在标定线的coarse-to-fine陷阱、融合精度线的自洽性边界说明中反复验证有效。三条线的收尾都不是失败，而是把"要不要继续投入"这个问题从直觉判断变成了有数据支撑的决策，且都明确了任务书条款或数据条件对下一步的实际约束。

**检测评估线（39.9%/42.7%总召回/总误检）仍是本session唯一有完整、可复现、多渠道交叉验证基线的部分。**

## BoatTracker改用base_link：第一次拿到真正可信的效果数字

三条探索线收尾后，转向合规性修复：BoatTracker此前一直依赖odom关联（世界系跟踪），但比赛现场不发布odom，当前实现在真实比测中根本无法运行。同时这也是本session从最早"2.7%→3.9%"（已作废）开始就一直悬空的问题——**tracker从未在正确的时钟域+坐标系下真正跑过一次，其真实效果至今是空白。**

### 单变量改动：只改关联坐标系，不动其他

`BoatTracker::update()`签名移除`odom_x/odom_y/odom_yaw`三个参数，内部不再做`boatToWorld`/`worldToBoat`坐标转换，直接在调用者传入的base_link坐标里维护轨迹位置/速度。不改时间戳源、不加去重、不调参数——严格单变量，避免重蹈本session开头"BoatTracker集成同时改四个变量"的覆辙。

`main.cpp`集成同步简化：不再需要odom同步查找，每次处理都直接调用`update()`；同时清理了因此变成死代码的`recordOdomSyncGap`及一整套odom-gap诊断字段（`odom_sync_tolerance_sec_`及相关计数器），这些只在旧的odom同步机制下有意义。

### 先证明它真的在跑，再测效果

这是这次和上次最大的区别。加了`updateCount()`诊断计数，改完后先跑一次确认：

```
[BoatTrackerDiag] updates=766 resets=0(nonmono=0,gap=0) active=5 ...
```

**766次update、0次reset**——和旧版"每帧都因odom同步失败而reset()、tracker从未真正运行"完全不同，确认这次是在真实工作。

### A/B测试：3次开/3次关，取中位数

跑测中一度发现某次tracker-on试验出现20次large-gap reset、det消息数骤降25%（772→578，超出5%口径警戒线），排查确认是系统负载导致的运行时抖动（非代码问题），重跑后恢复正常（0 reset），不计入最终统计。最终6次干净跑测（det消息数767-785，口径一致）：

| 配置 | boat召回率(3次) | 中位数 | boat误检率(3次) | 中位数 |
|---|---|---:|---|---:|
| tracker关闭 | 18.1% / 17.9% / 17.9% | **17.9%** | 41.1% / 41.7% / 41.8% | **41.7%** |
| tracker开启 | 20.4% / 19.0% / 20.6% | **20.4%** | 45.7% / 50.2% / 44.7% | **45.7%** |

**结论**：tracker确实有真实效果（这次是在正确坐标系下第一次验证），但召回+2.5pp的同时误检+4.0pp，是**召回/误检的权衡，不是干净的Pareto改进**——和更早（时钟域错误之前）那次"速度门扫描未找到召回不降且误检不升的点"是同一个模式，只是这次的数字第一次建立在真正跑通的base_link口径上，可信。四类总账（总召回38%/总误检46%附近）在开关tracker之间几乎不变，确认影响局限于boat类别，没有意外污染其他类。

**当前默认状态（首次写下时）**：`boat_tracker_enabled`保持默认关闭。如果要继续这条线，下一步是调参（`min_confirmed_hits`、`association_gate_m`、速度门）寻找更好的权衡点，而不是简单地"开"或"关"。

### 调参前先做归因：三个假说依次证伪，收敛到"根因在上游检测器"

没有直接盲扫参数，先加了`matchedOutputCount`/`backfillOutputCount`/`newlyConfirmedCount`计数和逐条`(track_id, misses, avg_speed)`诊断，离线交叉验证真值匹配情况，试图先预测最优参数值再验证。

**第一步：按misses分桶的匹配率（misses=0是本帧真实关联，>0是恒速外推补帧）：**

```
misses=0（真实关联）: 163/2240 = 7.3%匹配率
misses>0（恒速外推）: 148/707  = 20.9%匹配率
```

反直觉——补帧输出匹配真值反而比同帧真实关联更高，与"补帧在制造垃圾"的预期相反。

**第二步：按track id聚合，分"曾匹配真值"和"从未匹配真值"两组，看平均速度分布（假说：从未匹配的是速度接近0的静止杂波，验证`min_speed_mps`能否用来过滤）：**

```
曾匹配真值的track（真boat）: 14条, 中位avg_speed=0.013 m/s
从未匹配真值的track（噪声）: 51条, 中位avg_speed=0.628 m/s
```

**假说被推翻，而且方向相反**：噪声轨迹的平均速度反而更高。原因是`average_speed`这个量本身是"轨迹全生命周期净位移÷全生命周期时长"，不是路径速度——真boat轨迹存活得久（14条平均62帧/条），如果在低速航行/绕行/停靠段净位移小，均速会被拉到接近0；而噪声轨迹存活短（51条平均35帧/条，很可能是track id在邻近静态目标间跳），短时间内的位置跳变反而堆出较大的"均速"。阈值扫描确认了这个度量方向反了：`min_speed_mps=0.2`会排除29.5%的真boat帧，但只排除7.9%的噪声帧——`min_speed_mps`路线被否决，且是这个度量本身不适合做判据，不只是阈值没调对。

顺带发现：这也解释了第一步的反常——不是少数持久静止杂波在misses=0里拖累匹配率，而是从未匹配真值的51条短命轨迹贡献了misses=0总量的60%（1310/2182帧），量大但每条命短。

**第三步：这51条从未匹配真值的轨迹，到底是"tracker把好检测跟丢/跟串了"，还是"检测器本来就输出了垃圾、tracker只是忠实跟踪"？**

关键点：misses=0时tracker输出的坐标就是`detection.center.x/y`本身，未经任何修改——所以misses=0帧的真值匹配结果，等价于"tracker关闭时这帧检测是否也是误检"，不需要额外跑tracker-off对照。

```
从未匹配真值轨迹的misses=0帧（原始检测）: 1310帧，0%匹配 → 这些是检测器自己的误检，tracker关闭也一样是误检
从未匹配真值轨迹的backfill帧: 497帧 → 这部分不存在于tracker关闭的输出里，是tracker外推产生的净新增量
497/(199+497)=71.4%——从未匹配真值轨迹的backfill贡献了全部backfill输出的71.4%
```

结论是二者都成立，不是非此即彼：**65%是检测器噪声的忠实透传（tracker无罪），但另有约30%是tracker把这些噪声通过恒速外推延长了存在时间，是tracker净新增的误检量。**

**第四步：`max_missed_frames`能否找到一个干净的切分点？** 按精确misses值拆分backfill帧的"从未匹配轨迹占比"：

```
misses=1: 65.3%    misses=2: 71.4%    misses=3: 74.6%    misses=4: 77.9%    misses=5: 77.6%
```

占比随misses值平缓爬升，没有陡峭的分界点——意味着不管把`max_missed_frames`收紧到哪个值，砍掉的backfill里"噪声轨迹"和"真boat轨迹"的比例大致不变（~71-78%噪声），收紧窗口是接近线性的召回/误检权衡，不是精准手术刀。这和已观测到的+2.5pp召回/+4.0pp误检这个权衡本身的形状是吻合的。

**第五个、意外但更重要的发现**：连14条确实对应真boat的轨迹，自己的原始逐帧输出也只有17.5%落在2m容差内匹配真值。**即使tracker完美工作、跟的是真船，多数单帧位置本身就落在容差外**——这指向的不是tracker能不能修的问题，是**检测器本身的位置精度**，和boat 20%召回/42%误检、之前测过的融合点云位置精度（0-20m中位误差米级）能对上。现有"2m容差内算命中"的评估口径把"完全漏检"和"检出了但位置不够准"混在一个数字里，这两者的成因和修法不同，值得分开看。

**收尾判断**：两个可调参数（`min_speed_mps`、`max_missed_frames`）都没找到干净的解——前者被数据推翻（度量本身不适合做判据），后者没有分界点（收紧是近似线性权衡，不是精准过滤）。根因在上游检测器（噪声透传+位置精度），tracker下游调参对此无能为力。止损：`boat_tracker_enabled`保持默认关闭，base_link重写和这套归因诊断作为已完成的合规性修复+调查存档，转向检测器本身（重新做一次boat误检构成归因，见下节）。

## boat误检重新归因（2026-08-20）：阈值路线到顶，时序路线同步作废

用现成的`boat_fp_attribution.py`（复用四类评估器的时钟对齐/位姿插值/2m匹配口径）在`pillar_max_pts`修复后重新跑一次boat误检构成归因，干净的tracker关闭捕获，FPR=41.6%（987/2372，和已知基线41.7%对上）。

**构成对比**：

| 来源 | 旧（pillar_max_pts修复前） | 新（当前） |
|---|---:|---:|
| 纯噪声（都不匹配） | 47.1% | **67.3%**（664/987） |
| 匹配真buoy | 49.4%（含pillar污染） | 25.3%（250/987） |
| 匹配真block | 2.8% | 7.4%（73/987） |
| 匹配真pillar | 0.7% | **0.0%**（0/987） |

**pillar污染归零，正好反证了上次修复的有效性**——那2140个冒充boat的立柱被清干净后，剩下构成里的pillar分量应声消失，两次结果互相印证。同时纯噪声占比从47.1%涨到67.3%，符合修复前预判的"如果纯噪声占比进一步上升，那确认了阈值路线到顶"。

**三条候选参数的模拟结果，不是"没调好"，是在所有测试值下都无收益**（区别很重要：说明判据本身的可用空间已经榨干，不是调参力度不够，以后不用再回来试）：

- `pillar_max_pts`继续放宽（3500-8000全测）：**0个候选被移动，所有测试值下结果完全相同**——该判据能救的已经在上次修复里全部救完，没有剩余空间。
- `block_fp_min`继续放低（1.5-1.65）：边际收益，最大也只移动183个候选，boat FPR降1.8pp的同时boat召回也跌0.9pp——不是零成本，是打折的权衡，量级小到不值得动。
- `buoy_fp_max`放宽（1.05-1.2）：净损失，boat召回从17.9%掉到14.0-17.2%，FPR几乎不降；buoy自己的FPR同时从原有水平进一步恶化（64.6-64.8%）——与更早"buoy线判死"的结论一致，这次数据再次确认。

**时序路线同步作废，理由来自刚结束的BoatTracker归因线，不是新假设**：多帧持续性过滤的本质就是BoatTracker在做的事（连续命中才确认）。而上一节已经证明，51条never-matched轨迹里大量是短命不稳定轨迹——噪声簇在相邻帧位置足够接近，同样能满足"连续几帧出现"的门槛，**时序持续性对这批噪声没有区分力**。这不是没试，是已经用真实数据测过、被同一批噪声证伪过了。

**两条线合并成一句话**：**几何判据（阈值路线）到顶，时序关联（BoatTracker路线）对这批噪声无区分力**——这是boat误检问题目前排查过的完整封锁线，不是各自独立的两个未尽事项。剩下67.3%的纯噪声无法用现有信息维度（`fp_max`/`dz`/`pts`/`square`四个几何量 + 跨帧位置连续性）分离出去。

**下一步候选，按优先级（写下时的判断，已被后续验证部分推翻，见下）**：
1. **点云特征之外的维度**（强度/传感器来源/z方向空间分布模式）——现有分类器完全没用到，是几何判据之外唯一还没触碰的信息源。
2. **视觉软证据扩展到boat**——基础设施已有（`fuseVisionColor`/`verifyPillarColor`/`verifyBlockColor`跑在pillar/block上，未接boat），且现在优先级明显上升：几何到顶、时序无效之后，视觉是唯一还没被证伪的正交信息来源。动手前先做便宜诊断：67.3%纯噪声对应的原始检测，在相机图像里是什么样子（一片水面纹理还是有可辨识特征），决定视觉这条路是不是真的可行，而不是又一条"看起来该有用但没验证"的路线。
3. 接受当前水平，转向别的评分项。

## boat误检封锁线收尾（2026-08-20续）：视觉基础设施是死代码，粘连假说被点云数据推翻，两个问题合并成一个根因

### 前提纠正：视觉验证不是"接一个已验证管线"，是三个类别从零验证

动手前先查代码，发现`fuseVisionColor`/`verifyPillarColor`/`verifyBlockColor`三个函数**在整个代码库里从未被任何地方调用过**（`grep`确认零调用点）——不是"跑在pillar/block上、只是没接boat"，是全部三个从写出来那天起就是死代码。这纠正了对"视觉扩展到boat"的成本估计：不是复用一条已验证的现成管线，是三个类别都要从零集成、从零验证。这个纠正本身值得记录，因为它同样可能误导后来看到这两个函数名以为"视觉融合在跑"的人。

### 视觉可行性诊断：加了临时ROI裁剪诊断（已用完即删），发现"密集场景"是ROI尺寸的错觉

在`processLidarBranch`里加临时诊断，把每个boat候选投影到最佳相机、按固定80像素半径截ROI存盘，再用离线脚本按坐标把677个"纯噪声"候选对应的裁剪图挑出来人工判读。两批共28张样本，几乎每张里都同时出现棕色立柱、橙黑浮球、疑似小船轮廓——第一眼像是"密集停泊区里的多目标粘连"。

**但这个视觉印象被后续的点云层级分析推翻**：真正决定"这个候选到底是什么"的不是相机ROI里看到了什么，是雷达簇本身的点云。于是又加了第二个临时诊断（`ObstacleDetector.cpp`里boat/boat_fallback候选的原始代表点转存），对同一批677个候选做主轴PCA投影+最大间隙分析：

```
最大间隙(max_gap)：p10=0.116m  median=0.199m  p90=0.259m  max=0.555m
  gap>=0.3m（可分，多目标粘连的证据）：  21/670 = 3.1%
  gap<0.2m（完全无间隙，物理上不可分）： 342/670 = 51.0%
主轴总长(extent)：p10=1.00m  median=1.21m  p90=1.34m
簇内点数：median=38  p90=103
距离分布：0-20m=57  20-40m=451（67.3%）  40-60m=162  60m+=0
```

**主轴总长中位数只有1.21m，是单个小物体该有的尺寸量级，不是多目标合并该有的跨度**；97%的候选没有能把点分成两团的空隙。粘连假说被点云数据本身推翻——**这677个候选是孤立的、稀疏的（中位38点）、紧凑的（1.2m）单个小点团，不是立柱+浮球+船粘成的复合体**。

**"密集场景"视觉印象的真实来源**：固定80像素半径的ROI，在20-40m距离上对应的地面范围远超雷达簇实际的1.2m——画面里看到的立柱、浮球是恰好落在同一个宽ROI里的**别的真实物体**，根本不是这个候选簇的点云所对应的东西。图像证据的说服力天然很强（一张montage摆在那里几乎不会有人怀疑），但这次的"看到了什么"完全是被诊断工具的ROI尺寸决定的，不是被数据决定的——**这是一次教科书式的"诊断工具自身制造假象"，和这条线上更早踩过的几次同源（`bisect_left`越界、`std::cout`精度污染、`track_mean()`跨帧平均、缓存加速的2D近似）**。

**方法论记录**：视觉/图像类诊断，ROI大小必须按目标的实际物理尺寸和到相机的距离换算，不能用固定像素半径——同样的像素半径在不同距离上代表完全不同的地面范围，否则"诊断看到了什么"这件事本身会被工具的取景框决定，而不是被目标的真实边界决定。这条和已知的"缓存加速走2D近似丢了z轴"、"逐帧平均在移动参考系里做了错误的算术"是同一类问题的图像版本：工具的实现细节（这里是固定像素ROI）悄悄替换了想测量的量（这里是"这个簇周围有什么"）。

### 两个原本分开的问题，被这次数据合并成一个根因

`boat_fp_attribution.py`归因显示67.3%纯噪声候选里67%集中在20-40m；这和更早、完全独立的一条线——"20-40m规模更大（2681次漏检、79%聚类问题），下一步重心应转向这里"（见本文档第269行附近，2026-07-24记录）——落在了同一个距离段。两条线的出发点完全相反（一个查假阳性，一个查漏检），却独立收敛到同一处，且给出统一解释：**20-40m点云密度本来就低（中位38点），稀疏点子集偶然凑出的形状统计量本身不可靠——碰巧凑得像boat就分到boat（制造误检），碰巧凑得不像就分不进任何类别（制造漏检）。同一个物理原因，同时是两个问题的成因。**

**直接推论**：这两个问题不能分开修，也不该分别投入。任何只针对其中一个的方案（比如收紧判据降误检）必然加重另一个（召回更差）——这解释了为什么这条线上四次尝试（几何阈值、时序过滤、聚类粘连、视觉扩展评估）都摸到"召回/误检同向变化、找不到干净权衡点"这同一种形状：不是没找对参数，是这两个量本来就是同一枚硬币的两面。

### 收尾判断

四条候选路径依次验证完毕，无一通过：
1. **几何阈值**：67.3%纯噪声，三个候选参数在全部测试值下无收益或负收益。
2. **时序过滤（BoatTracker）**：噪声轨迹和真boat轨迹同样能满足"连续几帧出现"的确认门槛，无区分力。
3. **聚类粘连**：点云层级数据推翻，不是多目标粘连，是单个稀疏小簇。
4. **视觉验证**：基础设施是死代码（成本被上修），且可行性诊断本身先暴露了ROI尺寸的方法论问题，还没来得及真正验证视觉判据前，根因已经在步骤3收敛到"20-40m点密度不足"——这个根因视觉同样救不了（判据能看到的颜色/纹理特征同样依赖ROI里的点数和范围，稀疏输入下颜色统计一样不可靠）。

**boat误检41.7%附近，在当前传感器配置、当前"聚类→形状分类"架构下，大概率就是这套方法的天花板**，根源是20-40m点云密度不足，和boat漏检是同一个问题。`boat_tracker_enabled`保持默认关闭，`fuseVisionColor`等视觉函数保持未接入状态，这条线到此收尾，转向别的评分项。

## pillar召回率归因（2026-08-20续）：不是判据错配，是远距物理探测限制；一个被推翻的评估口径假说

### 逐条判据诊断：这次是干净的，没查出判据错配

用和`block_h_max`/`pillar_max_pts`那两次完全相同的方法（逐条判据通过率诊断），对贴近真值pillar(2m内)的候选簇按当前四条判据(`fp_max<=2.0`/`dz>=1.5`/`slenderness>=2.5`/`pts<=3000`)逐条统计。结果和前两次不同：**0个候选同时满足全部四条判据却没被分进pillar**——判据本身没有卡死任何东西。贴近真值的2255个候选里，71.6%已经正确标成pillar，22.7%标成buoy（`dz`中位数仅0.59m，物理上就不是立柱，是共存的浮球，不是误判）。**这次诊断成功地把问题排除在分类阶段之外**，不需要再往判据阈值上找。

### 候选形成率，按距离分桶：近距健康，远距断崖，且远距占了大部分样本

加临时诊断（`main.cpp`里对36根已知真值立柱世界坐标做base_link变换，统计1m/3m半径内原始融合点数，`ObstacleDetector.cpp`不改；这36个坐标仅供离线诊断硬编码，不进检测/分类逻辑，用完即删），交叉`cluster_diagnostic`的候选簇位置，得到候选形成率（不看标签，只看"2m内有没有任何簇"）：

```
距离段     总样本    候选形成    形成率
0-20m      1551      1547       99.7%
20-40m     3103      3094       99.7%
40-60m     3880      3688       95.1%
60m+      19399     12326       63.5%   ← 占全部(立柱,帧)组合的69%
```

**0-60m内候选形成基本是干净的（95.1-99.7%），60m+才断崖式下跌，而60m+恰好占了绝大多数样本**——36根立柱散布在约230m×190m区域，船的活动轨迹天然导致大多数时刻离大多数立柱很远。60m+里"没形成候选"的案例，86.6%是1m内**零点**（传感器物理探测距离限制，无解），仅12.6%（约891次）是"点数够(≥4)但没聚成簇"——规模不大的聚类参数机会，记为待办，不现在动（收紧`cluster_tol`是全局改动，风险面大，且规模太小不值得单独承担这个风险）。

**结论**：pillar的19.4%召回率，主要由"大多数时候大多数立柱本来就在探测距离外"决定，不是算法在能探测到的范围内表现差。这个结论和boat性质完全不同——**boat是"能探测到的范围内也做不好"（架构到顶），pillar是"能探测到的范围内做得挺好，只是大部分时间探测不到"**。两者数值接近（19.4% vs 20.4%）但不该被同等看待或用同样的方式改进。

### 一个被提出、又被推翻的假说：评估口径是否存在跨类别匹配错配

诊断链条走到这里出现一个诱人的数字巧合：候选形成率99.7% × 候选分类正确率71.6% ≈ 71.4%，和已知的0-20m实测召回率69.3%几乎对上（差0.1个百分点）。据此提出假说：**2m匹配容差下，密集共存的buoy候选可能"抢"了pillar真值的最近邻匹配名额**，压低了pillar记录到的召回率、同时抬高了buoy的（buoy 63.9%是四类最高，且之前有过"1036个大簇2m内撞上buoy真值"的旧记录，两条线索方向一致）。

**用两个独立方法验证，双双推翻**：

- **代码结构层面**：`four_category_evaluator.py`的`CATEGORIES`列表显示，四类评估分别调用`evaluate()`，用的是四个完全独立的检测流（`det`/`det_buoy`/`det_pillar`/`det_block`），对应节点发布的四个不同话题。一个被分类器标成`buoy`的候选，从数据源头就只进`det_buoy`流，根本不会出现在`det_pillar`的匹配候选里——**跨类别抢名额这个机制在代码结构上不存在**。
- **行为层面**：把`MATCH_TOL`从2.0m收紧到1.0m重跑。如果真存在"被更近的异类候选抢跑"，收紧容差应该排除掉错配、让pillar/buoy召回回升或至少持平；实测方向相反——**pillar召回15.1%→10.9%，buoy召回64.5%→33.9%，两类都随容差收紧而下降**，说明2m内的命中大多是位置误差在1m量级内的真实匹配，不是被顶替的假匹配。

**两个独立来源同时否决，假说撤回。**

### 方法论记录：数值巧合被当成机制证据——一种新的、更难防的失败模式

这条线上之前踩过的几次工具问题（`bisect_left`越界、`std::cout`精度污染、`track_mean()`跨帧平均、聚类粘连排查里ROI固定像素半径）有一个共同点：**工具本身产出了错误的数字**，查工具就能发现。

这次不一样：**71.6%、99.7%、69.3%这三个数字全部是对的，没有一个是错的**。出问题的是把它们相乘、看到结果和一个观测值接近，就据此反推出一个具体的因果机制（"buoy抢pillar的匹配名额"）——这个机制在代码层面从根上就不存在，而这个假说提出时之所以显得有说服力，恰恰是因为它同时"解释"了三件事、又能和另一条旧线索对上，**说服力强是因为编得圆，不是因为有证据**。71.6%和69.3%这两个数字实际统计的不是同一个量（前者是候选个体层面的分类正确率，后者是目标-帧层面的召回率），数值接近只是同一现象在两种口径下的巧合，不构成因果链条。

**纪律**：任何"因为数字对上所以机制是X"的推断，必须再找一个独立于该数值关系本身的证据来源去验证——不能让数字关系自己既当线索又当结论。数字对齐只能拿来指方向，不能替代验证。

**这次验证的方式值得固化为标准做法**：用了两个方法论上互相独立的验证——查代码结构（机制层面：这条路径是否存在）+ 收紧容差重跑（行为层面：如果机制成立，行为该往哪个方向变，而且方向是**验证前就定好的**，不是事后解释）。"提出机制→推出一个可证伪的行为预测→实测对照方向"这套做法，在`block_h_max`、`pillar_max_pts`两次代价评估的净账预测里已经用过，这次是第三次成功案例，值得作为这条线的标准动作固定下来。

### pillar收尾

| 距离段 | 状态 |
|---|---|
| 0-20m | 候选形成99.7%，分类基本健康 |
| 20-60m | 形成率95.1-99.7%，仍健康 |
| **60m+** | **形成率63.5%，其中86.6%是1m内零点，占全部样本69%** |

19.4%的总召回率主要由远距物理探测限制决定。评估口径本身没有查出失真证据，39.8%总召回率的类别间分配目前看是可信的。唯一的可修机会（891次点够未聚类）规模小、代价大，记为待办不现在做。这条线到此收尾。

## block召回率归因（2026-08-20续）：点稀疏+单视角近端面偏移，不是聚类参数问题；一次被自我推翻的中间结论

### 初步信号：候选形成率不如pillar干净，看起来像有机会

用同一套方法（临时诊断：6个已知真值block世界坐标转base_link，统计半径内原始融合点，交叉`cluster_diagnostic`候选位置）先看候选形成率，按距离分桶（block真值全在20-60m，0-20m占0%，此前已知）：

```
距离段     形成率    无候选案例中"点够(≥4)但没聚类"占比
20-40m     93.4%     18.9%
40-60m     73.2%     18.1%
```

这个占比明显高于pillar在60m+段的12.6%，第一眼像是block这条线和boat/pillar不同、真的存在一个可修的聚类参数机会——**这是这条线上第一次不是"到顶"的信号**。

### 深挖：把半径从1m/3m放宽到2m/4m，发现"无候选"案例在更大半径内其实有很多点

放宽半径复测后，"无候选"案例中4m内有点的比例从最初的一小部分跳到 20-40m段59.2%、40-60m段24.0%——一个诱人但需要谨慎对待的信号：**是点分散在GT周围更大范围（B：这些点可能属于别的物体），还是GT标注中心和点云实际质心系统性偏移（A：只是半径框歪了）？**

### 定案：三项联合诊断，A成立，不是B，也不是聚类参数问题

同一次跑测里加了三个互相独立的判断，避免样本量小（只有6个真值block）导致单一指标的结论不可靠：

**1. 偏移方向一致性**（区分A/B的关键判据）：把GT中心到点云质心的方向，与"本船到GT"的方位角做比较——

```
偏移方向朝本船（近端面假说A）：3285/4257 = 77.2%
偏移方向背离本船：                15/4257 =  0.4%
散乱（可能是B或噪声）：          957/4257 = 22.5%
偏移量级：中位0.78m，p90 1.38m
```

**77.2%系统性指向本船，方向高度一致**——这是A的强证据：block是有实体厚度的浮块，激光雷达物理上只能打到朝向本船的那一面，点云质心天然偏向本船一侧，偏离GT标注的几何中心。**这是单视角传感的物理必然，不是bug，也不是B（点属于别的物体）**。B（散乱，可能是噪声或真的属于别的物体）只占22.5%，不是主流模式。

**2. 成功组(形成候选)vs失败组(未形成候选)对照**（用成功组做基线，弥补样本量小的问题）：

```
                成功组(n=4000)    失败组(n=262,取有效点数>=3的子集)
n_points中位数     189               9        ← 差20倍
max_gap中位数      0.40m             0.80m
extent中位数       2.72m             1.90m
```

失败组点数中位数只有9，是成功组(189)的1/20——**是点本来就少，不是点多但被`cluster_tol`卡住连不上**。间隙0.80m虽接近`cluster_tol=0.9m`的上限、看起来"差一点就能连上"，但这是基于中位数仅9个点算出的1D主轴投影间隙，样本量太小、且不等于聚类实际用的2D/3D最近邻距离，不能当作"参数只差一点"的证据。

**3. 自我修正**：初步信号里那18.9%/18.1%"点够但没聚类"的估计，是在近端面偏移效应被发现之前、用未经修正的1m/3m半径算出来的——**这个半径本身框歪了（框在了GT几何中心而不是点云实际质心），把"其实有点，只是2m框没框到"的案例，和"真的没点/点不够"的案例混在了一起统计**，导致这个比例被高估。加入偏移方向判据后，真正符合"点多但连不上"（B+聚类参数问题）特征的案例规模远小于初始估计。

### 关于跳过代价评估：一条纪律的正确用法

`block_h_max`和`pillar_max_pts`两次都在"诊断显示存在可修空间"之后才做代价评估（扫描候选参数值，统计新增/影响候选的真假比例）。这次没有走到这一步——**因为代价评估的前提是"已确认存在可修的判据/参数问题"，而这次三项联合诊断已经把这个前提本身否掉了（不是B，不是"点多连不上"）。前提不成立时，代价评估没有对象可评估，跳过是遵循同一条纪律，不是绕开它**。

### 待观察项：近端面偏移量级和位置误差量级接近，但先不下结论

77.2%这批偏移的中位数0.78m、p90 1.38m，和此前多处记录的位置误差量级（0.6-1.1m，例如boat误检归因里的0.90m、融合精度评估里的同量级数字）接近。**这可能提示：全场景下相当一部分"位置误差"，成分上其实是"只测到近端面、没测到几何中心"的系统性偏移，不是随机噪声**。但这只是现象观察，先记录，不据此推断"实际精度比看起来更好"——那个结论依赖赛会评分口径本身怎么定义"位置精度"（是否对准几何中心、还是允许近端面误差），现在不掌握这个信息，不能替评分方下判断。

### block收尾

**点稀疏（失败组中位9点，是成功组的1/20）是block低召回的主要原因，聚类参数不是可修方向**。近端面偏移（77.2%系统性指向本船，中位0.78m）是block位置精度和候选定位的物理成因之一，同样不可通过调参解决。这条线到此收尾。

### 四类归因阶段性汇总：三条线，三种表现，同一个约束

| 类别 | 召回率 | 瓶颈性质 | 能否调参解决 |
|---|---:|---|---|
| boat | 20.4%/误检41.7% | 20-40m点密度不足，稀疏点子集偶然凑出不可靠的形状统计，同时制造漏检和误检 | 否——四条候选路径（阈值/时序/聚类粘连/视觉）依次验证均无效 |
| pillar | 19.4% | 60m+远距物理探测限制（86.6%是1m内零点），60m+占全部样本69% | 否——分类阶段本身干净（0个候选卡在判据上），瓶颈在能否探测到，不在探测到后怎么分类 |
| block | 26.1% | 点稀疏（失败组中位9点）+ 单视角近端面偏移（77.2%系统性偏向本船） | 否——不是聚类参数能连上的稀疏，是点本来就少 |

**三条线殊途同归**：表现形式不同（boat是形状统计不可靠、pillar是探测距离硬限制、block是单视角遮挡+点稀疏），但都指向同一类根本约束——**当前传感器配置下，特定条件（远距/小目标/单视角遮挡）下的点云密度不足以支撑当前"先聚类、再按形状分类"这套方法的可靠工作**。四类里只有buoy（63.9%）还没有做过同等深度的归因，是否也是同一个根因、还是另有原因（比如评估口径本身的问题），是下一步要查的。

## buoy 63.9%是否虚高（2026-08-20续）：随机基线判据定案，怀疑被推翻

### 起因：buoy真值密度极高，怀疑2m容差下"随便撞上"撑起了召回率

buoy真值密度是四类里最高的（36078次记录/42个真实目标），而`block_h_max`修复的代价评估里有过一条旧线索：1036个`fp_max∈[1.7,3.2]`的大簇（明显不是buoy的形状，`buoy_fp_max=1.0`）能在2m内匹配上buoy真值位置——提出假说：buoy的高召回率里，有相当一部分是"任何位置差不多的候选都容易撞上密集buoy场里的某一个真值"这种几何巧合，不是真实检测能力。

### 判据1（容差敏感性）：单独看像是佐证，但被一个对照组否掉

先测了`MATCH_TOL`从2.0→1.0→0.5m时四类的相对降幅：

```
              2.0m容差   1.0m容差   0.5m容差   2.0→1.0相对降幅
boat            18.1%     10.5%      6.0%         42.0%
buoy            64.5%     33.9%     15.9%         47.4%
pillar          15.1%     10.9%      0.0%         27.8%
block           22.3%     16.1%      1.0%         27.8%
```

单看buoy(47.4%)比pillar/block(27.8%)降得更多，像是虚高的佐证。**但boat的降幅(42.0%)几乎和buoy一样大，而boat真值本来就稀疏、不存在密度虚高的可能**——这个对照组直接否掉了容差敏感性作为证据的有效性。四类在2.0m容差下的位置误差均值也高度接近(0.84-0.95m)，说明这个指标主要反映的是**位置误差分布的形状**，不是密度效应。**一个指标如果在"机制该成立"和"机制不该成立"的两组上表现相同，它就不是那个机制的证据**——检验一个佐证是否有效，最简单的办法就是找一个天然不该受该机制影响的对照组（这里是boat）看它的表现。

### 判据2（分类结构核查）：det_buoy流本身已经保证fp_max<=1.0

`ObstacleDetector.cpp`里buoy的主判据和fallback判据都要求`fp_max<=cfg_.buoy_fp_max(1.0)`——任何进入`det_buoy`流、被计入buoy召回率统计的候选，形状上不可能是那种`fp_max=1.7-3.2`的大簇。那1036个候选从未被分类器标为`buoy`/`buoy_fallback`，它们只是几何位置巧合落在buoy真值附近，属于`block_h_max`修复代价评估里"检查对buoy的副作用是否为零"这件事，和buoy自己的召回率计算无关——这条旧线索本身就被错误地关联到了buoy虚高这个问题上。

### 判据3（随机基线，最终定案）：可证伪预测，实测方向和幅度都不支持假说

**用user提出的最硬判据**：把每帧真实的det_buoy候选替换成同数量的随机点（在该帧buoy真值的范围内或固定60m半径内均匀采样），跑同一套匹配逻辑（2m容差、逐帧最近邻、`matched_gt`去重），看"纯几何巧合"能撑起多少召回率。这是一个**可证伪的方向预测**：如果虚高假说成立，随机基线应该有可观的数值（十几到几十个百分点）；不成立的话应该接近零。

```
真实buoy召回率：                    64.5%
随机基线(每帧GT范围内均匀采样)：      0.0%（5次试验全部为0）
随机基线(固定60m半径均匀采样)：      3.4%（5次试验均值）
```

**随机基线远低于实测，差距大到没有解释空间。** 假说直接出局。

**过程中的自我纠错**：脚本第一版给`det_buoy`单独重新计算了一个epoch offset（用`det_buoy[0].stamp - odom[0].stamp`），而不是复用从boat gt算出的唯一canonical offset——这正是本文档反复记录、也已经写成约定的坑（"canonical epoch offset从gt[0]-odom[0]算出，套用到所有需要对齐的序列，不能每个序列自己重算一个"），这次在写新脚本时又踩了一次，第一版结果算出真实召回率77.5%（和已知的64.5%对不上），靠这个已知值当校验点才发现问题。**修复后重新验证：能复现已知的64.5%，才认为脚本本身可信，再采信它给出的随机基线数字。**

**纪律升级**：这个坑反复出现说明"知道这条纪律"和"每次写新脚本都执行它"之间有距离，光靠记住不够。**升级为结构性要求：任何新分析脚本的时间对齐，必须直接调用/复制一个已验证的对齐函数或复用同一个已算出的offset常量，不允许在脚本内重新推导一个新的offset**——从约定变成结构上不给出错空间。同时，**用一个已知正确的数字（这里是64.5%）当校验点，验证新脚本本身先可信，再采信它的新结论**，这个习惯本次成功防住了一次潜在的错误结论，值得保持。

### 结论：buoy的63.9%没有查出口径问题，39.8%不需要重算

三个判据（一个被自证无效，两个确认无污染）合起来：buoy的高召回率基本是真实检测能力，不是密度驱动的几何巧合。`block_h_max`代价评估里那条旧线索被重新核实为"和buoy召回计算无关"，不再作为怀疑buoy的证据保留。

### 四类归因线正式收尾

| 类别 | 召回率 | 根因 | 状态 |
|---|---:|---|---|
| boat | 20.4%/误检41.7% | 20-40m点密度不足，形状统计不可靠 | 四条候选路径全部验证无效，架构到顶 |
| pillar | 19.4% | 60m+超出探测范围（占69%样本），近距（0-60m）健康 | 物理探测距离限制，非算法问题 |
| block | 26.1% | 点稀疏（失败组中位9点，为成功组1/20）+ 单视角近端面偏移（77.2%系统性偏向本船，中位0.78m） | 聚类参数不是可修方向 |
| buoy | 63.9% | — | 独立验证三个判据，数字可信，无需修正 |

**三条线（boat/pillar/block）殊途同归收敛到同一个点云密度约束的不同表现形式，第四类（buoy）经独立验证排除了评估口径问题。整套四类评估39.8%总召回率的可信度到此确立，不需要因为这条线重算，后续汇报可以直接使用现有数字。**

## BEV图像精度初步基线（2026-08-20续）：首次量化，找到真实俯视相机，一个可能可修的窄带artifact

四类检测线收尾后，转向此前完全没有基线的一项：任务书3.1.2基础指标第1条"鸟瞰图的精度：与俯视摄像头的成像数据进行对比，来获得鸟瞰图的误差"。之前只定性看过`bev_check`（拼接框架work、boat位置准到0.17m），从未和真值源做过量化比对。

### 找到了真正的参考源：`overhead_camera_sensor`

`bag_09_03_17`里有一路之前没利用过的话题：`/wamv/sensors/cameras/overhead_camera_sensor/image_raw`——挂载在船体正上方11.95m、垂直向下看的针孔相机（1280×1280，水平FOV 120°），任务书3.1.2条里说的"俯视摄像头"就是它，此前的BEV定性排查完全没用到这个话题。

### 方法：两路话题不同时钟域，几何对齐先做验证再信任

`bev_check`用`now()`发布（epoch域），俯视相机原始话题用bag相对时间——套用本session反复验证过的canonical epoch offset对齐两者时间戳，取误差<0.05s的最近邻配对。几何对齐用俯视相机已知的针孔参数（高度11.95m、hfov=2.094rad）算出地面覆盖范围，裁剪缩放到和`bev_check`一致的±10m/1000px footprint。**对齐结果先验证再使用**：两张图里"船体轮廓包围盒中心"都精确落在图像正中心（俯视图640,640，bev图500.5,500.5，和config算出的理论值完全吻合），再用立柱、浮球散布、另一艘船图标等多个非对称特征肉眼核对相对位置一致，确认不需要额外旋转/镜像才敢采信后续SSIM数字——这一步如果偷懒，后面所有数字都是假的。

### 结果：两个口径的数字都要报，因为任务书没说清楚

任务书3.1.2原文只有一句话，**没有说明是否要排除自遮挡区域**（我们主动在`bev_check`中心画的黑框，`self_x_half=7.5m`/`self_y_half=2.75m`）。两种口径给出的数字差近0.2，对应完全不同的处境：

```
含自遮挡黑框（按整张图逐像素比对）：      SSIM = 0.7257 ± 0.0030
排除自遮挡黑框（只看周围障碍物和水面）：  SSIM = 0.9210 ± 0.0038
```
（3个相隔20+秒的独立时间窗口，共8个精确同步帧对，同步误差<0.05s）

**如果评分按整图比对（含遮挡区），那"用纯黑填充自遮挡区"本身就是当前最差的选择**——填成水面色、或用相邻帧/相邻相机信息补一下，SSIM都会比纯黑高，这是个和下面"窄带artifact"完全独立的、可能更便宜的改进点，但要不要动它取决于评分口径，口径不明前不动。

### 一个反直觉但诊断很干净的发现：不是"近场普遍更差"，是紧贴掩膜边界的一条窄带

最初按"近场(0-5m)/远场(5-10m)"粗分时，近场明显更差（0.8187 vs 0.9302）。**但细分半径后发现，0-2m半径内根本没有任何"排除遮挡后"的有效像素**——`self_y_half=2.75m`意味着自遮挡框沿窄轴方向已经把0-2.75m整个范围盖住了。**所以"近场0.8187"这个数字的真实含义要修正：它是2.75-5m这个环的表现，不是0-5m的表现**，之前的表述会让人以为覆盖了船体周边全部区域，这是口径描述问题，不是数据错误，但同样会误导，需要写清楚。

按更细的半径分桶重新看：

```
半径分桶       SSIM(排除自遮挡框后)
2.5-3.0m          0.4268   ← self_y_half=2.75m正好落在这一环里
3.0-3.5m          0.5448
3.5-4.0m          0.8165   ← 快速恢复
4.0-5.0m          0.9045
5.0-6.0m          0.9263
6.0-8.0m          0.8996   ← 轻微二次下探，样本小，不下结论
8.0-10.0m         0.9311
```

**断崖式下跌又快速回升(0.43→0.54→0.82→0.90)，这个形状本身就是判据**：如果是普遍性的视差/拼接误差，应该表现为随距离平缓衰减；实测是在`self_y_half`附近断崖式下跌、4m外迅速恢复正常（0.90-0.93），这是掩膜边界局部对齐问题的典型特征，不是大范围精度不足。

**三种可能成因，留作后续排查入口**（下次接手不用重新想，直接看边界像素长什么样就能区分）：
1. **掩膜画大了**——黑框超出真实船体轮廓，把本该有内容的区域涂黑了 → 边界内侧表现为"该有内容却是黑的" → 修法：缩小掩膜
2. **掩膜画小了**——船体边缘一圈留在图上，但那里是IPM投影严重失真的区域（接近相机正下方，投影拉伸极大） → 表现为"有内容但严重变形" → 修法：扩大掩膜
3. **边界处插值失真**——掩膜大小没错，但`cv::remap`在边界附近的采样落到源图像边缘，产生拉伸/重复像素 → 表现为"有内容但是拉伸的条纹" → 修法：边界处理逻辑本身

6-8m那个轻微二次下探（0.8996，std比其他桶高5倍）可能是相机拼接缝隙，样本量还小，先记录不下结论。

### 局限性：样本量还不够，下一步先扩样本再决定要不要修

8个样本来自3个独立时间窗口（每个窗口内的帧几乎是同一场景的连续快照，真正独立的场景配置只有3个），且集中在停靠接近阶段偏慢的一段轨迹，不是90秒全程的代表性抽样。

**当前三条发现（口径差异、窄带artifact的形状与成因候选、局限性）内部自洽，判断不会被扩样本推翻，值得先固化记录。但要不要动手修窄带artifact，等沿全程均匀采样确认它是否普遍存在之后再定——万一只在这3个窗口的特定几何配置下明显，修了也是无用功，这和这条线上其他几次"先证明现象普遍存在，再投入修复"的教训一致。**

### 扩样本确认：窄带artifact在全程都存在，不是窗口特有的

沿全程（10/20/27/40/50/60/70/80s，覆盖86秒bag的绝大部分）重新采样，7个独立窗口（off70这次恰好0个有效配对，其余7个都成功），13个精确同步帧对：

```
                    3窗口(初版)   7窗口(全程)
含遮挡SSIM            0.7257        0.7258
排除遮挡SSIM          0.9210        0.9210
每窗口排除遮挡SSIM区间  0.9173-0.9285  0.9156-0.9292
```

```
半径分桶       3窗口SSIM   7窗口SSIM(全程)
2.5-3.0m        0.4268      0.4252
3.0-3.5m        0.5448      0.5441
3.5-4.0m        0.8165      0.8137
4.0-5.0m        0.9045      0.9016
5.0-6.0m        0.9263      0.9231
6.0-8.0m        0.8996      0.8976
8.0-10.0m       0.9311      0.9348
```

**两组数字几乎逐桶重合**（最大差异不超过0.003），确认：整体SSIM基线和窄带artifact的形状都不是这3个窗口的特有现象，在近全程的不同场景配置下稳定复现。**基线数字和窄带artifact现在都可以视为已确认，值得投入去查具体是三种候选成因里的哪一种**（掩膜画大/画小/边界插值失真，见上文，通过看边界像素是黑洞/变形/拉伸区分）。6-8m那个轻微二次下探（0.8976→0.8996，std仍明显高于相邻桶）也同样复现，暂不下结论，留作后续观察项。

**当前状态**：BEV精度基线（含遮挡0.73/排除遮挡0.92）和窄带artifact（2.5-4m环，掩膜边界对齐问题）均已在全程范围验证确认，不是初步印象。评分口径（是否排除自遮挡区）仍待确认，两个数字都保留。是否投入修复窄带artifact，取决于评分口径确定后的收益评估，以及和其他仍待办事项的优先级比较。

### 窄带artifact成因排查：确认是掩膜画小了（IPM近船投影失真未被完全遮挡），但修复不是无条件有益

**视觉判据**：截取同步帧对里2-5m环附近的区域并排放大对比（俯视真值 vs 我们的`bev_check`），紧贴掩膜边界的多个目标（浮球）表现为**清晰的拉伸变形**——俯视图里是正圆，我们的BEV里拉成明显的斜向椭圆，尺寸也偏大。这不是"该有内容却是黑的"（掩膜画大），也不是"放射状条纹/像素重复"（remap边界越界），是"有内容但严重变形"——对应此前列出的三种候选成因里的第2种：**掩膜画小了，船体边缘外IPM投影严重拉伸的过渡带没有被遮挡区盖住**。物理机制吻合：接近相机正下方（船体附近）时，地面点对应的入射角趋近视场边缘，投影拉伸急剧增大。

**发现一个参数身兼两职的实例（这条线上第四次）**：`ImageStitcher::setSelfOcclusionBox`（`self_x_half_`/`self_y_half_`）原本只用于`detectBuoysVision`排除船体自身色块，语义是"点云/色块自身回波过滤范围（物理船体尺寸）"，和"BEV渲染该挡多大一圈"是两件不同的事，只是数值上历史地保持一致（注释写着"需与…保持一致"）。这次在动手改之前先识别出来，新增独立的`bev_occlusion_x_half_`/`bev_occlusion_y_half_`字段和`setBevOcclusionBox`接口，只修改BEV渲染这一路，不触碰点云自身过滤逻辑。**检查清单值得固化**：改任何配置参数前，先grep它被几处引用、各自语义是否真的相同；"需与…保持一致"这类注释是信号，说明的是当前取值相同，不代表语义相同。

**实测：扩大掩膜半径不是无条件有益，两种评分口径给出相反方向**（两个独立时间窗口，off27+off50）：

```
y_half:        2.75(当前默认)   3.5      4.0      4.5
含遮挡SSIM:       0.7281       0.6879   0.6471   0.6106   ← 单调下降
排除遮挡SSIM:     0.9240       0.9405   0.9334   0.9321   ← 峰值在3.5，之后边际递减
遮挡面积占比:      0.212        0.269    0.307    0.345
```

**这次的判断错误值得记录**：动手前曾判断"两种口径下这个修复都不会变差，只是收益大小不同"——这个判断本身推理不完整，漏算了一件事：**纯色（黑色）填充区域在SSIM里是天然最坏情况**（逐像素和有细节的真实内容结构完全不匹配），这个结论不需要实测就能推出来。扩大遮挡半径，在"整图比对"口径下是**净损失**（且降幅0.04起，比"排除遮挡"口径下的收益0.0165大得多，赔率不对称），只有在"排除自遮挡区比对"口径下才是净赚。口径没有倾向性证据支持任何一边时，不该押注。

**决定**：`bev_occlusion_y_half`默认值维持2.75（不引入未经验证的改动），把实测数据和两种口径各自的行动方案写进代码注释（`ImageStitcher.hpp`），口径确认后直接查表：排除遮挡→改3.5；整图比对→维持2.75不动。改动已就绪（独立ROS参数），切换成本为零，押注不省工作量只提前承担风险，没有押注的必要。

**副产品：一个可能收益更大、且和调半径互补的方向**——含遮挡口径下，遮挡区占比21.2%却贡献了含遮挡SSIM(0.73)和排除遮挡SSIM(0.92)之间近0.2的差距，**这个差距基本全部来自"纯黑"这个填充选择本身**，不是遮挡范围大小。真正该做的可能不是调半径，是**改遮挡区的填充方式**（填水面色、或用相邻帧/相邻相机信息补，而不是纯黑）——这个改动在整图口径下收益预期远大于调半径的0.0165（因为作用的是21.2%的面积、且是从"最坏情况"往上提），而在排除遮挡口径下没有影响（那块本来就不计分）。**和调半径正好互补，两种口径各自对应一个优化手段，不冲突**：
- 排除遮挡口径 → 调`bev_occlusion_y_half`到3.5
- 整图比对口径 → 改自遮挡区填充方式（水面色/相邻信息补），而不是调半径

两者都还没实现，等口径确认后再投入哪一个。**评分口径的确认，价值现在明确到能直接决定往哪个方向投入**——建议走正常渠道直接问赛会，比在这里继续猜测可靠。

## 合规性核查（2026-08-21）：找到并拆除一个和BoatTracker同款的odom地雷

BEV/pillar/block/buoy等精度线全部收尾后，转向合规性核查——理由是"框架不符合比赛现场约束是零分，精度不够只是分低"，性质不同，值得单独查一轮。

### grep清查odom残留依赖

```bash
grep -rn "odom" src/usv_perception/src/ src/usv_perception/include/
```

**没有发现阻塞式等待逻辑**（无`wait_for`/`spin_until`/同步等odom到达的代码），任何odom缺失场景都是优雅降级，不会导致节点卡死或崩溃。节点本身只订阅image/lidar/odom三路话题，没有订阅任何GT话题（只发布`_check`系列），符合"比测只发布7路点云+6路图像"的约束。

**但找到一处真实隐患**：`CandidateTemporalClassifier`（main.cpp:786-806）在`update()`里需要odom_x/odom_y/odom_yaw做boat→world坐标转换，`odom_valid`为false时优雅降级到`reset()`——**这和BoatTracker修复前的坑是同一个模式**：比测现场没有odom，`odom_valid`会永远是false，这个功能会在比测时完全失效，且不报错、不提示，"开着但从没真正跑过"。

**风险评估**：`candidate_temporal_enabled`默认值是`false`（`declare_parameter<bool>("candidate_temporal_enabled", false)`），当前默认配置下这条路径根本不执行，不构成活跃的合规问题。但只要有人为提升召回率打开这个开关、又没意识到odom依赖没修，就会复现"改了但从没真正跑过"的坑。

### 拆除决策：现在拆，不记入清单

**理由**：BoatTracker当初的坑，成因就是"先实现、odom依赖以后再说"——中间隔了时间，回来时没人记得这个依赖，直接打开就用，跑出假数字，又花好几轮才拆解出"它从来没运行过"。清单不能防止这类问题（打开开关的人未必先读清单），**代码改对了才不需要任何人记得**。而且修法是已知配方（`CandidateTemporalClassifier`自己的base_link A/B就是BoatTracker那次同一批数据做的：世界系21.40% vs base_link系21.40%，误检仅+0.03pp），成本极低，不是引入新的未知。

另外，这类问题和端到端合规演练是互补而非替代关系——**默认关闭的功能，演练根本不会触发这条代码路径，演练会给出"一切正常"的结论，而地雷还在**。这类"默认关闭功能里藏着的合规问题"只能靠代码审查发现，是端到端测试的盲区，值得先做完代码审查、拆完已知地雷，再进演练。

### 修复：和BoatTracker完全同款的手法

移除`update()`的`odom_x/odom_y/odom_yaw`三个参数，内部不再做boat<->world坐标转换，直接在base_link系维护候选轨迹。加了`updateCount()`/`resetCount()`诊断（同BoatTracker的证明-真的在跑套路），验证：开启`candidate_temporal_enabled:=true`跑20秒bag段，`updates=84 resets=0`，`promoted`从0涨到12——确认真实运行，不是又一次"看起来改对了、实际没跑过"。默认值维持`false`不变，这次只是拆地雷，不改变当前默认行为。

**扩大grep范围排查同类模式**（"依赖比测不存在的输入 + 默认关闭"的组合）：

```bash
grep -rn "_valid\|_enabled" src/usv_perception/src/ | grep -i "odom\|gt\|truth"
```

只命中`CandidateTemporalClassifier`那一处（已修复）。没有发现第二个同类实例。

### 端到端合规演练（2026-08-21续）：跑得起来，但发现两个之前从未暴露的性能问题

只发布比测现场真实存在的话题（7路点云+6路图像，显式排除GT/odom/overhead_camera），外参注入±0.5m/±10°扰动（`test_extrinsic_perturb_seed`），实时速率回放（`--rate 1.0`，不降速）。

**合规性结论是干净的**：节点正常启动、正常收尾，无exception/segfault/abort；四个`_check`话题（buoys/boats/pillars/blocks）稳定10Hz，标准差仅0.003s；启动预热确认在几秒量级（`ros2 topic hz`监控开始后1-2秒内即达到稳定频率），不是之前估计的8秒或更长——之前"8秒启动延迟"的顾虑不成立，检测流水线按设计用名义外参立即启动，不等待标定收敛。

**但发现两个之前在有GT/无扰动环境下从未暴露的性能问题**：

**问题1：`optimizeExtrinsics`（相机拼接重投影误差诊断，ORB+暴力匹配+RANSAC，6路相机两两组合）混进了BEV渲染主路径，每帧无条件跑一次**。分段计时定位：占BEV分支总耗时约一半（104-126ms/~200ms），且其输出（`pair_results_`和CSV）不参与`blendBEV`的渲染结果——纯诊断副产品，混进生产路径里没摘出来。这是这条线上第三次同一类模式（`std::cout`精度污染、ROI固定半径制造假象、这次是诊断计算占了生产渲染一半耗时——形态不同，根子都是"调试期加的东西没有和生产路径分离"）。**修复**：`ImageStitcher`新增`enable_stitching_diagnostics`开关，默认关闭，分析拼接质量时才打开。SSIM回归检查确认零影响（0.7245/0.9194 vs 已知基线0.7257-0.7258/0.9210，差值在样本噪声内，std本身有0.003-0.005量级）。

**修复后的一次误判，及时被纠正**：改完后单帧耗时确实减半（p50从167-184ms降到81-88ms），但`ros2 topic hz`测得的平均发布频率反而从4.4-4.5Hz降到4.09-4.18Hz，最大间隔从0.792s涨到1.002s——第一反应是"改完更差了、还有第三个瓶颈"。**用发布间隔的分布形状（而非只看均值）复核后推翻了这个判断**：用同一个二进制、运行时切换`enable_stitching_diagnostics`参数（不用重新编译），分别捕获30秒窗口内的逐帧发布间隔：

```
                        OLD(diagnostics=true)   NEW(diagnostics=false)
均值                        199.7ms                 197.5ms
中位数                      199.8ms                 200.0ms
分布形状                    单峰，集中在200ms左右      单峰，集中在200ms左右
```

**两组分布几乎完全重合，都稳定卡在200ms（`image_timer_period_sec`的名义周期）附近，不是双峰。** 说明BEV分支的稳态发布节奏本来就由wall timer的固定周期决定，不是由单帧计算耗时决定——旧版本167-184ms的单帧耗时本来就没超过200ms周期，所以不存在"超时后立即补偿重触发"的机制；新版本单帧耗时降到81-88ms后，仍然要按固定周期等满200ms才发布，不会因为算得快就发得更快。**`ros2 topic hz`测出的4.4Hz→4.1Hz这个"变差"，是两次独立~35秒测试窗口里，罕见的离群尖峰事件（`buildGroundProjectionMaps`重建、标定分支占线程）恰好各撞上几次的样本噪声，不是修复引入的系统性退化。** 30秒稳态窗口的分布数据显示修复对典型情况没有负面影响，是纯收益，没有下修的必要。

**问题2（更根本，独立于BEV）：标定分支单次ICP尝试耗时（最高7.9秒）远超其重试周期（`calib_timer_period_sec=1.0`），导致"1Hz重试"这个设计名存实亡**。演练日志实测：35秒bag回放窗口内，标定只完成5次尝试（间隔5.2-7.5秒，和单次耗时量级吻合，说明是紧挨着连续执行，不是按1Hz节奏间隔执行）。**按这个实际速率，跑满`calib_max_attempts=60`需要约60×7秒≈420秒（7分钟），远超86秒的完整bag时长，也远超任何实际比测场景的时间预算**——这意味着在当前扰动量级下，`calib_done_`基本不可能通过"达到最大尝试次数"这条路径置位，标定分支会在整个测试期间持续占用`MultiThreadedExecutor`四个工作线程中的一个（该线程池同时要服务7路lidar+6路image共13个订阅回调和2个定时器）。用`calib_max_attempts:=1`做对照实验验证了这个机制真实存在：限制到只跑一次尝试后，BEV频率从4.09-4.18Hz回升到4.35-4.4Hz，最大间隔从1.002s降到0.596s——但即便如此仍未回到理论上限，说明标定线程争用只解释了部分差距，还有更小的常规争用（13个订阅回调共享4线程池）未定位，优先级低，不继续深挖。**这个问题没有修，是本次演练最重要的新发现，比BEV频率本身更值得投入**：它意味着"重试"这个安全机制在当前配置下基本不会真正生效，标定线程会长期占用宝贵的执行器线程资源。

**关于BEV频率本身是不是合规问题**：任务书对BEV的要求是发布`/world/obstacles/bev_check`话题、精度与俯视相机比对，**没有对发布频率提出硬性数值要求**（目前抠到的原文里没有）。4.1-4.4Hz vs 名义5Hz的差距，对一个用于评估图像相似度的话题，只要每帧内容和时间戳正确，实质影响很可能为零。**这个判断需要和BEV口径一起去问赛会确认**，但在此之前，标定线程占用这个问题应该独立于"BEV频率是否合规"来评估——它是资源占用/设计缺陷问题，不是频率是否达标的问题。

### 合规演练待办

- **标定单次ICP尝试耗时上限**：给`icpPointToPoint`或标定循环加一个耗时/迭代数熔断，避免单次尝试无界跑到7-8秒；或者把`calib_timer_group_`从共享的4线程执行器池里拆出来，用独立线程跑标定，不与检测/BEV路径竞争资源。两种修法代价不同，需要评估。
- **`calib_max_attempts=60`这个配置在当前ICP耗时下不现实**，需要重新核算一个和实际单次耗时匹配的、在合理测试时长内可达的数值，或者改成按总耗时预算（而非尝试次数）判断何时放弃。
- 标定分支长期占线程对lidar_branch/image_branch的具体影响量级，只做了粗略的`calib_max_attempts:=1`对照，没有做完整的定量归因（13个订阅回调的常规争用 vs 标定线程占用，各自贡献多少），如果后续要继续投入BEV频率问题，需要先把这个拆开。
- BEV频率是否是合规硬指标，和BEV口径一起列入待问赛会清单。

### ICP耗时定位（2026-08-21续）：三个假说依次验证，收敛到"局部收敛≠整体接受"

标定分支单次尝试7-8秒这个数字本身不合理，值得先查清楚慢在哪，再决定根治还是限流。分三轮验证，每轮都先读代码/预判，再埋点验证，不盲改。

**假说1：最近邻搜索是暴力O(n²)——读代码即被排除**。`icpPointToPoint`已经用`cv::flann::Index`（KD-tree）做最近邻查询，不是暴力搜索，动手前先读代码避免了一次无意义的埋点。

**假说2：调用结构是简单的"6路串行"——被结构证伪，实际是嵌套循环**。埋点定位：`2 phase × 5 round × 6 sensor` = 60次`icpPointToPoint`调用/次尝试，每次最多30次迭代。实测（3次独立捕获）：

```
calls=60  iterations=771-897  max_source=8287-8440点  max_target=15010-15099点
kdtree_build=548-565ms(9%)  nn_search=5202-6410ms(90%)  solve=8-10ms(<0.2%)  total=5784-6979ms
```

单次最近邻查询耗时约799ns——**KD-tree本身工作正常，没有退化，"换KD-tree根治"这个选项不存在，因为它已经在用了**。真正的成本是纯粹的量：60次调用×平均12.8-18次迭代×最多8440点/次查询≈650万次最近邻查询，每次都便宜，乘起来是5-7秒。这是"工作量太大"，不是"用错算法"，两者修法完全不同——前者是调参/减负，后者是换实现，本次调查澄清这不是后者。

**假说3（用户提出，本人验证）：早停可行——被数据干净否决，同时排除了一个更值钱的假说**：

失败调用（30次）全部恰好1次迭代退出（min=max=1），`matched<10`判据已经在iter=0就抓住了它们——**不存在"跑了几轮才发现要失败"的浪费**，早停没有可省的空间。成功调用（139次）平均18次迭代（20%跑满30轮上限），贡献2506次迭代、占总量98.8%——**成本几乎全部集中在正常收敛的调用里，不是失败调用的沉没成本**。

**假说3.5（用户提出，本人验证）：按传感器重叠率提前跳过低重叠对——收益比预想小得多，且用户自己指出了"重叠率跳过≈跳过注定被拒的部分"这个假设不成立**：

按传感器拆分成功率：`sensor 1/2/3/5=100%成功，sensor 4=93%，sensor 6(forward_lidar，EMX窄视场雷达，和其余6路Airy雷达360°×90°完全是两种几何)=0%`。**sensor 6本来就在iter=0失败退出，已经是最便宜的路径**，跳过它只能省约270ms（30次×~9ms KD-tree重建），量级远小于总耗时。**真正贵的sensor 1/2/3/4/5是在正常收敛、做真实有效的局部配准，不是"注定被拒还硬跑"，跳过会丢失有效结果，不该动。**

**最终发现，比三个假说加起来更重要——"局部收敛≠整体接受"**：本次捕获的2次标定尝试（`test_extrinsic_perturb_seed=42`扰动条件），日志确认全部"未收敛"（`res.ok=false`）。也就是说，**139次单传感器ICP数值收敛的迭代（占总迭代98.8%的成本）100%喂给了最终被拒绝、丢弃的整体尝试**——不是大部分被拒，是全部被拒。这解释了为什么"提前判断该不该跑"这个方向（无论是早停还是重叠率跳过）注定收益有限：卡住整体收敛的不是任何一次局部ICP的质量，是6路传感器要在5轮内同时收敛到一个自洽联合解这件事本身，在当前扰动量级下做不到（`round_has_update`没有在5轮内稳定），这和更早期session里"标定探到可辨识性边界"的结论方向一致、互相印证。

**证据强度要分清楚，避免文档误导后续判断**：
- **已确认（证据强，3次独立捕获+按传感器拆分验证）**：KD-tree无退化；调用结构是2×5×6嵌套；ICP数值收敛与标定整体接受是两回事；成本98.8%集中在成功调用；sensor 6因几何差异稳定0%局部成功
- **推定，非结论（样本仅2次尝试）**：整体接受率低。这个推定和"可辨识性边界"的方向一致、互相支持，**但本次2次尝试的样本量本身不足以独立支撑"大概率被拒"这个概率判断**——如果扰动幅度变小、或者做出Airy六路的精确配对关系，接受率可能完全不同。下一步决策（比如降`calib_max_attempts`）如果要用到这个概率判断，需要先补一次无扰动条件下的验证（无扰动是最有利条件，如果那时候都要跑很多次尝试才被接受，有扰动时更没戏；如果无扰动下第1-2次就成，`max_attempts`降到3-5就足够），不能直接拍一个数字。

**优化方向的排序结论**：ICP本身的减负（降采样/降迭代上限）优先级下调——既然整体尝试大概率被拒（推定），把单次尝试从7秒压到3秒的意义，不如直接控制"该不该重试这么多次"（即`calib_max_attempts`该降到多少，需要先做上面那个前置验证）。重叠率跳过方案被数据否决，不再考虑。**这条ICP性能线并入更早记录的`calib_max_attempts`待办，不再单独作为一个方向**。

### 标定回调组拆独立线程（2026-08-21续）：不依赖任何未决判断，已完成且实测有效

三项ICP减负方案（降采样、降迭代上限、重叠率跳过）都依赖"整体接受率低不低"这个还没独立确认的判断（样本仅2次尝试），拆线程这一项不依赖——不管ICP本身最后怎么优化、`calib_max_attempts`降到多少，把标定回调组从共享4线程执行器池里拆出去都是对的，解决的是合规影响（拖累检测/BEV），不是精度或效率问题，所以先做。

**实现**：`calib_timer_group_`创建时传`automatically_add_to_executor_with_node=false`，不再自动挂到`main()`里的`MultiThreadedExecutor(4)`；`main()`里新起一个`SingleThreadedExecutor`+独立`std::thread`，只跑这一个callback group。标定本身不受影响（新增`calibCallbackGroup()`公开访问器供`main()`拿到这个group）。

**验证**（两次独立跑测，`test_extrinsic_perturb_seed=42`扰动条件）：

```
                        修复前(仅optimizeExtrinsics关闭)   修复后(+拆线程)
bev_check频率              4.09-4.18Hz                    4.6-4.98Hz
最大发布间隔                 1.002s                          ~0.6s
四个_check话题(10Hz)         不受影响                        不受影响，稳定10Hz
标定行为                    5次尝试/86秒，未收敛              相同（5次尝试，相同"未收敛"结果）
```

两次跑测结果一致（4.6-4.98Hz vs 4.656-4.681Hz，最大间隔0.604s vs 0.607s），节点正常启动、正常收尾，无崩溃。**这是这几轮合规演练里三个修复中收益最大、且没有任何附带风险的一项**——不改变标定的收敛行为，只是让它不再拖累其他分支。

### 当前状态：BEV频率问题基本解决，标定"该重试几次"仍待前置验证

三个修复叠加后，BEV频率从最初的4.4-4.5Hz（含隐藏的optimizeExtrinsics开销和标定线程争用两个问题）恢复到接近名义5Hz的4.6-4.98Hz。剩余待办收敛为一项：`calib_max_attempts`该设多少，需要先做无扰动条件下的前置验证（第几次尝试能被接受），再决定具体数值，不能直接拍。
