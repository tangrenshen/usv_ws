# 自测报告（骨架）

> 提交清单第6项要求材料。结构对齐DG-202608评分细则"测试数据评价方法与评分
> 细则"一节的指标表格，逐项标注数据状态：已有/部分/缺失，以及缺失原因和
> 补齐路径。本文档目的是暴露"哪里真的没数字"，不是最终提交稿。

## 一、环境感知软件包

评分细则表格5项基础指标 + 1项附加指标。

### 1. 感知系统标定方法 【专家评分项，不产出跑测数字】

不参与排名打分，由专家组按科学性/自动化程度/适应性评审。测试方法本身不需要
数字，需要的是方案陈述材料——已完成，见`report/calibration_chapter.md`。
**状态：内容已具备，仍需第三方（你）过一遍技术准确性**（见该文件开头的
待办说明）。

### 2. 拼接重叠像素（环视拼接重叠区平均重投影误差，px）【状态：已成章】

完整方法论、数字与适用范围见`report/stitching_reprojection_chapter.md`
——单次运行内稳健聚合(中位数/截尾均值)+5次运行间算术平均的两层方法、
"平均"字面歧义的正面回应、0.9998px最终数字、以及两条适用范围限制（名义
外参下测得、5份录制场景配置相同未含随机外参扰动）均已写入该章节，此处
不重复。

### 2b. 鸟瞰图精度（与俯视摄像头成像数据的像素误差）【状态：链路已建成，有单次运行数字，待5-run】

**此前骨架完全缺失这一项**——它是任务书3.1.2基础指标的第1项，与评分细则
表格里的"拼接重叠像素"是两个独立指标：后者只看我们自己相邻两路的几何
一致性（不需要外部基准），前者拿整幅鸟瞰图与俯视相机照片逐像素比。完整
方法与数字见`report/bev_accuracy_chapter.md`，此处只列状态。

**已有**（`bags_5run_v2` run1、30帧）：整图 MAE 13.21、RMSE 19.23
（曝光均衡后；均衡前为14.31/23.00）。分区域：纯水面12.25、目标邻域16.61
（占有效区17.4%）。

**链路中最关键的一步是俯视相机等效高度的实测标定**：任务书写13.0m，实测
拟合为11.77m（对水面约12.1~12.3m），残差0.70px、6px内占比100%。照搬标称
值会引入10%径向尺度误差，使整幅图出现由内向外递增的系统性错位——这一项
的数字将完全失去意义。该差异是第三处"任务书标称值与仿真实际不符"（雷达
2.90/实测1.85、鱼眼3.00/实测1.95，前两者差值精确同为1.05m），已列入待
赛会确认事项。

**指标本身的局限须一并呈报**：区分力仅约1.45倍（正确对齐13.21 vs 参考图
转90°的19.12），因为画面绝大部分是水面，而水面镜面反射在掠射视角与正上方
视角下本就不同，这部分误差与拼接质量无关且对所有队伍一致。

**待补**：按纪律需在5份独立录制上重跑并给出区间，现有数字为单次运行量级。

### 3. 周围目标识别精度（三维位置+轮廓形状误差）【状态：轮廓已有真实5-run数字，位置误差待补】

**位置误差**：静态三类（buoy/pillar/block）在`--sensor-clock-gap`修正后，
位置误差从1.0-1.5m降到0.19-0.70m（历史结果，需要按纪律1在提交前重新跑一次
确认这个数字在当前代码状态下依然成立，不能直接引用）。boat作为唯一动态类别，
该修正对它有害——**在新5-run数据集上重新确认了这一点**：不加修正时boat
100%帧被跳过（0/0，测不出数字）；强制加上修正后测出3.2%召回/89.6%误检，
比不修正更糟。boat目前在本方案的评估体系里完全没有可用的位置误差测量口径，
见下方"跨模块共同待办"第4条，这是一个需要专门解决的缺口，不是"数据还没测"
那么简单。

**轮廓/尺寸误差**：本session的核心产出，见`report/contour_accuracy_chapter.md`，
已用5次真正独立的随机世界初始化验证（自建仿真录制）：
- 0-20m基本无偏（-4.9%~+9.0%，跨5次独立初始化稳定，无系统性方向偏置）
- 40m+系统性收缩约40-53%（-38.1%~-53.1%，跨5次独立初始化方向和量级都高度
  一致，已确认为几何近端面效应而非采样稀疏）
- 20-40m**偏差方向本身不稳定**（-25.9%~+10.7%，5次里2次为负、1次接近零、
  2次为正）——这比"量级散布大"更能说明问题：任何固定方向的补偿系数在部分
  场景下都会把误差推向反方向，是"不做补偿"这一决定里论证力度最强的证据
- 已判定本轮不引入尺寸补偿（风险不对称：40m+所需的~2倍放大系数，在5次独立
  随机世界初始化下已有约15个百分点的自然波动，比测的随机外参扰动会在此
  基础上进一步放大波动）
- 仅覆盖pillar（主）+block（次，旋转修正），buoy因类型歧义排除，boat/z轴
  推后

**待补**：boat位置误差基线（需要专门方案，见跨模块待办第4条）；buoy轮廓
（需要先解决类型歧义，见第五节讨论的"双峰识别"思路）；z轴尺寸（需要先统一
检测侧/GT侧的水面裁剪基准）。

### 4. 周围目标识别遗漏率【状态：已有真实5-run数字，但揭示了旧基线的方法论局限】

**重要框架调整**：旧基线（37.7%/47.8%，同会话3次取中值）测的是**同一场景
重复测量的噪声**，不是评分细则"5次不同初始化条件下独立运行"要求的**场景
泛化方差**——这是两个不同的量，旧数字不是错的，是回答了不同的问题，本节
不再引用它作为跨场景基线。

**用5次真正独立的随机世界初始化重测**（自建仿真录制，boat/buoy/pillar/
block合计，boat因下述原因排除在外）：**按赛会2026-09-02口径重算**，5次运行
总遗漏率33.0%~41.4%、总误检率7.5%~20.4%、分类错误率1.7%~4.0%（逐类见检测
章节§2.1）——散布仍远大于旧的"同会话3次"方法论所能揭示的范围。已定位主要
驱动因素：立柱中位距离与召回率负相关，**按新口径重算后 r=−0.756（n=5）**
——注意该系数比按旧口径算出的 r=−0.95 明显更弱，且n=5时达不到统计显著性，
因此其定位是"方向与已独立量化的距离衰减机制相吻合的观察"，而不是由相关
系数本身证明的结论（见轮廓章节40m+发现）。最差的一次运行（立柱召回33.5%，
其余4次57.2%~64.1%）对应5次里立柱中位距离最远(92.4m)、且60m量程内可见
立柱数最少的一次。

**这一发现本身是有分量的方法论结论，应该写进报告**：感知指标对场景配置
高度敏感，同场景重复测试会显著低估真实方差，这正是评分细则要求5次独立
随机初始化的原因——本方案的意外发现印证了评分细则设计的必要性，而不只是
被动符合它。报告中给出的召回/误检数字必须带上5次跑测的区间，不能只写
单一平均值。

**boat**：按赛会口径已可测出——遗漏率94.5%（区间92.3%~96.5%）、误检率
44.7%、分类错误率23.8%、位置误差0.66m。该数字比历史记录的"检出率15~18%"
更差，原因是新口径"没有发布在指定话题即视为未检出"使23.8%的分类错误
在boat类别里全部计漏检。成因已定位到**级联顺序**（非点数门槛，旧结论已于
2026-09-02重测作废），见检测章节第5.3节
与跨模块待办第4条。

### 5. 感知融合精度【状态：数据条件限制，暂时测不了】

已确认无法在现有录制数据上建立真正基线：没有精准点云真值话题，唯一可用的
静态真值参照（立柱）在0-20m样本量不足（历史调查：仅2根立柱）。诊断本身有
交叉验证价值（两条独立评估路径互相印证了19.4%的检测线基线），但**融合精度
本身的评分只能在比测现场用赛会提供的精准点云完成，自测报告里应如实说明这一
数据条件限制，而不是勉强给一个不可靠的数字**。

### 6. 附加指标：实船部署验证【状态：未开始】

需要在实船（4路半球雷达+4路鱼眼摄像头）上完成360环视/虚拟场景/融合的部署
验证，现场确认后加5分。本session全部工作基于仿真录制数据，未涉及实船。

## 二、规划控制软件包【状态：不在本session范围】

静态/动态障碍物避障成功率、规划周期、停靠精度、撞击速度、任务完成时间
——本session未涉及规划控制模块代码，无数据。需要确认这部分由谁负责、
进度如何。

## 三、锁定与释放模块【状态：不适用于自测报告】

该模块评分细则明确"不设仿真测试"，计入方案评审的报告完整性/可行性部分，
自测报告不需要覆盖，但综合研究报告需要单独的设计资料章节（机械/电气/控制
电路/交互协议），本session未涉及。

## 四、跨模块共同待办

0. **鸟瞰图精度（第2b项）需在5份独立录制上重跑**——现有13.21/19.23来自
   单次运行30帧。另需注意`bags_5run`（含雷达、无俯视）与`bags_5run_v2`
   （含俯视、无雷达）两个数据集互补但不重合，任何需要"雷达检测结果 + 俯视
   基准"同时在场的评估（例如判定遮挡掩膜对鸟瞰图误差的净影响）都需要补录
   一批同时含两者的短包。本轮未做，因为已量化拖影权重仅约0.8~1.0灰阶。
1. 按纪律1，本文档里标注"历史数字"的几处（0.19-0.70m位置误差）在最终定稿
   前都需要用当前代码状态重新测一次，不能直接誊抄。召回/误检基线(第4项)
   和拼接重投影误差(第2项)已用5次独立随机世界初始化重测，不再是待办。
2. ~~5次独立跑测的数据缺口~~——已解决：自建仿真环境可本地录制任意数量的
   独立初始化场景（`ros2 launch vrx_gz competition.launch.py world:=
   perception_user`，每次launch生成新随机种子），不依赖赛会额外提供数据，
   已用5次自录场景重测拼接重投影误差、轮廓/尺寸误差、召回/误检基线三项。
3. GT标注范围的赛会答复已到（"雷达能测到的都算立柱"）。**该项的影响量级
   已大幅缩小**：80.7%是旧数据集加旧代码的产物，按新口径在当前代码5个独立
   场景上重算，立柱误检率为0.9%~3.9%，第4项的数字已按新口径全部重算完毕。
   **仍需保留的口径说明**：每次录制都是全新的随机世界，未标注结构是否存在、
   位置在哪都会变化，因此pillar误检数字始终带着这一不确定性；只是其影响
   量级已从数十个百分点降到个位数，不再是需要优先催办的事项。
4. **【已排查完毕，2026-08-29，结论：不是评估方法的问题，是已知的检测召回
   问题】** boat强制加`--sensor-clock-gap-boat`后测出3.2%召回/89.6%误检，
   一度怀疑是评估方法（时钟对齐）本身有缺陷。排查链条：(a) 直接测量
   det-vs-gt时间戳残差，稳定在17-50ms，无漂移——排除时钟精度问题；
   (b) 检验odom自身消息到达的真实时序（在原始录制文件里odom最后一条记录
   与gt最后一条记录几乎同时出现在99.2%的位置），确认odom消息覆盖了完整
   会话时长，只是其自身header.stamp的推进速率比真实时间慢（约0.608倍，
   与7雷达+6相机的高计算负载下原始录制时仿真实时率<1一致）；对这一发现
   实施了速率修正的时间映射，重新测试——**没有修复残差**，排除了线性时间
   映射（无论是否带速率修正）能解释问题的可能；(c) 直接从GT算每条船的
   真实速度，中位数约1 m/s，最高约1.5-1.6m/s，物理上完全合理，且**这个
   速度量级本身就排除了时间误差作为残差成因的可能**——10-18m的残差如果
   要用时间误差解释，需要十几秒的时钟偏差，而实测精度是17-50ms，差了近
   三个数量级；(d) 对检测到的boat位置做贪心帧间最近邻跟踪，最长的track
   （515点、跨68秒）隐含速度中位数仅0.08m/s（与真实船速吻合），但混入
   最高23.34m/s的异常跳变——这是稀疏检测下跟踪器把不同的船误认成同一条
   的典型signature，不是单个检测目标本身在漂移。
   **结论需要拆成两件事，不能合并成一句"没问题"：**
   1. **评估方法论本身没有缺陷**——这是本次排查证明的：时钟对齐精度、
      时间映射方式、跟踪关联逻辑都已逐一验证，均不是残差的成因。
   2. **boat遗漏率94.5%（新口径重算），是一个真实的、尚未解决的性能缺口**——这是
      已有排查记录的既有结论，不是本次调查的新发现，也不是"因为评估
      方法没问题所以这个缺口不重要"。每帧只检出10条船里的3-5条，且
      具体检出哪几条随时间变化（船在移动、进出可见范围），这个召回率
      本身仍然是感知模块的一个实质短板。

   **不再投入时间修复评估方法本身**（第(1)条已证明不需要）——但第(2)条
   的召回缺口依然存在，自测报告里"周围目标识别精度"这一项对boat的诚实
   表述应该是："评估方法论已验证无缺陷；受限于boat检出率偏低（新口径下
   遗漏率94.5%，根因已定位到级联顺序），现有检出样本
   不足以支撑有意义的位置/轮廓误差统计"——把方法论清白和性能缺口分开说，
   不能读起来像是把一个真实的检测性能问题包装成了"评估工具的问题，已
   排除"。

   **成因已于本轮定位（2026-08-31）**，第(2)条的表述可相应加一句而不改变
   其性质：簇级诊断（`enable_cluster_diagnostics`）显示，GT船只实例中
   **【2026-09-02 重测更正】**用当前代码在5份录制上重测3815个真值船簇：
   正确检出44.7%、被浮块分支取走32.2%、被浮球分支取走23.1%、
   **被丢弃0.0%（5份方差为零）**。原结论"仅因点数不足被丢弃"不可复现，
   已作废。真正机制是级联顺序（block→buoy→pillar→boat）把船只分支的
   有效视野夹成 fp_max∈(1.0,1.65)；独立佐证是点数门槛40→10仅多挽回
   2个真阳。距离与运动两个直觉解释此前已被数据证伪。详见第5.3节。
   **注意这不改变第(2)条的结论**——门槛调整的误检代价尚未测量（噪声
   碎片8-12点与待挽回真船簇10-14点分布高度重叠），缺口仍然是真实且
   未解决的，只是从"已系统排查、根因待定"变成"根因已定位、代价待量化"。
